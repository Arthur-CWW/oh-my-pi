id: effect-no-unknown-error-channel
valid:
  - |
    interface Deps {
      readonly load: () => Effect.Effect<string, LoadError>
    }
invalid:
  - |
    interface Deps {
      readonly load: () => Effect.Effect<string, unknown>
    }
  - |
    function load(): Effect.fn.Return<string, unknown> {
      throw new Error("boom")
    }
