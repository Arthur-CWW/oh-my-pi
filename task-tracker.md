# Migration Task Tracker

_Last updated: 2026-03-12_

State legend (symbol-only):
- `[ ]` Open
- `[@]` Ongoing
- `[@User]` Checking (awaiting user validation)
- `[x]` Checked
- `[~]` Obsolete
- `[?]` In Question

## Tasks

- [x] Create migration baseline (old/new folder split, docs, handoff context).
- [x] Add initial boundary tests and e2e smoke scripts.
- [x] Enforce no-`any` rule in agent instructions and testing expectations.
- [x] Add running task workflow to AGENTS.md and create this tracker.
  - Result: tracker + state model added.

- [x] Add Effect TS dependencies and scaffold core modules (`Config`, `Errors`, `Http`, `Observability`) with tests.
  
- [@User] Remove dynamic `import("node:sqlite")` usage from Effect event store loader path.
  - Result: Reworked `src/effect/observability/EventStore.ts` loader to use static `createRequire`-based driver resolution (`bun:sqlite` first, `node:sqlite` fallback) with no runtime `import()` calls in the Effect path; preserved dual-runtime behavior and validated via typecheck + event-store/effect-index tests + full suite.
- [x] Extract Effect search runtime with optional fail-open SearchEvents service (in-memory/SQLite sink) and wire `web_search` default path through it.
  - Result: Added `src/effect/search-runtime.ts` (Effect.fn-instrumented provider selection/fallback flow) and `src/effect/search-events.ts` (optional fail-open event service with noop/in-memory/SQLite paths); wired Effect entry default `web_search` through runtime + optional `PI_WEB_ACCESS_EVENT_DB_PATH` sink initialization; surfaced `correlationId` in `web_search` details; added `tests/search-runtime-events.test.ts` covering Kagi success, Kagi→Gemini fallback, total failure, and SQLite event persistence.
- [x] Replace raw tool param casts in `src/effect/index.ts` with Effect Schema boundary decoding and actionable validation errors.
  - Result: Added Effect Schema param contracts (`EventStoreSmokeParamsSchema`, `WebSearchParamsSchema`, `CookiesParamsSchema`) and runtime decode helper using `Schema.decodeUnknown` + `ParseResult.TreeFormatter`; removed raw `as` casts in tool execute paths; added consistent invalid-parameter responses and maintained existing success/error flows; expanded `tests/effect-index.test.ts` with schema-validation assertions for `effect_event_store_smoke`, `web_search`, and `chrome_cookies`.
- [@User] Decouple Effect Gemini search path from `src/old/{gemini-api,gemini-web}` by introducing Effect-owned Gemini API/Web modules (no dynamic imports).
  - Result: Added `src/effect/gemini-api.ts` and `src/effect/gemini-web.ts` (Effect-owned Gemini API/Web helpers with static imports only), switched `src/effect/gemini-search.ts` to consume the new modules, and added coverage in `tests/gemini-web-effect.test.ts`; also added `src/effect/chrome-cookies-legacy.ts` and rewired `src/effect/chrome-cookies.ts` to remove direct `src/old/chrome-cookies` imports from Effect code paths.
- [x] Remove unused Effect core HTTP abstraction (`src/effect/core/Http.ts`) and related dead error types now that runtime paths are service-specific.
  - Result: Removed `src/effect/core/Http.ts`, pruned unused HTTP tagged errors from `src/effect/core/Errors.ts`, updated `src/effect/core/index.ts` exports, and refreshed `src/effect/README.md` foundation module list.
- [@User] Add a controlled legacy-bridge kill switch in `src/effect/index.ts` for migration testing (`PI_WEB_ACCESS_DISABLE_LEGACY_BRIDGE=1`) while preserving default behavior.
  - Result: Added `isLegacyBridgeDisabled()` gate in `src/effect/index.ts` so the legacy registrar is skipped when `PI_WEB_ACCESS_DISABLE_LEGACY_BRIDGE` is set to `1`/`true`; default behavior remains unchanged. Expanded `tests/effect-index.test.ts` with env-gated coverage asserting legacy tools are omitted while Effect tools still register.
- [@User] Unify search provider/recency contracts via shared Effect Schema module and align `core/Config` provider parsing (including `kagi`).
  - Result: Added `src/effect/search-contracts.ts` with shared provider/recency schema contracts + value sets; switched `src/effect/index.ts` and `src/effect/core/Config.ts` to consume shared contracts; moved `core/Config` decoding to Effect Config + `ConfigProvider.fromJson` flow; updated `src/effect/gemini-api.ts` to read env/file key paths via Effect Config; wired `src/effect/gemini-search.ts` CLI execution path through `@effect/cli` command parsing (while keeping existing parse helper exports for compatibility tests); removed user-facing `auto` provider selection from `web_search` tool boundary so omitted provider now defaults to Kagi/fallback flow.
- [@User] Align Effect package dependency versions and add Effect CLI stack with compatible peer versions.
  - Result: Added `@effect/cli@0.73.2` plus required printer peers (`@effect/printer@0.47.0`, `@effect/printer-ansi@0.47.0`); pinned Effect package versions in `package.json` (`effect`, `@effect/platform`, `@effect/platform-node`, `@effect/schema`, `@effect/language-service`) to avoid cross-version drift during migration.
- [@User] Extract `get_search_content` into an Effect-native tool path and keep legacy-output parity while bridge remains for `fetch_content`.
  - Result: Added flattened `src/effect/search-content.ts` with schema-first stored-result decoding + Effect.fn execution, registered Effect-owned `get_search_content` in `src/effect/index.ts` (overrides legacy tool when bridge is enabled and remains available when bridge is disabled), and expanded `tests/effect-index.test.ts` with legacy-format contract checks + schema-validation coverage.
- [@User] Extract `fetch_content` into an Effect-native tool path while preserving legacy output/storage behavior.
  - Result: Added flattened `src/effect/fetch-content.ts` with schema-first params, Effect-native execution, legacy-compatible output/storage formatting, session persistence hooks, and legacy-style `renderCall` / `renderResult` parity in `src/effect/index.ts`; registered Effect-owned `fetch_content` so it overrides the legacy tool while the bridge remains and stays available when the bridge is disabled; added colocated `src/effect/fetch-content.test.ts`, kept entry-boundary validation in `tests/effect-index.test.ts`, and updated root/package test scripts to include colocated tests; validations run (`bun run typecheck`, `bun run test`, `bun run test:e2e:cookies`, `pi --no-extensions -e ./src/effect/index.ts --help`; Gemini e2e still fails in this environment because the configured Gemini API key is quota-exhausted and Gemini web cookies are unavailable).
- [@User] Refactor the general web-page `fetch_content` pipeline behind the Effect-owned boundary (HTTP/HTML extraction + Jina/Gemini fallback orchestration), preserving stored output parity before tackling GitHub / YouTube / local-video paths.
  - Result: Added `src/effect/fetch-content-runtime.ts` to own the general web-page extraction path (HTTP/HTML readability + RSC/PDF handling, Jina fallback, Gemini URL-context fallback, Gemini web fallback) while explicitly delegating GitHub / YouTube / local-video / timestamp flows to the legacy extractor for now; rewired `src/effect/{fetch-content,index}.ts` defaults to the new Effect runtime; added colocated `src/effect/fetch-content-runtime.test.ts` covering HTTP success, Jina fallback, Gemini API fallback, Gemini web fallback, non-recoverable errors, legacy delegation, and final guidance parity; validations run (`bun run typecheck`, `bun run test`, `bun run test:e2e:cookies`, `pi --no-extensions -e ./src/effect/index.ts --help`; `bun run test:e2e:search:gemini` remains blocked here because Gemini auth is unavailable in this environment).
- [@User] Refactor remaining `packages/kagi/*` runtime helpers (session/lenses/advanced/rules) to Effect-native package interfaces with package-scoped typed errors.
  - Result: Added `packages/kagi/src/kagi-client-effect.ts` with Effect-native wrappers for session refresh/load/save, lens discovery, advanced-search redirect, and domain/video rule mutations; kept provider-specific typed errors package-local (`session-unavailable`, `storage-failed`, `unauthorized`, `forbidden`, `rate-limited`, `http-error`, `request-failed`, `invalid-target`); added package-local `packages/kagi/test/kagi-client-effect.test.ts` and updated `packages/kagi/package.json`/`packages/kagi/README.md`; validations run (`bun run typecheck`, `bun run test`, `bun run test:e2e:cookies`, `pi --no-extensions -e ./src/effect/index.ts --help`; Gemini e2e still requires local auth/API and fails without credentials in this environment).
- [@User] Refactor Kagi package surface to provide Effect-native search interface + provider-specific error classification; keep Effect app layer thin.
  - Result: Added `packages/kagi/src/kagi-search-effect.ts` with Effect-native `runKagiSocketSearchEffect` and typed Kagi-specific error classification (`session-unavailable`, `unauthorized`, `forbidden`, `rate-limited`, `http-error`, `request-failed`); switched `src/effect/kagi-search.ts` to consume this Effect interface (removing Kagi HTTP/session branching from Effect app layer); updated Kagi tests to inject Effect deps and added `packages/kagi/test/kagi-search-package-effect.test.ts` covering package-level error classification.
- [@User] Keep Effect migration internals Effect-first and push `async`/`Promise` usage to explicit boundaries only.
  - Result: Refactored `src/effect/{gemini-web,gemini-search,search-runtime,index}.ts` to keep provider orchestration and web/API calls in `Effect` values (typed tagged errors + `Effect.fn`), leaving `async` wrappers at CLI/tool execute boundaries; fixed `src/effect/observability/EventStore.ts` row-decoding guard regression; updated tests in `tests/{gemini-web-effect,gemini-search-effect,effect-index}.test.ts`; validations run (`bun run typecheck`, scoped tests, full `bun run test`, `bun run test:e2e:cookies`, `pi --no-extensions -e ./src/effect/index.ts --help`; Gemini e2e requires local auth/API and still fails without credentials).
- [@User] Reorganize Kagi-specific tests into package-local `packages/kagi/test` and reserve top-level `tests/` for repo-level coverage.
  - Result: Moved package-owned Kagi tests to `packages/kagi/test/*`, moved the thin Effect adapter coverage to `src/effect/kagi-search.test.ts`, updated `packages/kagi/package.json` + Kagi docs/handoff references, and persisted the new repo layout preference in `AGENTS.md`; scoped Kagi tests + `bun run typecheck` + `bun run test` + `bun run test:e2e:cookies` + `pi --no-extensions -e ./src/effect/index.ts --help` passed, while `bun run test:e2e:search:gemini` remains blocked here because Gemini auth/API credentials are unavailable in this environment.
- [@User] Migrate `src/effect/*` + package Effect code toward the local `vendor/effect-smol` v4 stack and local CLI modules, while leaving `src/old/*` untouched.
  - Result: Switched root Effect deps to local `vendor/effect-smol` packages (`effect`, `@effect/platform-node`, `@effect/platform-node-shared`), built the vendored packages for local dist-backed resolution, removed the old `@effect/cli` / split v3 package setup and language-service patching, refactored `src/effect/*` runtime code onto v4-compatible APIs (`Data.TaggedError`, `ServiceMap.Reference/Service`, `Schema.decodeUnknownExit`, `ConfigProvider.fromUnknown`, `Effect.result`, `Effect.catch` / `catchDefect`), migrated the remaining direct CLI surfaces in `src/effect/{gemini-search,chrome-cookies,kagi-search}.ts` to local `effect/unstable/cli`, and simplified several Effect modules while leaving `src/old/*` untouched; validations passed for `bun run typecheck`, `bun run test`, `bun run test:e2e:cookies`, and `pi --no-extensions -e ./src/effect/index.ts --help`, while `bun run test:e2e:search:gemini` remains blocked in this environment because Gemini auth/API credentials are unavailable.
- [@User] Simplify the Effect entry/tool organization, move Effect slice tests into adjacent `test/` folders, and remove Effect imports from `src/old/storage` by introducing a shared storage module.
  - Result: Moved Effect slice tests from colocated `src/effect/*.test.ts` into `src/effect/test/*`, extracted fetch-content tool rendering out of `src/effect/index.ts` into `src/effect/fetch-content-render.ts` (also removing the remaining `../old/utils` import there), introduced neutral shared stored-result helpers in `src/shared/stored-results.ts`, rewired `src/effect/{fetch-content,search-content}.ts` and repo-level tests to use the shared module, kept `src/old/storage.ts` as a thin legacy wrapper for session restore + re-exports, and removed low-value CLI help/storage helper tests encountered during the cleanup pass.
- [@User] Move the legacy implementation out of `src/old/*` into `packages/legacy-web-access` and update repo/runtime references to the new package boundary.
  - Result: Moved the legacy implementation to `packages/legacy-web-access/src/*`, added `packages/legacy-web-access/package.json`, rewired all repo/runtime references (legacy bridge loader, Effect fetch runtime, old/reference tests, and e2e scripts) to the new package path, updated TS/package include lists, and kept the legacy package isolated while preserving current behavior.
- [@User] Simplify/remove unnecessary custom abstractions in `src/effect/core/*` and `src/effect/observability/*` in favor of direct Effect/platform APIs.
  - Result: Removed unused wrapper modules (`src/effect/core/{Errors,Observability,index}.ts`, `src/effect/observability/index.ts`), added flat `src/effect/search-event.ts` for shared event typing/construction, localized config + sqlite event-store error types into their owning modules, simplified `src/effect/search-events.ts` to use direct Effect fail-open logging, and trimmed `tests/effect-core.test.ts` down to config behavior instead of removed helper abstractions.
