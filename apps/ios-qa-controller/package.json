{
  "name": "ios-qa-controller",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Owned/supervised iOS device QA controller MVP with human-in-the-loop console and audit log",
  "scripts": {
    "start": "node src/server.mjs",
    "smoke": "node scripts/smoke.mjs",
    "check": "node --check src/server.mjs && node --check scripts/smoke.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
