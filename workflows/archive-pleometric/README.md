# archive-pleometric

Workflow skeleton for archiving selected public Pleometric tweets, threads, quote tweets, and media into the local Twitter/X archive.

Use low concurrency, cache all requests/captures, and scope browser capture to the X/Twitter main column plus search input only.
