# Companion QA triage — 2026-07-15

Inputs: `local/motion-oracle-qa/report.md`, `local/motion-debug-qa/report.md`, `local/provider-swap-qa/report.md`, `local/provider-compare-qa/report.md`, `local/lab-resizable-qa/report.md`, and `local/center-split-qa/report.md`. No `replay-v3-qa` directory was present. The companion log was read at `data/ai-companion-rtc/errors.log`; it contains older server-side quota/connection rows plus the fresh private-lab browser-hook proof listed below.

| Finding | Disposition | Evidence / reason |
|---|---|---|
| Motion Oracle checkbox names were exposed as generic `on` (P2/a11y) | **Fixed** — `apps/ai-companion-rtc/public/lab/controls.tsx`: explicit `aria-label` on Receive Apple Vision, Drive body, and Drive hands inputs. | Private `/lab` inspection returned all three intended names. |
| Motion Oracle keyboard traversal from panel summary did not advance in the cmux backend (P2/a11y) | **Deferred** — browser-backend evidence gap; direct control focus worked, but this run cannot distinguish cmux traversal behavior from a product defect. | Re-run with a browser exposing focus-step history. |
| Motion Oracle source/packet-age presentation was reported missing (P1/P2 evidence) | **Verified current** — `apps/ai-companion-rtc/public/motion-oracle-transfer.ts` already renders the protocol source and packet age; the report predates the current bundle. | No contested-file edit; needs a fresh live capture if this gate must be re-attested. |
| Motion Oracle stale-release test was blocked by live sidecar interleaving | **Deferred** — environment limitation; no sidecar was killed. | Requires an isolated no-packet UDP window. |
| Body-only/hands-only avatar motion was not proven with the supplied synthetic replay | **Deferred** — fixture lacks required body core and full Hand-21 geometry; no feature change is justified by invalid input. | Use a complete body + Hand-21 fixture for the evidence rerun. |
| `/api/debug/lab-state` returned `null` during the earlier oracle run (P1) | **Verified current** — private 4911 instance returned HTTP 200 with a non-null fresh whitelisted snapshot after the heartbeat. | Current telemetry path is working; earlier capture was timing/build evidence, not reproduced. |
| Responsive 1440×900 / 1024×768 evidence was not honored by cmux | **Deferred** — cmux reported a fixed ~730×993 surface for both requests. | Requires a viewport-capable browser backend. |
| Whole-session console capture was unavailable in Motion Oracle and Motion Debug QA | **Deferred** — evidence-tool lifecycle/agent termination gap; no product warning/error was observed in the captured portions. | Re-run with console listeners installed before navigation. |
| Browser errors from `/lab` were absent from the one error log | **Fixed** — `apps/ai-companion-rtc/public/lab/browser-error-log.ts`, imported by `public/lab/lab.tsx`; uses existing trusted `/error-report`. | Private 4911 test dispatched `window_error` and `unhandled_rejection`; both appended to `data/ai-companion-rtc/errors.log` with route `/lab`. |
| Motion Debug guard strip showed idle diagnostics during synthetic replay | **Deferred** — current evidence was synthetic-only and did not exercise live camera diagnostics. | Arthur/live-camera QA remains required. |
| Motion Debug showed stale `arbiter not mounted` fallback text | **Fixed** — `apps/ai-companion-rtc/public/lab/motion-debug/stage.tsx` now says `No capture-lane snapshot` when the current frame has no arbiter payload. | The arbiter is shipped; null means no snapshot in that frame, not an unmounted subsystem. |
| Provider Compare rig screenshots did not visually prove arbiter ownership | **Deferred** — screenshot limitation; lifecycle test already verifies provider-specific replay-tier registration/release. | No product defect demonstrated. |
| Provider swap, resizable panes, and center split reports had no remaining open product finding | **No action** — all reported gates passed. | Their report commands/evidence were read and retained as the current proof. |
| Docs referenced retired monolithic lab files / legacy stylesheet | **Fixed** — updated `streams/companion/HANDOFF-BEHAVIOR.md`, `streams/companion/notes/lab-design-system.md`, `streams/companion/notes/anichat-pipeline-recon.md`, and `docs/state/tasks/done-2026-07.md` to current `public/lab/` / `lab.css` paths. | Repository-wide markdown search finds no `lab-system.css`, `public/lab.ts`, or backtick `lab.ts`/`lab.css` references. |

## Verification

- `cd apps/ai-companion-rtc && bunx tsc --noEmit` — passed.
- `cd apps/ai-companion-rtc && bun run build:lab` — passed.
- Private browser test on `http://127.0.0.1:4911/lab` — labels, browser error POSTs, and non-null lab telemetry verified; private process was stopped afterward.
- `cd apps/ai-companion-rtc && bun test` — 471 pass, 0 fail, 6,373 assertions.
