# Desktop book inventory

Remote host: `desktop` (`/home/arthur`). Read-only SSH inventory run 2026-07-07.

Existing requested home dirs: `/home/arthur/Downloads`, `/home/arthur/Documents`. Missing or non-matching requested dirs: `/home/arthur/books`, `/home/arthur/Books`, `/home/arthur/Calibre*`.

Calibre libraries (`metadata.db`) within `~` maxdepth 5: none found.

No matching `.epub`, `.mobi`, `.azw3`, or `.pdf` files over 100 KiB were found within maxdepth 4 of the existing requested roots.
