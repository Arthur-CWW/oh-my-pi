# hsk5-001-060 report

Rows: 60 first-array entries. Cards: 49. Skips: 11 function/bound/measure-word entries. All cards are T2 and include retrievalTarget, verdict, and reason.

Anomalies: normalized 女性 pinyin from source `nu:3 xìng` to `nǚ xìng` after CEDICT check. Skips are recorded per JSON row in `skipReason`.
