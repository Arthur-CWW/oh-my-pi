> Rescued 2026-07-13 from /Users/arthur/.omp/agent/sessions/-agents/2026-07-07T00-00-51-895Z_019f39e0-a6b7-7000-a848-f3e75d966101/local/resume-forensics.md

# ResumeRootCause findings (2026-07-07)

{'text': '"1. Full --continue path\\n\\n- CLI flag: vendor/oh-my-pi/packages/coding-agent/src/cli/args.ts:184-187: `} else if (arg === \\"--continue\\" || arg === \\"-c\\") { result.continue = true; }`.\\n- Decision point: vendor/oh-my-pi/packages/coding-agent/src/main.ts:699-702: `if (parsed.continue) { return await SessionManager.continueRecent(cwd, parsed.sessionDir); }`.\\n- Breadcrumb write points:\\n  - vendor/oh-my-pi/packages/coding-agent/src/session/session-manager.ts:361-362: `if (!this.#suppressBreadcrumb) writeTerminalBreadcrumb(cwd, sessionFile);`\\n  - new session writes immediately when a session file path is allocated: session-manager.ts:570-573: `this.#sessionFile = ...; this.#rememberBreadcrumb(this.#cwd, this.#sessionFile);`\\n  - resume/open writes before/after cwd adoption: session-manager.ts:706-707 and 731-732: `this.#sessionFile = resolvedSessionFile; this.#rememberBreadcrumb(...)`.\\n- Breadcrumb storage format: vendor/oh-my-pi/packages/coding-agent/src/session/session-paths.ts:150-158: `const terminalId = getTerminalId(); if (!terminalId) return; ... const content = `${cwd}\\\\n${sessionFile}\\\\n`; ... Bun.write(breadcrumbFile, content).catch(() => {});` It is best-effort and silently skipped on write failure.\\n- Breadcrumb read: session-paths.ts:172-185: `const terminalId = getTerminalId(); if (!terminalId) return null; ... const content = await Bun.file(breadcrumbFile).text(); ... const sessionFile = lines[1]; ... const stat = fs.statSync(sessionFile, { throwIfNoEntry: false }); if (stat?.isFile()) return { cwd: breadcrumbCwd, sessionFile };`\\n- What invalidates it:\\n  - No current terminal id: session-paths.ts:172-174: `const terminalId = getTerminalId(); if (!terminalId) return null;`.\\n  - Terminal id changes because getTerminalId keys by `/dev/...` TTY or terminal env: vendor/oh-my-pi/packages/tui/src/ttyid.ts:41-75: `if (process.stdin.isTTY) ... return ttyPath.slice(5)...`; then `ZELLIJ_PANE_ID`, `TMUX_PANE`, `CMUX_SURFACE_ID`, `KITTY_WINDOW_ID`, `WEZTERM_PANE`, `TERM_SESSION_ID`, `WT_SESSION`; finally `return null`.\\n  - Breadcrumb file missing/corrupt/short: session-paths.ts:176-181: `const content = await Bun.file(breadcrumbFile).text(); const lines = content.trim().split(\\"\\\\n\\"); if (lines.length < 2) return null;`.\\n  - Session file moved/deleted/not a file: session-paths.ts:185-186: `const stat = fs.statSync(sessionFile, { throwIfNoEntry: false }); if (stat?.isFile()) return ...`.\\n  - Read errors are swallowed except debug log: session-paths.ts:188-191: `if (!isEnoent(err)) logger.debug(\\"Terminal breadcrumb read failed\\", { err }); ... return null;`.\\n- Dangled breadcrumb user-visible error: none from breadcrumb dangling. A dangling/missing breadcrumb returns null, then continueRecent falls back. Evidence: session-manager.ts:1600-1607: `if (chosenSession === undefined) chosenSession = await findMostRecentSession(dir, storage); ... if (chosenSession) await manager.setSessionFile(chosenSession); else manager.#resetToNewSession();`.\\n- Current robust fallback already exists for deleted/corrupt breadcrumb: most-recent-session-in-cwd. Cheapest robust fallback for force-closed terminal-id mismatch: do the same before honoring a same-cwd breadcrumb, or when no breadcrumb is found; exact decision point is session-manager.ts:1559-1607 in `continueRecent`. More specifically: after `const breadcrumb = await readTerminalBreadcrumbEntry();` and before/inside the `if (breadcrumb)` branch, prefer `findMostRecentSession(dir, storage)` when it is newer/current-cwd, because terminal ids can change across terminal app force-close.\\n\\n2. Full --resume path\\n\\n- CLI optional-value parsing: vendor/oh-my-pi/packages/coding-agent/src/cli/flag-tables.ts:83-85: `const setResume ... result.resume = value !== undefined ? value : true;`; flag aliases at lines 205-209: `\\"--resume\\"`, `\\"-r\\"`, `\\"--session\\"`, with `rejectEmpty: true`.\\n- `--resume <arg>`:\\n  - main.ts:639-642: if arg includes `/`, `\\\\`, or ends `.jsonl`, it opens path directly: `return await SessionManager.open(sessionArg, parsed.sessionDir);`.\\n  - main.ts:644-649: otherwise `resolveResumableSession`; no match throws `Session \\"${sessionArg}\\" not found.` plus hint.\\n  - main.ts:651-681: local/global missing-cwd sessions run `moveMissingCwdSessionIfNeeded`.\\n  - main.ts:682-695: global cross-project sessions may prompt to fork; non-interactive unavailable throws `Session \\"${sessionArg}\\" is in another project (${match.session.cwd}); run interactively to fork it into the current project.`\\n  - main.ts:698: final open: `return await SessionManager.open(match.session.path, parsed.sessionDir);`.\\n- `--resume` with no value:\\n  - main.ts:1097-1108: folder scope first: `const folderSessions = await ... SessionManager.list(cwd, parsedArgs.sessionDir);` If empty, it falls back to `SessionManager.listAll`.\\n  - main.ts:1108-1110: if global list also empty: `No sessions found`.\\n  - main.ts:1120-1122: cancellation prints `No session selected`.\\n  - main.ts:1125-1138: if selected cwd differs from current project, `setProjectDir(selected.cwd)`, clear plugin/capability caches, reload settings.\\n  - main.ts:1139: `sessionManager = await SessionManager.open(selected.path);`.\\n- Session listing filters/invisibility:\\n  - Current-folder scope is default; all-project scope only if folder empty initially or user presses Tab. Picker wrapper: cli/session-picker.ts:10-13 says `Tab toggles between current-folder and all-projects scope`; component toggles at modes/components/session-selector.ts:539-566.\\n  - With `--session-dir`, `resolveResumableSession` never scans global: session-listing.ts:601-603: `if (sessionDir) { return undefined; }`.\\n  - Files with no parsed session header are invisible: session-listing.ts:339-352: `const header = parseSessionListHeader(...); if (!header) return undefined;`.\\n  - Directory scan failures are silent empties: session-listing.ts:506-515: `try { ... } catch { return []; }`.\\n  - listAll scans only `*/*.jsonl` under agent sessions root: session-listing.ts:528-534: `new Bun.Glob(\\"*/*.jsonl\\").scan(sessionsRoot)`.\\n  - Search filters by fuzzy/literal over id/title/cwd/firstMessage/allMessagesText/path, not lifecycle/staleness: modes/components/session-selector.ts:45-58 and 80-113. Lifecycle status is display-only: lines 29-43 and metadata render lines 351-355.\\n- Staleness filters: no stale cutoff was found in the resume picker/listing path. Status is derived for display from tail, but not used to filter. session-listing.ts:8-21 defines status meanings; modes/components/session-selector.ts:351-355 appends status to metadata.\\n- Single-writer JSONL lock:\\n  - There is no interprocess lock/pid/heartbeat in the JSONL storage path. The only “single writer” is an in-memory `SessionManager` field: session-manager.ts:332-336: `/** The single open append writer; the manager only ever writes one file at a time. */ #writer ...`.\\n  - The file writer just opens an fd in append/write mode: session-storage.ts:65-76: `const flags = options?.flags ?? \\"a\\"; ... this.#fd = fs.openSync(fpath, flags === \\"w\\" ? \\"w\\" : \\"a\\");`.\\n  - Read-only loader explicitly says it does not lock: session-loader.ts:122-126: `Does NOT create a writer or take the session lock — safe to call against a file another session is writing.` The implementation has no actual lock creation here.\\n  - On open after a crashed process, there is no stale lock to detect. OS closes the old fd; SessionManager.open loads the file and `setSessionFile` does not open a writer until the next append. Evidence: session-manager.ts:1500-1530 `open` creates manager then `await manager.setSessionFile(filePath)`; session-manager.ts:700-739 loads entries and sets state; writer opens only in `#appendWriter` at lines 430-439.\\n  - Exact writer error strings in this path if the current in-process writer is bad: session-storage.ts:86-96: `throw new Error(\\"Writer closed\\")`; `throw new Error(\\"Short write\\")`. Read/open non-ENOENT errors propagate raw from `Bun.file(path).text()` via session-loader.ts:56-61.\\n\\n3. Fork/tree representation and suspicious stale/wrong-branch mechanisms\\n\\n- JSONL structure:\\n  - session-manager.ts:290-292: `A session is a JSONL file: one header line followed by entries. Entries form a tree by (id, parentId), and the mutable leaf pointer selects which path is active...`.\\n  - session-entries.ts:22-26: every entry has `id`, `parentId`, `timestamp`.\\n  - session-entries.ts:78-85: `BranchSummaryEntry` persists as `type: \\"branch_summary\\"`, `fromId`, `summary`, optional details/fromExtension.\\n  - session-entries.ts:11-18: header has optional `parentSession`, used for fork provenance.\\n- Active branch reconstruction on resume:\\n  - SessionEntryIndex.insert sets leaf to every inserted entry: session-manager.ts:130-132: `this.#entriesById.set(entry.id, entry); this.#leaf = entry.id;`.\\n  - Rebuild replays all entries and therefore selects the last physical JSONL entry as leaf: session-manager.ts:124-127: `this.clear(); for (const entry of entries) this.insert(entry);`.\\n  - `setSessionFile` applies loaded entries via rebuild: session-manager.ts:736-739: `this.#applyEntries(header, fileEntries.slice(1) as SessionEntry[]); this.#fileIsCurrent = true;`.\\n  - SDK restores agent messages from that branch: sdk.ts:1214-1219 builds `sessionManager.buildSessionContext()` and `sessionManager.getBranch()`; sdk.ts:2535-2538: `if (hasExistingSession) { agent.replaceMessages(existingSession.messages); }`.\\n- Tree UI selection:\\n  - selector-controller.ts:670-683: tree is `this.ctx.sessionManager.getTree()` and active leaf is `this.ctx.sessionManager.getLeafId()`.\\n  - tree-selector.ts:85-88: starts selection at `initialSelectedId ?? currentLeafId`.\\n  - tree-selector.ts:155-184 and 215-220: subtrees containing active leaf are prioritized.\\n- Fork/new branch files:\\n  - AgentSession.branch validates that the target is a user message: agent-session.ts:10790-10794: throws `Invalid entry ID for branching` if not a user message.\\n  - If root user message, it creates a new session; otherwise it creates a branched session from selectedEntry.parentId: agent-session.ts:10820-10825.\\n  - createBranchedSession writes a new header with `parentSession: this.#persist ? sourceSessionFile : undefined`: session-manager.ts:1410-1432.\\n- In-file tree navigation:\\n  - navigateTree chooses new leaf: for user/custom_message it sets leaf to parent and returns editor text; otherwise leaf is target: agent-session.ts:10970-10987.\\n  - Without summary it only mutates in-memory leaf: agent-session.ts:11005-11012: `resetLeaf()` or `this.sessionManager.branch(newLeafId)`.\\n  - `SessionManager.branch` is not persisted: session-manager.ts:1374-1377: `this.#index.setLeaf(branchFromId);` no record entry/rewrite.\\n  - With summary it persists a `branch_summary` entry at the new leaf: agent-session.ts:10997-11003 calls `branchWithSummary`; session-manager.ts:1388-1404 records entry.\\n- Plausible mechanisms, evidence-limited:\\n  1. Wrong branch selected on resume after tree navigation without summary and before a new append: leaf changes only in memory (`branch`/`resetLeaf`), but resume rebuild makes the last physical JSONL entry active. This is directly evidenced by `insert` setting leaf to each entry and no persisted leaf pointer.\\n  2. Stale tree state in an already-open tree overlay: `showTreeSelector` captures `tree` and `realLeafId` before user interaction at selector-controller.ts:670-683. If session entries change while the overlay is open, that component uses the captured tree; no refresh hook is visible in the selector construction.\\n  3. Wrong visual prioritization can follow wrong active leaf: tree-selector prioritizes branches containing `currentLeafId` (tree-selector.ts:155-184, 215-220), so if resume reconstructed the wrong leaf, the tree view will also sort/select the wrong branch.\\n\\n4. Resume failure modes inventory and ranked likely causes\\n\\nInventory of throw/error-return/user-return points in resume/open path:\\n- `--fork` + `--no-session`: main.ts:618-621 throws `--fork requires session persistence`.\\n- `--fork <id>` no match: main.ts:626-631 throws `Session \\"${forkSource}\\" not found.` with hint `Run `omp --resume` without an argument...`.\\n- `--resume <id>` no match: main.ts:644-649 throws `Session \\"${sessionArg}\\" not found.` with same hint.\\n- Missing recorded cwd and non-interactive move prompt unavailable: main.ts:562-566 throws `Session \\"${sessionArg}\\" belongs to a directory that no longer exists (${sourceCwd}); run interactively to move it into the current project.`\\n- User declines missing-cwd move: main.ts:659-664 or 677-681 returns undefined; main.ts:1089-1094 prints `Resume cancelled: session is in another project.` even though the decline may be a move decline too.\\n- Cross-project global match in non-interactive mode: main.ts:684-688 throws `Session \\"${sessionArg}\\" is in another project (${match.session.cwd}); run interactively to fork it into the current project.`\\n- User declines cross-project fork: main.ts:689-693 returns undefined; main.ts:1089-1094 prints `Resume cancelled: session is in another project.`\\n- `--resume` picker, no sessions: main.ts:1108-1110 prints `No sessions found` and returns.\\n- `--resume` picker cancelled: main.ts:1120-1122 prints `No session selected` and returns.\\n- Session file read: session-loader.ts:56-61 returns [] only for ENOENT; any other read error is thrown raw.\\n- Explicit nonexistent path (`--resume path/thing.jsonl`) is not an error: session-loader.ts:58-60 returns []; session-manager.ts:709-717 treats empty/missing as fresh session at requested path and rewrites header.\\n- Corrupt/malformed JSONL with no recoverable header is not an error: session-loader.ts:39-48 `if (sessionIndex < 0) return [];`; setSessionFile then creates a fresh session at that path.\\n- Blob/image ref resolution could surface storage errors while opening: setSessionFile awaits `resolveBlobRefsInEntries` at session-manager.ts:723. Missing blobs generally log/fallback in blob-store, but read errors can propagate from the storage layer if not handled.\\n- SDK model restore does not fail resume; it warns. sdk.ts:1266-1268: `modelFallbackMessage = `Could not restore model ${failedSessionModel}`;`.\\n\\nRanked top 3 likely causes of “session resume suddenly broken for certain sessions”:\\n1. Persisted active leaf is missing, so resume always picks the last JSONL entry. Evidence: leaf is mutable in memory (session-manager.ts:290-292), rebuilt as last inserted entry (session-manager.ts:124-132), and tree navigation without summary only calls `branch`/`resetLeaf` with no persisted entry (agent-session.ts:11005-11012; session-manager.ts:1374-1377). This matches “wrong branch selected on resume” and “stale tree state”.\\n2. Terminal-id breadcrumb mismatch after terminal close/reopen makes `--continue` lose the intended session. Evidence: breadcrumb is keyed only by current `getTerminalId` (session-paths.ts:150-155, 172-178), getTerminalId depends on TTY/env that can change (ttyid.ts:41-75), and missing breadcrumb silently falls back to most recent in cwd or new session (session-manager.ts:1600-1607). This matches random continue failures.\\n3. Session discovery silently drops sessions or narrows scope unexpectedly. Evidence: malformed/no-header files return invisible (`if (!header) return undefined`, session-listing.ts:339-352), scan errors return [] (session-listing.ts:506-515), `--session-dir` disables global lookup (session-listing.ts:601-603), and no-value picker starts current-folder unless empty or Tab (main.ts:1097-1119; session-selector.ts:539-566). This explains “certain sessions” disappearing without an explicit error.\\n\\n5. Minimal fix touch points per top cause, without full design\\n\\n1. Persist active leaf across resume/tree navigation:\\n   - Touch `vendor/oh-my-pi/packages/coding-agent/src/session/session-entries.ts` to add a minimal persisted leaf/navigation entry or header field.\\n   - Touch `SessionEntryIndex.rebuild` / `SessionManager.setSessionFile` in `session-manager.ts` so rebuild restores that leaf instead of blindly using the last entry.\\n   - Touch `AgentSession.navigateTree` in `agent-session.ts` where it currently only calls `branch`/`resetLeaf` without persistence.\\n   - Touch tree tests around `session-manager` / tree navigation.\\n\\n2. Harden --continue breadcrumb fallback:\\n   - Touch `SessionManager.continueRecent` in `session-manager.ts`, exact decision region lines 1559-1607.\\n   - Possibly touch `session-paths.ts` only if breadcrumb content needs extra metadata; cheapest fix likely does not.\\n   - Add tests like existing `test/session-manager/continue-relocation.test.ts` for terminal-id missing/different and same-cwd most-recent fallback.\\n\\n3. Make resume discovery failures visible/less lossy:\\n   - Touch `session-listing.ts` scan paths: `scanSessionFile`, `scanSessionDir`, `listAllSessions`, and/or `resolveResumableSession` so unreadable/malformed candidates can be reported or recovered instead of silently disappearing.\\n   - Touch `main.ts` no-value picker handling if “current folder vs all projects” needs a clearer default or message.\\n   - Touch picker UI only if exposing skipped/unreadable session notices; otherwise avoid UI churn."', 'details': {'resolvedPath': '/Users/arthur/.omp/agent/sessions/-agents/2026-07-07T00-00-51-895Z_019f39e0-a6b7-7000-a848-f3e75d966101/ResumeRootCause.md', 'contentType': 'application/json', 'meta': {'source': {'type': 'internal', 'value': 'agent://ResumeRootCause/architecture'}}}}