> Rescued 2026-07-13 from /Users/arthur/.omp/agent/sessions/-agents/2026-07-07T00-00-51-895Z_019f39e0-a6b7-7000-a848-f3e75d966101/local/control-seams.md

# ControlSeamScout findings (2026-07-07)

{'text': '"1. Interrupt semantics today\\n\\n- `job cancel` is a job-level cancel, not a soft agent interrupt. The tool first scopes the target to the caller owner, refuses missing/foreign/non-running jobs, then calls `manager.cancel(id, ownerFilter)` and returns immediately with a cancel summary. Evidence: `tools/job.ts:153-176` snippet: `const cancelIds = params.cancel ?? []; ... if (existing.status !== \\"running\\") ... const cancelled = manager.cancel(id, ownerFilter); ... { id, status: \\"cancelled\\", message: \\u001bCancelled background job ${id}.\\u001b }`. If cancel-only was requested, it does not poll/wait: `tools/job.ts:178-182` snippet: `// If only \\u001bcancel\\u001b was provided (no \\u001bpoll\\u001b), don\'t wait — return immediately. const shouldPoll = requestedPollIds !== undefined || cancelIds.length === 0;`.\\n\\n- End-to-end signal path: `AsyncJobManager.cancel` marks the job `cancelled`, aborts the job\'s AbortController, schedules eviction, and does not enqueue completion delivery. Evidence: `async/job-manager.ts:250-259` snippet: `if (job.status !== \\"running\\") return false; job.status = \\"cancelled\\"; job.abortController.abort(); this.#scheduleEviction(id); return true;`. When the run later returns/throws while already cancelled, the manager only stores `resultText` or `errorText` and schedules eviction: `async/job-manager.ts:219-231` snippet: `if (job.status === \\"cancelled\\") { job.resultText = text; this.#scheduleEviction(id); return; } ... if (job.status === \\"cancelled\\") { job.errorText = ...; this.#scheduleEviction(id); return; }`.\\n\\n- For task jobs, the cancelled job signal is the `runSignal` passed into `#executeSync(..., runSignal, ..., agentId, ..., true)`. Evidence: `task/index.ts:910-929` snippet: `return manager.register(\\"task\\", agentId, async ({ jobId: ownJobId, signal: runSignal, ... }) => { ... const result = await this.#executeSync(..., runSignal, undefined, agentId, ...); }`. In the executor monitor, that signal calls `requestAbort(\\"signal\\")`, which aborts the monitor controller and calls `activeSession.abort()`. Evidence: `task/executor.ts:811-829` snippet: `const requestAbort = (reason: AbortReason) => { ... abortController.abort(); if (activeSession) { void activeSession.abort(); } };`, plus `task/executor.ts:833-840` snippet: `signal.addEventListener(\\"abort\\", () => { if (!resolved) requestAbort(\\"signal\\"); }, ...)`.\\n\\n- A cancelled running subagent is normally terminally marked `aborted`, disposed, and not kept idle/parked. Evidence: `task/executor.ts:2150-2164` snippet: `const timeoutKeptAlive = aborted && monitor.runtimeLimitExceeded() && worktree === undefined; if (aborted && !timeoutKeptAlive) { ... registry.setStatus(id, \\"aborted\\"); ... await ... session.dispose(); }`. Non-aborted non-isolated runs are the keep-alive path: `task/executor.ts:2176-2184` snippet: `registry.setStatus(id, \\"idle\\"); AgentLifecycleManager.global().adopt(id, { idleTtlMs: agentIdleTtlMs, revive: reviveSession ?? undefined });`. Isolated worktree runs are parked but not adopted: `task/executor.ts:2165-2175` snippet: `registry.setStatus(id, \\"parked\\"); ... session.dispose(); registry.detachSession(id);`.\\n\\n- Parent result for cancel-only is a cancel report plus snapshot details, not the subagent\'s latest yield. Evidence: `tools/job.ts:328-335` snippet: `if (cancelOutcomes.length > 0) { lines.push(\\u001b## Cancelled (${cancelOutcomes.length})\\\\n\\u001b); for (const o of cancelOutcomes) lines.push(\\u001b- ${o.message}\\u001b); }`, and `tools/job.ts:354-360` snippet: `details: { jobs: jobResults, ...(cancelOutcomes.length ? { cancelled: ... } : {}) }`. Because manager cancellation suppresses `#enqueueDelivery`, no async-result follow-up is sent for the cancelled job.\\n\\n- `AgentSession.abort(...)` aborts the current operation/turn and tool subprocesses, waits for idle, and resets in-flight bookkeeping; it does not itself unregister or kill the session. Evidence: `session/agent-session.ts:6052-6060` snippet: `Abort current operation and wait for agent to become idle. ... async abort(options?: ...)`, and `session/agent-session.ts:6077-6092` snippet: `this.abortRetry(); ... this.abortCompaction(); this.abortHandoff(); this.abortBash(); this.abortEval(); ... this.agent.abort(options?.reason); ... await this.agent.waitForIdle(); ... this.#resetInFlight();`. Promptability after abort is implied by no dispose/unregister in `abort`; contrast with `dispose`, which sets disposed and clears queues/provider: `session/agent-session.ts:3745-3748` snippet: `this.#isDisposed = true; this.#pendingIrcAsides = []; this.yieldQueue.clear(); this.agent.setAsideMessageProvider(undefined);`.\\n\\n- Existing pause/steer-at-boundary machinery beyond IRC: (a) `sendCustomMessage(... deliverAs: \\"steer\\" | \\"followUp\\" | \\"nextTurn\\")` routes to `agent.steer`, `agent.followUp`, or next-turn append/prompt. Evidence: `session/agent-session.ts:5848-5850` snippet: `options?: { triggerTurn?: boolean; deliverAs?: \\"steer\\" | \\"followUp\\" | \\"nextTurn\\"; ... }`, and `session/agent-session.ts:5878-5884` snippet: `if (options?.deliverAs === \\"followUp\\") { this.agent.followUp(normalizedAppMessage); } else { this.agent.steer(normalizedAppMessage); }`. (b) `YieldQueue`/aside provider injects async results and late diagnostics at step boundaries: `session/agent-session.ts:1482-1490` snippet: `each step boundary as non-interrupting asides ... thunks.push(...this.yieldQueue.drainLazy());`. (c) Advisor concern/blocker uses steer with triggerTurn: `session/agent-session.ts:1621-1647` snippet: `Concern and blocker interrupt ... through the steering channel ... void this.sendCustomMessage(... { deliverAs: \\"steer\\", triggerTurn: true })`. (d) TTSR can abort stream and schedule retry/injection: `session/agent-session.ts:3078-3084` snippet: `this.agent.abort(this.#formatTtsrAbortReason(matches)); ... this.#schedulePostPromptTask(`. (e) Hotswap queues apply at `agent_end`: `task/hotswap.ts:244-246` snippet: `unsubscribeSession = session.subscribe((event) => { if (event.type === \\"agent_end\\") applyQueued(); });`.\\n\\n2. What a soft `interrupt` primitive needs\\n\\n- Desired behavior maps to existing seams: abort current turn, but keep the AgentRef live and addressable (`idle`/eventually `parked`), optionally inject a steer note. `AgentSession.abort` already gives the turn-level abort and post-abort promptability. Evidence: `session/agent-session.ts:6079-6082` snippet: `this.agent.abort(options?.reason); await postPromptDrain; await this.agent.waitForIdle();`.\\n\\n- Registry/lifecycle already support surviving addressability if the caller chooses the keep-alive path: registry statuses include live idle and parked, and parked refs retain sessionFile for revival. Evidence: `registry/agent-registry.ts:17-25` snippet: `running ... idle ... parked ... aborted`, and `registry/agent-registry.ts:100-108` snippet: `setStatus(id, status) ... if (status !== \\"running\\") ref.activity = undefined; ... emit`. Lifecycle adoption/park/revive exists: `registry/agent-lifecycle.ts:72-83` snippet: `Take ownership of a finished subagent. Caller has already set registry status to \\"idle\\" ... adopt(id, opts)`, `registry/agent-lifecycle.ts:107-119` snippet: `await ref.session.dispose(); ... this.#registry.detachSession(id); this.#registry.setStatus(id, \\"parked\\");`, and `registry/agent-lifecycle.ts:181-185` snippet: `attachSession(id, session, sessionFile); ... setStatus(id, \\"idle\\");`.\\n\\n- Executor keep-alive path already preserves completed/failed subagents for IRC follow-up; the missing piece is a path that treats caller-requested interrupt as non-terminal instead of `aborted`. Current code explicitly sends caller/budget aborts to terminal teardown. Evidence: `task/executor.ts:2150-2164` snippet: `if (aborted && !timeoutKeptAlive) { ... registry.setStatus(id, \\"aborted\\"); ... session.dispose(); }`; keep-alive exists only in the later `else`: `task/executor.ts:2176-2184` snippet: `registry.setStatus(id, \\"idle\\"); AgentLifecycleManager.global().adopt(...)`.\\n\\n- A soft interrupt should probably be implemented as a new executor/job-manager/session control path, not by overloading current `job cancel`. Exact functions to touch: `JobTool.execute` for command surface (`tools/job.ts:24-38` schema currently has only `poll/cancel/list/setModel`), `AsyncJobManager` for a non-cancelling interrupt operation or callback hook (`async/job-manager.ts:154-160` register currently only provides `{ jobId, signal, reportProgress, markRunning }`), `TaskTool.#registerSpawnJob` to expose per-job subagent control (`task/index.ts:910-929`), and `runSubprocess`/`runSubagent` finally branch to keep the session idle/adopted on soft interrupt (`task/executor.ts:2150-2184`).\\n\\n- Optional steer note can reuse `AgentSession.sendCustomMessage`/`sendUserMessage` before or after abort depending on semantics. Existing steer paths: `session/agent-session.ts:5933-5948` snippet: `async sendUserMessage(... options?: { deliverAs?: \\"steer\\" | \\"followUp\\" }) ... if (options?.deliverAs === \\"steer\\") { await this.#queueUserMessage(text, images, \\"steer\\"); return; }`, and custom next-turn note for hotswap: `task/hotswap.ts:67-76` snippet: `session.sendCustomMessage({ customType: \\"hotswap:model\\", ... }, { deliverAs: \\"nextTurn\\" })`.\\n\\n- What is missing: no job-manager concept of “interrupt but retain job binding/latest output”; no executor outcome distinct from `aborted`; no registry branch that sets `idle` after caller interrupt; no receiving command envelope over external IRC. Current job statuses are only `running | completed | failed | cancelled`: `async/job-manager.ts:32-33` snippet: `status: \\"running\\" | \\"completed\\" | \\"failed\\" | \\"cancelled\\"`.\\n\\n3. Stale job results after IRC wake\\n\\n- Original task job result object is captured once in `AsyncJobManager.register`: the job body\'s returned text becomes `job.resultText`, status becomes `completed`, and a delivery is enqueued. Evidence: `async/job-manager.ts:212-227` snippet: `const text = await run({ jobId: id, signal: abortController.signal, ... }); ... job.status = \\"completed\\"; job.resultText = text; this.#enqueueDelivery(id, text);`. Failures similarly store `job.errorText`: `async/job-manager.ts:234-237` snippet: `job.status = \\"failed\\"; job.errorText = errorText; this.#enqueueDelivery(id, errorText);`.\\n\\n- For background task jobs, the returned text is built from the initial `#executeSync` result only: `task/index.ts:928-944` snippet: `const result = await this.#executeSync(...); const finalText = result.content.find(... )?.text ?? \\"(no output)\\"; const singleResult = result.details?.results[0]; ... const deliveryText = \\u001b${finalText}${buildFollowUpHint(...)}\\u001b; ... return deliveryText;`. That result is then frozen into the AsyncJob object above.\\n\\n- `job poll`/`list` reads the current `AsyncJob` record and snapshots `resultText/errorText`; it does not query the agent session’s latest yield or transcript. Evidence: `tools/job.ts:283-302` snippet: `const current = this.session.asyncJobManager?.getJob(j.id); const latest = current ?? j; return { id: latest.id, ... ...(latest.resultText ? { resultText: latest.resultText } : {}), ...(latest.errorText ? { errorText: latest.errorText } : {}) };`.\\n\\n- Later IRC wakes run real turns on the same idle subagent session but do not rebind the original job result. Evidence for wake: `irc/bus.ts:95-132` routes to `session.deliverIrcMessage`; `session/agent-session.ts:10259-10267` snippet: `if (this.isStreaming) { this.#pendingIrcAsides.push(record); ... return \\"injected\\"; } ... this.agent.prompt(record).catch(...); return \\"woken\\";`. Evidence that only status sync happens on later agent_end: `task/executor.ts:1632-1641` snippet: `installRegistryStatusSync ... target.subscribe(event => { if (event.type === \\"agent_start\\") ... \\"running\\"; else if (event.type === \\"agent_end\\") ... \\"idle\\"; });`. There is no write to `AsyncJob.resultText` outside `async/job-manager.ts` (search found only lines 219/225/230/236/379 in `async/job-manager.ts`).\\n\\n- Async delivery to the parent also captures the original result text once. Evidence: `sdk.ts:1429-1441` snippet: `onJobComplete: async (jobId, result, job) => { ... const formattedResult = await formatAsyncResultForFollowUp(result); ... session.yieldQueue.enqueue(\\"async-result\\", { jobId, result: formattedResult, job, durationMs }); }`, and result message details omit later output pointer: `sdk.ts:231-260` snippet: `const jobs = entries.map(entry => ({ jobId: entry.jobId, result: entry.result, ... })); ... details: { jobs: jobs.map(job => ({ jobId, type, label, durationMs })) }`.\\n\\n- Fix surfaces: either (a) re-bind the job’s `resultText` on subsequent `agent_end` for adopted subagents, probably in `installRegistryStatusSync` or a new lifecycle monitor, by extracting latest successful `yield`/assistant output and updating the `AsyncJobManager` record; or (b) add a per-agent latest-output pointer separate from immutable job completion, and teach `JobTool.#snapshotJobs` / completed snapshots to prefer it for task jobs. Exact touch points: `task/executor.ts:1632-1641` (later-turn agent_end hook), `async/job-manager.ts` (new update API for completed task job payloads), `tools/job.ts:283-302` (snapshot source), and possibly `sdk.ts:231-260` if async-result follow-up cards should also reflect refreshed output rather than just `job poll`.\\n\\n4. Cross-session control / IRC EXTERNAL bus\\n\\n- External bus is SQLite-backed at `~/.omp/agent/irc-bus.sqlite`, with `peers(session_id,name,cwd,pid,last_seen,state,state_ts)` and `messages(id,ts,from_peer,to_peer,body,delivered)`. Evidence: `irc/bus-external.ts:42-43` snippet: `const DEFAULT_DB_PATH = path.join(os.homedir(), \\".omp\\", \\"agent\\", \\"irc-bus.sqlite\\");`, `irc/bus-external.ts:181-202` snippet: `CREATE TABLE IF NOT EXISTS peers (...) ... CREATE TABLE IF NOT EXISTS messages (... body TEXT, delivered INTEGER DEFAULT 0)`, and indexes at `irc/bus-external.ts:203-204`.\\n\\n- Message format is plain text body plus from/to peer names; no structured command field. Evidence: `irc/bus-external.ts:18-24` snippet: `interface IrcExternalMessage { id; ts; fromPeer; toPeer; body; }`, and `irc/bus-external.ts:284-292` snippet: `sendMessage(args: { fromPeer: string; toPeer: string; body: string }): number ... INSERT INTO messages (ts, from_peer, to_peer, body) ...`.\\n\\n- A main session receives external inbound messages only by polling at step boundaries / prompt flush; it renders them as `irc:incoming` custom messages and marks delivered. Evidence: `session/agent-session.ts:10475-10489` registers the peer, `session/agent-session.ts:10503-10525` snippet: `const messages = bus.pollMessages(name); for (const message of messages) { ... customType: \\"irc:incoming\\" ... details: { id: \\u001bexternal:${message.id}\\u001b, from: message.fromPeer, message: message.body } ... this.#pendingIrcAsides.push(record); bus.markDelivered(message.id); }`. Polling is called by the aside provider: `session/agent-session.ts:1484-1489` snippet: `this.agent.setAsideMessageProvider(() => { this.#pollExternalIrcMessages(); ... thunks.push(...this.yieldQueue.drainLazy()); })`, and by idle flush: `session/agent-session.ts:10529-10536` snippet: `#flushPendingIrcAsides() { this.#pollExternalIrcMessages(); ... this.agent.emitExternalEvent({ type: \\"message_start\\", message: record }); ... }`.\\n\\n- The agent IRC tool can send to external peers but treats them as fire-and-forget text; `await:true` is explicitly in-process-only. Evidence: `tools/irc.ts:287-303` snippet: `if (externalTarget) { if (params.await) { return errorResult(\\"\\u001bawait:true\\u001b is in-process-only; external peers receive fire-and-forget messages.\\", ...); } external.bus.sendMessage({ fromPeer: external.name, toPeer: externalTarget.name, body: message }); ... outcome: \\"injected\\" }`.\\n\\n- The human CLI surface already supports `omp irc list | send <peer> <message> | inbox <peer>`. Evidence: `commands/irc.ts:27-31` snippets examples include `omp irc send harness-abc123 \'please check your inbox\'`; implementation: `cli/irc-cli.ts:47-52` dispatches `list/send/inbox`, and `cli/irc-cli.ts:79-94` snippet: `const body = cmd.args.slice(1).join(\\" \\").trim(); ... const id = bus.sendMessage({ fromPeer, toPeer: target.name, body });`.\\n\\n- No external structured/command handling exists beyond the LLM seeing a text `irc:incoming` message. A `setModel`/`interrupt` command targeting another session’s MAIN agent would have to be handled on the receiving side in/near `AgentSession.#pollExternalIrcMessages` (decode before converting to `irc:incoming`), or a new control loop invoked from the same polling sites (`setAsideMessageProvider` and `#flushPendingIrcAsides`). Peer state for main sessions is already maintained: `session/agent-session.ts:2088-2092` snippet: `if (event.type === \\"agent_start\\") this.#updateExternalIrcPeerState(\\"working\\"); else if (event.type === \\"agent_end\\") this.#updateExternalIrcPeerState(\\"waiting_input\\");`, and `session/agent-session.ts:10492-10500` only updates for `#agentKind === \\"main\\"`.\\n\\n- Smallest honest surface: (a) add a structured command envelope inside `messages.body` (e.g. versioned JSON distinguishable from prose), parsed only by the receiving MAIN session in `#pollExternalIrcMessages`; (b) leave `omp irc send` usable by humans because it already writes arbitrary body strings; (c) add receiving-side allowlist/approval because today any process that can write the SQLite DB can send arbitrary `body` to a fresh peer. Existing code has freshness/name lookup but no auth/approval: `irc/bus-external.ts:296-306` finds fresh peer by name, `cli/irc-cli.ts:86-93` sends after name match, and there is no sender verification beyond `fromPeer` text.\\n\\n5. Transcript provenance today\\n\\n- Session JSONL has tree provenance (`id`, `parentId`, `timestamp`) on every entry and `parentSession` in the header, but not per-agent authorship for ordinary messages. Evidence: `session/session-entries.ts:6-16` snippet: `SessionHeader ... id ... cwd ... parentSession?`, and `session/session-entries.ts:24-30` snippet: `SessionEntryBase { type; id; parentId; timestamp; }`.\\n\\n- Ordinary messages can carry message-level attribution (`user`/`agent`) in the AgentMessage object, and assistant entries get turn-level model/thinking/advisor provenance. Evidence: prompt messages: `session/agent-session.ts:5249-5252` snippet: `const promptAttribution = options?.attribution ?? (options?.synthetic ? \\"agent\\" : \\"user\\"); const message = options?.synthetic ? { role: \\"developer\\", ..., attribution: promptAttribution } : { role: \\"user\\", ..., attribution: promptAttribution }`. Persistence: `session/agent-session.ts:2343-2352` snippet: `assistant message - persist with turn-level provenance ... appendMessage(event.message, this.#sessionMessageAttribution(event.message)); ... user/developer/toolResult/fileMention ... appendMessage(event.message)`. Entry schema: `session/session-entries.ts:27-39` snippet: `SessionMessageAttribution { model?: string; thinkingLevel?: string | null; advisor?: string; } ... SessionMessageEntry ... message: AgentMessage`.\\n\\n- Custom messages have explicit `attribution` and persist it as `custom_message.attribution`; this covers IRC, async-result, advisor, hotswap notices, etc. Evidence: `session/messages.ts:357-365` snippet: `CustomMessage ... customType ... details? ... attribution?: MessageAttribution; timestamp`, `session/agent-session.ts:2331-2338` snippet: `appendCustomMessageEntry(... event.message.details, event.message.attribution ?? \\"agent\\")`, and `session/session-manager.ts:1221-1236` snippet: `appendCustomMessageEntry(... attribution: MessageAttribution = \\"agent\\") ... attribution`.\\n\\n- IRC attribution includes agent ids in custom details, not a separate trusted author field. In-process incoming: `session/agent-session.ts:10244-10258` snippet: `customType: \\"irc:incoming\\" ... details: { id: msg.id, from: msg.from, message: msg.body, ...(replyTo...) } ... attribution: \\"agent\\"`. External incoming: `session/agent-session.ts:10510-10522` snippet: `customType: \\"irc:incoming\\" ... details: { id: \\u001bexternal:${message.id}\\u001b, from: message.fromPeer, message: message.body } ... attribution: \\"agent\\"`. History formatting exposes this as `[irc] from → me`: `session/session-history-format.ts:148-156` snippet: `case \\"irc:incoming\\": return \\u001b[irc] ${str(\\"from\\") || \\"?\\"} → me: ...\\u001b`.\\n\\n- Model changes are explicit transcript entries with role labels, including hotswap/fallback/temporary roles. Evidence: `session/session-entries.ts:51-57` snippet: `ModelChangeEntry ... model ... role?: string`, `session/session-manager.ts:1162-1164` snippet: `appendModelChange(model: string, role?: string)`, hotswap writes role `hotswap`: `task/hotswap.ts:90-93` snippet: `await session.setModel(model, \\"hotswap\\");`, and thinking-only hotswap explicitly appends model_change `hotswap`: `task/hotswap.ts:112-116` snippet: `session.sessionManager.appendModelChange(modelString, \\"hotswap\\")`.\\n\\n- Task spawn records exist in each subagent JSONL as `session_init` (system prompt, initial task, tools, output schema), and parent-child linkage exists via registry/session file paths, but parent session transcript does not persist a durable “agent X wrote this later output” pointer beyond task result/custom async-result cards. Evidence: `session/session-entries.ts:126-135` snippet: `SessionInitEntry ... systemPrompt ... task ... tools ... outputSchema`, and executor appends it: `task/executor.ts:2050-2055` snippet: `session.sessionManager.appendSessionInit({ systemPrompt: ..., task, tools: session.getActiveToolNames(), outputSchema })`. Agent registry tracks parentId live: `registry/agent-registry.ts:29-35` snippet: `AgentRef { id; displayName; kind; parentId?; status; session; sessionFile; }`.\\n\\n- What “which agent wrote this part” can answer today from a session file: within a subagent’s own JSONL, you can tell the task/session_init, model roles, custom message source metadata (IRC from ids, async-result job ids), and whether prompts were user/agent-attributed. What is missing: ordinary assistant/user/developer messages do not store a stable `agentId`/writer id field; parent transcript does not automatically attribute later IRC-wake yields back into the original task job result; external IRC `fromPeer` is just body metadata from SQLite, not authenticated provenance."', 'details': {'resolvedPath': '/Users/arthur/.omp/agent/sessions/-agents/2026-07-07T00-00-51-895Z_019f39e0-a6b7-7000-a848-f3e75d966101/ControlSeamScout.md', 'contentType': 'application/json', 'meta': {'source': {'type': 'internal', 'value': 'agent://ControlSeamScout/architecture'}}}}