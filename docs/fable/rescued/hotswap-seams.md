> Rescued 2026-07-13 from /Users/arthur/.omp/agent/sessions/-agents/2026-07-07T00-00-51-895Z_019f39e0-a6b7-7000-a848-f3e75d966101/local/hotswap-seams.md

# Hotswap seam scout report (2026-07-07)

{'text': '"1. AgentSession events: subscribe is `subscribe(listener: AgentSessionEventListener): () => void`, appends listener and returns unsubscribe; no once helper (`agent-session.ts:3623-3634`). `AgentSessionEvent` extends core `AgentEvent` (`agent-session.ts:296-300`). Core lifecycle shapes are `{ type: \\"agent_start\\" }`, `{ type: \\"agent_end\\"; messages; telemetry?; coverage? }`, `{ type: \\"turn_start\\" }`, `{ type: \\"turn_end\\"; message; toolResults }` (`packages/agent/src/types.ts:571-583`). `installRegistryStatusSync` watches `agent_start`→running and `agent_end`→idle, not turn_start/turn_end (`task/executor.ts:1789-1797`), installed on initial and revived sessions (`task/executor.ts:2005-2019`). `isStreaming` returns `this.agent.state.isStreaming || this.#promptInFlightCount > 0` (`agent-session.ts:3899-3902`).\\n\\n2. Safe model set: `runExtensionSetModel(session, model): Promise<boolean>` checks `modelRegistry.getApiKey`, returns false when missing, then `session.setModel(model)` (`compact-handler.ts:23-39`). `setModel(model, role = \\"default\\", options?)` validates configured auth, clears retry fallback, calls `#setModelWithProviderSessionReset`, appends model_change, optionally persists, records usage, reapplies thinking (`agent-session.ts:6285-6311`). `setModelTemporary(model, thinkingLevel?, { ephemeral? })` writes role `temporary` or `EPHEMERAL_MODEL_CHANGE_ROLE` (`agent-session.ts:6319-6348`). `#setModelWithProviderSessionReset` closes provider sessions, calls `agent.setModel`, syncs append-only context (`agent-session.ts:8137-8146`). Core `Agent.setModel` only assigns state, no streaming guard (`packages/agent/src/agent.ts:679-681`). There is no generic deferred model-set boundary. Retry fallback mutates model inside `#applyRetryFallbackCandidate` and `#maybeRestoreRetryFallbackPrimary` (`agent-session.ts:9478-9481`, `9554-9555`); fallback is invoked in retry error handling (`agent-session.ts:9708-9734`), while fallback primary restore is called before scheduled continue (`agent-session.ts:2648-2666`) and before new prompt send (`agent-session.ts:5348-5361`). Calling setModel while streaming is not guarded by the setter path.\\n\\n3. Model resolution: `resolveModelOverrideWithAuthFallback(modelPatterns, parentActiveModelPattern, modelRegistry, settings?)` returns `{ model?, thinkingLevel?, explicitThinkingLevel, authFallbackUsed }` (`model-resolver.ts:958-968`). It resolves primary, handles blocked Fable fallback patterns, and auth-falls back to parent model only when primary lacks auth and parent is authenticated/non-blocked (`model-resolver.ts:969-1023`). Simpler APIs: `resolveModelOverride(modelPatterns, modelRegistry, settings?)` (`model-resolver.ts:918-935`), `resolveModelRoleValue(roleValue, availableModels, options?)` (`model-resolver.ts:795-842`), and `resolveModelFromString(value, available, preferences?, modelRegistry?)` (`model-resolver.ts:873-882`). Thinking suffixes use last-colon `splitThinkingSuffix` and `parseThinkingLevel` (`model-resolver.ts:1-12`, `70-75`); `parseModelPatternWithContext` tries full model id first, then strips valid suffix and recurses, warning/fallback for invalid suffix (`model-resolver.ts:557-621`).\\n\\n4. Message/notice injection: `IrcBus.send` revives parked recipients with lifecycle `ensureLive`, then calls `session.deliverIrcMessage(message, opts)` (`irc/bus.ts:101-134`). `deliverIrcMessage(msg, opts?): Promise<\\"injected\\" | \\"woken\\">` builds a `CustomMessage` `role:\\"custom\\"`, `customType:\\"irc:incoming\\"`, rendered template, details, attribution agent (`agent-session.ts:10210-10228`). If streaming, pushes to `#pendingIrcAsides`, optional auto-reply, returns injected (`agent-session.ts:10229-10234`); flushing emits `message_start` and `message_end` external events (`agent-session.ts:10500-10512`). If idle, fire-and-forget `agent.prompt(record)` wakes a real turn (`agent-session.ts:10235-10240`). General `sendCustomMessage` supports `deliverAs: steer|followUp|nextTurn` and `triggerTurn` (`agent-session.ts:5850-5925`). XML-ish notices: IRC `<irc>` template (`prompts/system/irc-incoming.md:1-9`), todo `<system-reminder>` (`agent-session.ts:2432-2438`), checkpoint `<system-warning>` (`agent-session.ts:7789-7793`).\\n\\n5. JSONL model_change: `ModelChangeEntry` has `type:\\"model_change\\"`, `model`, optional `role` with undefined treated as default (`session-entries.ts:42-48`); `EPHEMERAL_MODEL_CHANGE_ROLE = \\"fallback\\"` (`session-entries.ts:5-6`). `appendModelChange(model, role?)` writes the entry (`session-manager.ts:1161-1165`). Roles written: initial SDK omits role (`sdk.ts:2537-2542`); setModel default/custom role (`agent-session.ts:6285-6299`); setModelTemporary `temporary` or `fallback` (`agent-session.ts:6319-6336`); cycle paths omit role or route through setModel (`agent-session.ts:6482-6484`, `6512-6514`); retry apply/restore write fallback (`agent-session.ts:9478-9479`, `9554-9555`). Context reconstruction stores `models[role]` (`session-context.ts:138-160`). `getLastModelChangeRole` returns last `entry.role ?? \\"default\\"` (`session-manager.ts:1287-1294`). `getRestorableSessionModels` returns default only for absent/default/fallback, otherwise role then default (`session-context.ts:25-43`). SDK restore is skipped when `hasExplicitModel = options.model !== undefined || options.modelPattern !== undefined`; then `sessionModelStrings` is `[]` (`sdk.ts:1235-1245`).\\n\\n6. Park/revive: `AgentLifecycleManager.adopt`, `park`, and `ensureLive` are the key APIs (`agent-lifecycle.ts:19-24`, `72-85`, `96-121`, `130-155`). `#revive` awaits the registered reviver, attaches session, sets status idle (`agent-lifecycle.ts:177-183`). IRC send triggers ensureLive for parked recipients (`irc/bus.ts:101-107`); Agent Hub/collab also call ensureLive (`agent-hub.ts:1117-1121`, `collab/host.ts:485-489`). Task executor reviver reopens `sessionFile`, adopts artifacts, calls `createAgentSession(buildSubagentSessionOptions(reopened))`, reinstalls sync (`task/executor.ts:2006-2019`). `buildSubagentSessionOptions` captures spawn-time `model` and `thinkingLevel` explicitly (`task/executor.ts:1936-1943`), causing SDK explicit-model restore suppression (`sdk.ts:1235-1245`). Minimal fix: in `reviveSession`, after open, inspect reopened context models + last role, call `getRestorableSessionModels`, resolve through `modelRegistry`, and pass that model (or omit explicit model when safe) before createAgentSession.\\n\\n7. job tool: registered in builtin factory `job: s => new JobTool(s)` (`tools/index.ts:438-439`), renderer in `tools/renderers.ts:72-73`, builtin name in `tools/builtin-names.ts:20-21`. Schema is `{ poll?: string[], cancel?: string[], list?: boolean }` with descriptions (`tools/job.ts:20-24`); fields name `job`, approval read, strict, interruptible, discoverable (`tools/job.ts:82-91`). `AsyncJob.ownerId` is registry id of the owning agent (`async/job-manager.ts:40-46`). Job tool filters by `this.session.getAgentId?.()` owner (`tools/job.ts:111-116`) and does not resolve AgentRegistry entries. For task background jobs, job id is the spawned `agentId` because register options pass `id: agentId`; ownerId is the parent/calling session id (`task/index.ts:993-996`). Output markdown headings and details are built in `#buildResult` (`tools/job.ts:320-363`); renderer conventions including duplicate id suppression for task labels are `tools/job.ts:451-580`.\\n\\n8. Thinking level: `setThinkingLevel(level: ConfiguredThinkingLevel | undefined, persist = false): void` (`agent-session.ts:6528-6538`) handles `AUTO_THINKING`, otherwise resolves/clamps, applies `toReasoningEffort`/disableReasoning, appends thinking change and optionally persists (`agent-session.ts:6528-6578`). Values are inherit, off, minimal, low, medium, high, xhigh (`thinking.ts:14-40`; core constants `packages/agent/src/thinking.ts:8-18`). `ConfiguredThinkingLevel = ThinkingLevel | \\"auto\\"` (`thinking.ts:103-106`).\\n\\n9. Tests: lifecycle tests fabricate a dispose-only session stub and test TTL park, running disarm, revive, coalesced revive (`test/registry/agent-lifecycle.test.ts:8-34`, `59-130`). Agent Hub tests use a subscribe-only stub and cover parked history/revive (`test/agent-hub-activate.test.ts:22-35`, `83-129`). Session focus harness uses local registry/lifecycle (`test/session-focus-controller.test.ts:96-99`). Task batch tests reset globals, stub discovery/runSubprocess, use AsyncJobManager, assert task job ids Alpha/Beta and idle/history text (`test/task/task-batch.test.ts:231-240`, `256-293`). Task model override tests use fake registry and stub runSubprocess (`test/task/task-model-override.test.ts:22-48`, `80-142`). Model persistence tests use real AgentSession over core Agent, isolated Settings, shared AuthStorage/ModelRegistry, and JSONL helper (`test/agent-session-model-persistence.test.ts:16-130`, behavior `160-330`). Retry fallback tests assert retry_fallback event payloads (`test/agent-session-retry-fallback.test.ts:69-122`, `150-214`).\\n\\n10. hotswap search: case-insensitive search for `hotswap|hot-swap|hot_swap` under `vendor/oh-my-pi/packages/coding-agent/src` returned no matches."', 'details': {'resolvedPath': '/Users/arthur/.omp/agent/sessions/-agents/2026-07-07T00-00-51-895Z_019f39e0-a6b7-7000-a848-f3e75d966101/HotswapSeamScout.md', 'contentType': 'application/json', 'meta': {'source': {'type': 'internal', 'value': 'agent://HotswapSeamScout/architecture'}}}}