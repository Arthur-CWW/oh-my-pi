# Overnight continuation: HR-164 Phase 0→1 + harness queue

## Provenance (manual exemplar for HR-166 — future handoffs autostamp this block)

- Predecessor session: `019f69ac-0907-7000-bf70-990752949949`
- Predecessor journal: `~/.omp/agent/sessions/-agents/2026-07-16T06-45-10-023Z_019f69ac-0907-7000-bf70-990752949949/`
- Predecessor binary: `16.0.1+fork.b97c2a1cb3c0` (you run the newer blessed `16.0.1+fork.468ef51ae3c1` — fresh launch)
- Predecessor handoff chain: `2026-07-17-harness-continuation.md` → this file
- Written: 2026-07-16, cwd `~/agents`, by Fable (Main) at Arthur's direction

## Mission (Arthur, 2026-07-16, verbatim intent)

"Set a goal to do all the harness work overnight that we planned … if they still have juice then they can do even more and continue. basically i just want to get as much done before compaction."

FIRST ACTION: `goal create` — objective: "Complete the planned harness queue below in order, gating and promoting each slice"; workstream `harness`; no hard token budget (advisory throughput, keep working).

## Ground rules (inherited, non-negotiable)

- You run `468ef51ae3c1`: child yields now recover from journals, child `edit` works, child IRC gives typed refusals. The salvage recipes below should rarely fire — but silence ≠ death remains law: check `git status` + child journals before respawning any worker.
- Gate ritual: stage exact files → `vendor/oh-my-pi/scripts/checkpoint-gate.sh -- <focused suites>` → commit → `bun scripts/omp-promote.ts` (bless ≠ adopt; working sessions skip).
- Register discipline: every ask has an HR row; flip with proof links. Update `streams/harness/migration/STATUS.md` at every wave gate — Arthur glances there.
- Routing: bounded work → Luna xhigh; synthesis/taste/architecture/integration → Sol medium+ or yourself; never spawn Fable subagents; never Terra. Kimi for reviewer/scout volume.
- Protected regions: `agent-session.ts` durable-input/abort ranges — sign-off required (Arthur or Main-session Fable via IRC if live).
- Test isolation law: `IrcExternalBus(dbPath)` + `OMP_SESSION_CONTROL_DB` under tmpdirs.
- Workers: no project-wide gates, no destructive git, disjoint file ownership per packet.

## State as of handoff (all committed, all blessed)

- HR-159/160/161 (status-line label, `ga` cmux attach, Ctrl-S/Ctrl-Q hub semantics) — shipped in `30b78ecb8`.
- HR-163 (durable child yields) — shipped in `468ef51ae`.
- HR-164 Phase 0 wave 1+2 — shipped in `15ec09387`/`6f0811714`/`5d25f5f47`: H1 contract (`streams/harness/migration/CONTRACTS/h1-child-lifecycle.md`), pure model + DST driver (1,200 runs/36ms, self-consistent), seam suite (11+2todo), trace conformance, `EFFECT-PORTING.md` (14 probes), H2 evidence (`notes/h2-evidence.md`), STATUS/DIVERGENCES ledgers.

## Queue (in order; each item: implement → gate → commit → promote → STATUS/register update)

1. **HR-165 `/handoff` ownership bug** — root-cause + fix + regression test. Entry points in the register row. P1: Arthur hit it today.
2. **HR-166 handoff provenance autostamp** — same feature area; this file's provenance block is the target shape.
3. **HR-164 H1 inventories scout wave** — ERRORS/CANCELLATION/RESOURCES/CONCURRENCY/TIME .tsv over `src/task/` + spawn runner (scouts + adversarial review per plan §prep artifacts). Prep only — NOT the port.
4. **HR-164 H2 contract draft** — author `CONTRACTS/h2-mailbox-park-revive.md` from `notes/h2-evidence.md` (10 candidate invariants), same shape as H1: state machine, invariants, fault matrix, frozen interface. Mark DRAFT — Arthur's read required before its port wave.
5. **HR-026 detach daemon** (design approved per predecessor handoff — see `TASKS.md` pointer).
6. **HR-148 slice 2**, then **HR-137/138/157**, then **HR-129 slices 3–5** as juice allows.
7. Stretch: HR-164 trial loop (1–3 files of the H1 port through implementer+2reviewers+fixer) on a branch — ONLY if the queue above is clean and green.

## Hard gates you may NOT cross overnight

- The **H1 port wave** (beyond the 3-file trial): requires Arthur's explicit approval of the H1 contract. Not given yet.
- The **H2 port**: requires Arthur's read of the H2 contract draft.
- `agent-session.ts` protected ranges: sign-off only.
- No `sudo`, no secrets, provider spend gates per AGENTS.md.

## Coordination

- The predecessor Main session may still be live in another cmux tab initially — it owns nothing in this queue anymore; if both are live, you own the queue, it answers questions (IRC peer list shows it as an external `agents-*` peer).
- Arthur reviews via `STATUS.md`, the register, and `git log` in the morning. Leave each slice's proof (test counts, gate output refs) in its commit message and register row.
