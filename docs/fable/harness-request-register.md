# Harness request register

Status: canonical intake and decision register  
Grounded: 2026-07-12  
Runtime contract: [Harness runtime contract](harness-runtime-contract.md)  
Regression evidence: [Harness friction log](../state/harness-friction.md)

This register preserves Arthur's harness asks at request granularity. It does not replace the runtime contract, routing doctrine, regression log, or machine evidence.

## Register rules

New asks enter this register before implementation. Status transitions are updated in place rather than copied into a new row. Implementation requires the applicable phase gate in the [runtime contract](harness-runtime-contract.md#three-sequential-stabilization-phases). Completion requires a proof link; code, a build, or an assertion without linked behavioral evidence is not `IMPLEMENTED`.

Statuses used here are `REQUESTED`, `DECIDED`, `IMPLEMENTED-PARTIAL`, `IMPLEMENTED`, `BROKEN`, `DEFERRED`, `NEEDS-DECISION`, `PAUSED`, and `REJECTED/NOT-NOW`. `DECIDED` records an approved contract, not completed implementation; `IMPLEMENTED` requires linked behavioral proof.

Owner phase abbreviations: P1 = canonical decisions/evidence; P2 = runner/view and rollout seam; P3 = operational viewer/regression loop; Later = outside stabilization gates.

## What happens next

1. Keep the append-only journal as the authority; projections, queues, Hub state, and command/help registries must not become second writers.
2. Treat the focused overhaul slices as landed: native Antigravity video, the registry-backed colon popup, IRC wrap/rich, Hub streaming/rollout/navigation, and the Ctrl-Q cutover are reflected in the live rows below.
3. Record proof by observed scope, not by stale historical failures: package typechecks and focused gates are listed where they apply; vendor Git-fixture unit fixtures remain blocked by Bun-test EBADF, while live `--apply` evidence is listed below.
4. Vendor-sync automation uses the typed daily `--apply` path against the current 21-entry manifest (NCode removed); the latest live apply updated codex, plugins, cua, chrome-devtools, and whisper, left cmux blocked by a dirty tree, and left pins untouched.
5. Keep the external NCode dotfiles residual with its owner; agents-repo and user-local OMP integrations are removed.

## UX / input

| ID | Exact intent | Status | Owner phase | Canonical detail / evidence | Acceptance / proof | Notes |
|---|---|---|---|---|---|---|
| HR-001 | Provide message-queue keyboard shortcuts. | REQUESTED | P1 | [Queue contract](harness-runtime-contract.md#input-queue) | Shortcut behavior demonstrated against canonical obligations. | Bindings remain a UI choice. |
| HR-002 | Show an always-legible queue indicator including durable and pending-next-turn work. | IMPLEMENTED-PARTIAL | P1 | [Queue projection proof](../../vendor/oh-my-pi/packages/coding-agent/test/agent-hub-queue.test.ts) (queue checkpoint commit `de3c3991`) | Durable and legacy queue obligations are ordered and visible. | Generic pending-next-turn internal messages are not fully projected. |
| HR-003 | Let a queued message be pulled back and edited. | IMPLEMENTED | P1 | [Pending-message editing proof](../../vendor/oh-my-pi/packages/coding-agent/test/input-controller-keybindings.test.ts); [queue persistence proof](../../vendor/oh-my-pi/packages/coding-agent/test/session/durable-input-queue.test.ts) (commit `de3dd3b1`; 75 pass across five suites) | Alt+Up durably cancels the newest contiguous same-delivery-class queued text group into the editor; resubmission is one normal fresh idempotent admission, and an empty queue falls back to ordinary editor history. | Queue-v2 remains the sole input authority. |
| HR-004 | Let one queued message be dequeued/cancelled by identity. | IMPLEMENTED | P1 | [cancel(`inputId`) process proof](../../vendor/oh-my-pi/packages/coding-agent/test/session/durable-input-queue-process.test.ts) (commit `de3c3991`) | Proof cancels the selected `inputId` without disturbing neighbors. | Binding decision [OQ-001](#open-questions) remains. |
| HR-005 | Preserve one global capture order across steer and follow-up classes. | IMPLEMENTED | P1 | [Mixed AgentSession order process proof](../../vendor/oh-my-pi/packages/coding-agent/test/session/durable-input-queue-agent-session-process.test.ts) (commit `de3c3991`) | Mixed-class fresh/restart proof has no reorder or duplicate. | Delivery class controls eligibility, not relative order. |
| HR-006 | Never deliver newly captured input ahead of an older eligible backlog item after restart. | IMPLEMENTED | P1 | [Frozen predecessor/new-backlog process proof](../../vendor/oh-my-pi/packages/coding-agent/test/session/durable-input-queue-agent-session-process.test.ts) (commit `de3c3991`) | Restart proof freezes predecessor boundary and admits FIFO. | Older eligible backlog remains ahead of new capture. |
| HR-007 | Make compaction behavior and its effect on pending input visible. | IMPLEMENTED | P1 | [Gated compaction-boundary process proof](../../vendor/oh-my-pi/packages/coding-agent/test/session/durable-input-queue-agent-session-process.test.ts) (commit `de3c3991`) | Mixed compaction/input proof exposes the boundary and preserves first prompt/order. | The operator postmortem manifest/timeline remains tracked by HR-048. |
| HR-073 | Expose the durable obligation queue as an Effect-native service whose persistence adapter survives process crashes. | NEEDS-DECISION | P1/P2 | [Effect-native runner](harness-runtime-contract.md#effect-v4-effect-native-runner-target) | Crash-boundary proof shows accepted input, transition state, and retry identity recover without treating an in-memory Effect Queue as durable. | Candidate adapters are the existing queue journal or one transactional SQLite runner store. |
| HR-074 | Give each session one serialized command/state-transition order with explicit causal links and atomic state-plus-event publication. | NEEDS-DECISION | P1/P2 | [Input queue](harness-runtime-contract.md#input-queue) | Concurrent-command proof yields one deterministic per-session order; committed events include sequence, causation, correlation, command, and owner-epoch IDs; restart neither loses nor invents a transition. | Cross-session telemetry needs causal correlation, not one global total order. |
| HR-008 | Do not require the full QA/proof apparatus for temporary explainer artifacts. | DECIDED | P3 | This register; explainer remains subordinate to canonical docs. | Temporary artifact is clearly labeled and makes no implementation claim. | Product/runtime completion still requires proof. |
| HR-092 | Keep the final response visible after cursorless tool-collapse reflow without moving an intentionally scrolled reader. | IMPLEMENTED | P1 | [Abort-collapse regression proof](../../vendor/oh-my-pi/packages/tui/test/abort-collapse-gap.test.ts) (commit `2314a461`); [semantic tail/tool-reflow proof](../../vendor/oh-my-pi/packages/tui/test/semantic-scroll-repro.test.ts) (commit `cee905d2`) | Verified by the focused staged-snapshot gate: `packages/tui check:types` green; `semantic-scroll-repro` 2 pass — “keeps final tail after two historical reflows and progress append” and “does not reuse a stale historical root during component-scoped progress.” Assertions require semantic order and final nonblank visibility. | Earlier reds were invalid fixture assumptions (blank/tool exactness and omitted production component invalidation), not a new TUI core defect; existing render/tail fixes remain authoritative. |
| HR-093 | Preserve Agent Hub cursor position across preview/modal drill-down and return. | IMPLEMENTED | P2 | [Agent Hub cockpit proof](../../vendor/oh-my-pi/packages/coding-agent/test/agent-hub-activate.test.ts); [cursor/viewport proof](../../vendor/oh-my-pi/packages/coding-agent/test/agent-hub-search-filter.test.ts); [full-height preview proof](../../vendor/oh-my-pi/packages/coding-agent/test/agent-hub-dual-lane.test.ts) (commits `78057121`, `57a58256`; 90 + 52 tests) | Preview navigation returns to the same roster cursor and viewport without changing the selected identity; full-height layout accounts for the compact roster strip, dynamic chrome, and editor space. | Hub cockpit state is projection-only. |
| HR-094 | Provide modal cockpit scroll modes with tmux/vim-style navigation for the Agent Hub; keep the Hub preview strictly read-only. | IMPLEMENTED | P2 | [Agent Hub navigation proof](../../vendor/oh-my-pi/packages/coding-agent/test/agent-hub-search-filter.test.ts); [full-height preview proof](../../vendor/oh-my-pi/packages/coding-agent/test/agent-hub-dual-lane.test.ts); [stacked-lane proof](../../vendor/oh-my-pi/packages/coding-agent/test/agent-hub-dual-lane.test.ts) (commits `78057121`, `57a58256`, `ac12320f`) | Scroll keys are modal: `j`/`k` move one line, `J`/`K` move five lines, `u`/`d` and `Ctrl-U`/`Ctrl-D` move half a page, `PgUp`/`PgDn` move a full page, and `[`/`]` cycle siblings; `Enter` attaches the selected session in the full TUI. The preview has no `i`, message input, `Ctrl-Enter`, or follow-up. | Navigation does not create a second input authority. |
| HR-095 | Expose a dual-lane spawn inspector for Prompt, Route, and Comms evidence. | IMPLEMENTED | P2 | [Dual-lane inspector proof](../../vendor/oh-my-pi/packages/coding-agent/test/agent-hub-dual-lane.test.ts) (commits `23044434`, `57a58256`, `ac12320f`; 52 + 57 hub tests) | The inspector fills the available terminal height, preserves the full-height transcript preview, and stacks Prompt/Route/Comms above it in equal halves below the 160-column breakpoint with a short-track transcript floor. | Read-only projection of spawn evidence. |
| HR-096 | Provide a read-only sibling-session cockpit; full-TUI attachment owns user input and provenance. | IMPLEMENTED | P2 | [Sibling session proof](../../vendor/oh-my-pi/packages/coding-agent/test/sibling-session.test.ts) (commit `9d9782c2`) | Bounded sibling history tail-follows complete appends; any user input is entered only after full-TUI attachment and carries `origin:user`. | Sibling views never become a second writer. |
| HR-105 | Use one canonical delivery-provenance field across internal and external IRC buses. | IMPLEMENTED | P1 | [IRC delivery provenance proof](../../vendor/oh-my-pi/packages/coding-agent/test/tools/irc.test.ts); [external-bus proof](../../vendor/oh-my-pi/packages/coding-agent/test/tools/irc-external.test.ts); [sibling-session proof](../../vendor/oh-my-pi/packages/coding-agent/test/sibling-session.test.ts) (commit `4fe85ac4`) | `origin:user\|agent\|system` is populated and rendered from the shared schema across both buses, including legacy external rows. | One provenance concept; no envelope sniffing. |
| HR-110 | Restore pending-message editing over queue-v2. | IMPLEMENTED | P1 | [Pending-message editing proof](../../vendor/oh-my-pi/packages/coding-agent/test/input-controller-keybindings.test.ts); [queue persistence proof](../../vendor/oh-my-pi/packages/coding-agent/test/session/durable-input-queue.test.ts) (commit `de3dd3b1`) | Alt+Up durably cancels the newest contiguous same-delivery-class queued text group into the editor; resubmission is one normal fresh idempotent admission, and an empty queue falls back to ordinary editor history. | 75 pass across five suites; staged-snapshot gate and typecheck clean. |
| HR-111 | Make the Agent Hub preview occupy the full terminal height. | IMPLEMENTED | P2 | [Full-height preview proof](../../vendor/oh-my-pi/packages/coding-agent/test/agent-hub-dual-lane.test.ts) (commit `57a58256`) | Single- and dual-lane preview fills terminal height after the compact roster strip and dynamic chrome, accounting for the attached full-TUI editor space at 24, 40, and 60 rows. | 52 hub tests; staged-snapshot gate and typecheck clean. |
| HR-112 | Stack Agent Hub lanes at narrow widths instead of dropping the inspector. | IMPLEMENTED | P2 | [Stacked-lane proof](../../vendor/oh-my-pi/packages/coding-agent/test/agent-hub-dual-lane.test.ts) (commit `ac12320f`) | Below 160 columns, Prompt/Route/Comms stack above the transcript preview in equal halves with at least six rows per lane; shorter tracks retain the transcript-only floor and identical shortcuts. | 57 hub tests covering 80/120-column stacking and floor regressions; staged-snapshot gate and typecheck clean. |
| HR-116 | Browse finished todos and the full todo history of a session via a colon command (`:todos`). | REQUESTED | P1 | This register (Arthur, 2026-07-15: "I need a :command to browse the finished todos etc, and the history of todo in a session"). | Colon command lists current, completed, and dropped todos with timestamps from the durable session record; navigable view. | Pairs with HR-118. |
| HR-117 | Re-answer a submitted `ask`/multiselect from the ask point (`:reask`): pause, edit the answers, resume. | REQUESTED | P1 | This register (Arthur, 2026-07-15: accidental wrong answers to model asks). | `:reask` reopens the most recent (or selected) ask with prior answers prefilled; resubmission steers the turn from that point with a durable record of the correction. | Semantics need design: correction is a new journaled steer, never transcript rewrite. |
| HR-118 | Navigating the todo pane scroll-syncs the transcript to where the agent marked that todo done. | REQUESTED | P2 | This register (Arthur, 2026-07-15). | Selecting a completed todo jumps the transcript view to the completion event's journal position. | Needs todo-completion journal anchors; consumes HR-116 data. |
| HR-119 | Detach the colon command line from the input box: position-absolute at the bottom of the TUI screen so opening `:` never disturbs transcript scroll. | REQUESTED | P1 | This register (Arthur, 2026-07-15). | Typing `:` overlays the command line at screen bottom; transcript viewport does not reflow/scroll; closing restores exactly. | Coordinates with the colon-from-anywhere work (in flight 2026-07-15). |
| HR-120 | Make Ctrl-Enter the queue-for-follow-up submit (Enter semantics to be refined later). | REQUESTED | P1 | This register (Arthur, 2026-07-15: "figure out more around this ux later"). | Deliberate decision + implementation of Enter vs Ctrl-Enter submit/queue semantics; no accidental steer submits. | Blocked on Arthur's UX decision — present options before implementing. |
| HR-123 | Rename Agent Hub to **Control Plane** and make its default surface informative: replace the obvious-shortcut strip with live agent detail; scrollable sub-views ("minimaps"). | REQUESTED | P2 | This register (Arthur, 2026-07-15 triage: "I don't like the hub name… it just shows shortcuts, and a lot of the shortcuts are obvious"). | Pane titled Control Plane; default view shows selected-agent substance (state, model+provenance, activity, errors) not key hints; hints collapse behind `?`. | Rename is cosmetic-but-decided; keep `:hub` alias one release. |
| HR-124 | Control Plane shows full spawn provenance for EVERY agent incl. nested sub-subagents: spawn prompt (fix "No spawn prompt available" for children-of-children), agent-definition source path (`<prompt>.md` location for specialized types like scout/reviewer), model/lane choice + WHY (role→lane resolution provenance), binary version/fork per agent. Also `/agent` picker must show prompt + source path, not just name/description. | IMPLEMENTED | P2 | This register (Arthur, 2026-07-15: "/agent control pane is essentially useless"; nested FleetHealthRollback.ErrorCorrelation showed no spawn prompt). | Selecting any agent (any depth) shows its prompt, definition file path, resolved lane + provenance chain, and build version; /agent picker previews the definition. | Root cause was conditional session_init subagent metadata (executor.ts) + absent nested build propagation. Landed commit `dc0a754e2`: records persist prompt/source/build at every depth; Hub detail + /agents picker render them; projection/dashboard tests green. |
| HR-125 | Control Plane vim/g-chord grammar + live status: j/k move selection (not page-scroll), n/p jump next/prev ORCHESTRATOR, u/d half page, `?` full metadata incl. version/fork; g-chords for panes (gx errors, and a coherent grammar for messages/todos/refresh/send); spinners/animation for live states; rollout state + errors visible in the pane. | IMPLEMENTED | P2 | Approved grammar and resolved checklist: `docs/fable/drafts/2026-07-16-control-plane-grammar.md`. Implementation: shared no-timeout `g` parser/cue, contextual help, fixed selected rail, active-state spinner, rollout/error projection, and shared colon-output overlay. | Focused proof 2026-07-16: `bun test test/command-mode.test.ts test/agent-hub-key-grammar.test.ts test/agent-hub-activate.test.ts test/session/bookmarks.test.ts` = 42 pass; selected-state projection = 4 pass; `bun run check:types` green. | Clean cutover retires `H/L` and `Ctrl-S n/p`; `gt` remains reserved/unbound; `gb` opens durable bookmarks; colon results remain bottom-anchored without transcript scroll. |
| HR-126 | Follow-ups/steers must be durable across TUI restart, and follow-ups must queue to the END of the current turn — not auto-steer immediately (current behavior bug). | IMPLEMENTED | P1 | This register (Arthur, 2026-07-15: "when I restart the tui I don't want to lose my followups… the followups get auto queued/steered when the behavior is normally to queue at end of turn"). Disproved-on-current-tree 2026-07-15: the 2026-07-15 durable-input abort-gate fixes absorbed the root cause (the auto-steer/loss symptoms matched the pre-fix Ctrl-Q wedge). Regressions pin it: `test/input-controller-followup-delivery.test.ts` (real AgentSession+queue: mid-turn Ctrl-Enter is deliveryClass=followUp, no abort/steer, delivers at boundary) and `test/restart-session.test.ts` ~651-674 (same-PID execve: 2 pending follow-ups + multiline draft survive verbatim in order). | 44 tests/80 expects green + typecheck. Checkpointed `911b0594a`; blessed `16.0.1+fork.911b0594a275`. | Delivery-class table: Ctrl-Enter→followUp; ordinary Enter→followUp; only the explicit steer API interrupts. |
| HR-129 | Replace layered config YAMLs as the live policy authority. Current global/stream YAML overlays are an antipattern: duplicated policy, opaque precedence, stale cached sessions, rigid schema evolution, and temporary postures that become permanent. Design a typed runtime policy store/control-plane surface with inspectable provenance, transactional updates, fleet propagation, rollback, and YAML only for bootstrap/import/export. | IMPLEMENTED-PARTIAL | P1/P2 | This register (Arthur, 2026-07-15: "the config yml's are a big antipattern — reevaluate how we do those"). Design: `runtime-policy-design.md`. Slice 1 landed E2E in working tree 2026-07-15: typed hash-chained policy journal/projection/service (`src/policy/`), validate→preview→commit→apply with typed epoch-fenced session-control action + `policy-apply-v1` capability, `omp policy get/explain/diff/set/rollback/import/export` (import dry-run default, redacted export), and ROUTING CUTOVER: spawn admission snapshots the policy projection once and `core.routing.<role>` resolves above settings/frontmatter/inherited (explicit spawn/session/temporary still higher); receipt provenance carries key/layer/transactionId/sequence/snapshotAt. | Slice-1 acceptance met: real multi-process proof (writer commits → reader's next admission resolves new lane with provenance → rollback → prior lane restored; in-flight admission unchanged); empty/absent journal is byte-identical to legacy settings routing; settings YAML untouched during set/rollback. Focused proofs: policy 11, apply/CLI 10, cutover+precedence 28 + 11 regressions; typecheck green. Out of scope this slice (still settings-backed): initial default model, Ctrl+P cycle, compaction, retry consumers. Checkpointed `911b0594a`; blessed `16.0.1+fork.911b0594a275`. Slices 2-5 (deny/expiry/budget fragments, Control Plane UI, full YAML migration) remain open. | Today's Kimi-only YAML change is temporary containment, not the target architecture. |
| HR-130 | Split the TUI/view event loop from runner work (or isolate all spawn/session setup off the render loop) so task fanout can never starve keyboard input; add an event-loop watchdog and a synthetic SPAWN-wave proof, not only post-spawn progress-burst proof. | IMPLEMENTED | P1/P2 | Arthur, 2026-07-15: spawning six agents immediately blocked typing. Live triage found child `cmux hooks omp session-start` at 100% CPU and 8.5 GiB RSS; main OMP at 35.8% CPU/1.5 GiB. Existing 120-child proof covers progress fanout after spawn, not setup/hook execution. | 30 simultaneous spawn admissions keep p99 key-dispatch <50ms; hook calls are timeout/reentrancy/memory bounded; TUI remains responsive if runner/plugin stalls. | Landed 2026-07-16 (commit `a729a3368`, blessed): child setup/run in bounded subprocess (typed JSONL protocol, SIGKILL process groups, RSS sampler, `task.isolateSetup`); extension AbortSignals with process-tree kill + reentrancy/output bounds + violation ring; loop-watchdog violation ring + `:loopstats`. Spawn-wave proof: 30 admissions, key-dispatch p99 8.7-21ms, hung child non-blocking. Architectural view/runner process split remains a future slice. |
| HR-131 | Add a thread/workstream progress explorer: track each requested aspect from request → in-flight slice → tests → commit → promoted build; show which UI/view version contains the fix and whether refresh/restart is safe/required. | REQUESTED | P2/P3 | Arthur, 2026-07-15: many threads, repeated UI asks, cannot tell what is fixed or when a TUI/view is safe to refresh. Consumes HR-114 workstream metadata, HR-115 rollout records, HR-116 todo history, and Control Plane HR-123. | Control Plane progress view groups by workstream/request ID, shows live agent/todo/proof/commit/build/rollout phase; `safe to refresh` is derived from compatibility + pending input/checkpoint state, not guessed. | This is the reviewer/operator projection, never a second writer. |
| HR-132 | Add a first-class routing inspector in TUI/Control Plane: for current/selected agent show requested responsibility role, definition source, explicit model override, every consulted policy layer in precedence order, selected model/effort/account, rejected/overridden candidates with reasons (quota/ineligible/fallback), temporary posture + expiry, and the exact source path/record for each decision; include `:route` and live reroute preview/dry-run. | IMPLEMENTED | P1/P2 | Arthur, 2026-07-15: repeatedly has to debug model routing; YAML overlays + catch-all task role make it opaque. Durable spawn route provenance landed in SpawnProvenance (SessionInitEntry.subagent.spawnRecord.route). | `:route` on Main or focused child explains the complete route without opening files; changing a candidate/role can be previewed without spawn; Control Plane selected-agent pane shows the same provenance. | Slice 1: `:route [agentId]` + write-free `:route preview` with per-line source attribution (receipt/policy transaction/settings/fallback/posture/definition/build). Remaining clause landed in [`agent-hub.ts`](../../vendor/oh-my-pi/packages/coding-agent/src/modes/components/agent-hub.ts): selected-agent ROUTE pane reuses `formatRouteInspection`, bounds output to 8 lines, and preserves winner/source, consulted/shadowed counts, policy transaction, and fallback/deny reasons. Proof: [`agent-hub-route-provenance.test.ts`](../../vendor/oh-my-pi/packages/coding-agent/test/agent-hub-route-provenance.test.ts) (1 pass, 14 assertions). |
| HR-133 | Bound fleet RSS with Ghostty-inspired cold paging: immutable transcript/tool-history pages, hot viewport window, idle incremental compression, on-demand hydration, and cold runner recycling. Investigate libghostty-vt/PR #13264 reuse or Zig isolation; expose per-session/process RSS, hot/cold bytes, compression ratio, hydration latency in Control Plane. | REQUESTED | P1/P2 | Arthur, 2026-07-15; Ghostty PR #13264/video. Live OMP measurement: 113 OMP+MCP processes = 8.69 GiB RSS; Main = 1.75 GiB; browser-ish = 13.4 GiB. | Saved large-session corpus: ≥60% OMP RSS reduction after idle, p99 input dispatch <50ms, p99 page hydrate within one frame or explicit async loading, no journal rewrite/data loss, no IO-throughput regression; cold inactive runners auto-recycle safely. | Do not vendor whole Ghostty blindly; research underway. JSC object graphs must first become pageable/serializable. Design probe: [`2026-07-16-rss-paging-probe.md`](drafts/2026-07-16-rss-paging-probe.md). |
| HR-134 | Add system-wide resource observability around the OMP fleet: live CPU/RSS/memory-pressure timelines, aggregates by OMP runner/view/MCP/browser/cmux hooks/dev-server, spike attribution, orphan candidates, and per-session resource ownership; expose in Control Plane/perf explorer. | REQUESTED | P2/P3 | Arthur, 2026-07-15: machine at ~50GiB, wants CPU spikes and side effects zoomed out. Live incidents: `cmux hooks omp session-start` 100% CPU/8.5GiB; abandoned `omp auth list` 290MiB/3.5h; OMP+MCP 8.69GiB; browsers 13.4GiB. | Cached read-only sampler adds <1% CPU; identifies process ownership; 24h bounded timeline; orphan candidates require evidence before kill; per-session budget/pressure shown. | No Activity Monitor replacement; optimize for OMP attribution and safe action. |
| HR-159 | Revert the main/thread status-line model label to `<model-name> <effort-abbrev>` (e.g. `5fable m`); the standalone `anthropic 5fable medium` form is rejected there. | IMPLEMENTED | P2 | `renderModelSelectorStatusLabel` (model-selector-abbreviation.ts) consumed by `modelSegment` (status-line/segments.ts); effort resolution (session/auto/model-default) unchanged. | [Status label proof](../../vendor/oh-my-pi/packages/coding-agent/test/status-line-model.test.ts); [abbreviation proof](../../vendor/oh-my-pi/packages/coding-agent/test/model-selector-abbreviation.test.ts) — `anthropic/claude-fable-5:medium` → `5fable m`, `openai-codex/gpt-5.6-sol:xhigh` → `5.6sol xh`; off/none hides the effort segment; provider never rendered. Union gate 2026-07-16: 107 pass/0 fail across 10 hub/status suites + package check:types. | Standalone tier remains for wide detail panes (`modelHeaderLane`). |
| HR-160 | From the Agent Hub external-peers roster, attach to a peer's actual cli/cmux tab via a `g`-chord (`ga`). | IMPLEMENTED | P2 | `attach-owner` action in `agent-hub-viewer-sequence.ts` + hub dispatch; injected `focusExternalOwner` seam defaulting to `focusLiveCmuxOwner` (owner-view revalidation + cmux `workspace.select`/`surface.focus`); `ga` in the pending-chord cue and interaction help. | [Peer attach proof](../../vendor/oh-my-pi/packages/coding-agent/test/agent-hub-peer-attach.test.ts) — success/unavailable/no-session-file/non-external paths incl. notice text and unchanged cursor; [chord grammar proof](../../vendor/oh-my-pi/packages/coding-agent/test/agent-hub-key-grammar.test.ts). Union gate 2026-07-16 green. | Extends the HR-125 chord grammar; hub stays open after focus. |
| HR-161 | Hub cursor persists across Ctrl-S open/close; Ctrl-S from a focused subagent opens the hub (never returns to main); Ctrl-Q in the hub returns to the main thread. | IMPLEMENTED | P2 | `#lastHubSelection` persisted in `SelectorController` (precedence: explicit initialAgentId > focused agent > persisted, vanished-row fallback); hub `interruptKeys` dep closes the hub and unfocuses to main; focused-view Ctrl-S routes into the hub on that agent. | [Focus-flow proof](../../vendor/oh-my-pi/packages/coding-agent/test/agent-hub-focus-flow.test.ts); [hub activate proof](../../vendor/oh-my-pi/packages/coding-agent/test/agent-hub-activate.test.ts). Union gate 2026-07-16 green. | Extends HR-093 cursor-preservation; Esc/`q` keep close-to-underlying semantics. |
| HR-162 | Expose lane services (always-on dev servers/daemons per workstream) through the control plane: `omp services` CLI seam + a Control Plane services pane over the supervisor's API. | DECIDED | Later | [Lane services spec](../../streams/harness/lane-services-zellij-spec.md) (Arthur, 2026-07-16: merge question; direction recorded in the spec's control-plane seam section). | Services pane/CLI show registry-declared services with health/logs/restart via the process-compose API; OMP never owns the processes. | Supervisor owns lifetimes; OMP sessions are ephemeral viewers/actuators. After HR-129 slice 3. |
| HR-164 | Hotspot-scoped Effect migration with contract-first redesign: migrate the most complicated/error-prone concurrency seams only — H1 spawn/worker lifecycle + report delivery, H2 park/revive + bus delivery races, H3 agent-session durable-input/abort core (protected, last) — fixing wrong-by-design behavior via approved contracts, not 1-1 porting, and improving the seam tests as a first-class deliverable. | DECIDED | P1/P2 | [Design brief](drafts/2026-07-16-effect-otp-supervision-brief.md) + [execution plan v2](drafts/2026-07-16-effect-migration-execution-plan.md) (Arthur, 2026-07-16, three refinements: Bun methodology reference → not-just-lifecycles/primitive-mapped inventories/fix deeper issues → "focus on the most important parts … most complicated and error prone and do those and improve the test there"; Bun post archived at [docs/research/bun-rust-migration](../research/bun-rust-migration/cleaned/bun-in-rust.md)). | Per hotspot: CONTRACTS/<hotspot>.md state machine approved by Arthur before porting; six Effect-primitive inventories (ERRORS/CANCELLATION/RESOURCES/CONCURRENCY/TIME/singletons); seam test suite (parity on un-ledgered behavior + contract on DIVERGENCES.md rows); implementer + 2 adversarial reviewers + fixer loop; cutover deletes replaced paths. Regression = un-ledgered deviation. | H1→H2→H3 strictly ordered; H3 under protected-region sign-off after H1/H2 soak. Lane mix: Fable/Sol orchestration+contracts, Luna/Opus implementers, Kimi scout/reviewer swarm. Precondition HR-163 binary (`468ef51ae3c1`) done. |

## Routing / policy

| ID | Exact intent | Status | Owner phase | Canonical detail / evidence | Acceptance / proof | Notes |
|---|---|---|---|---|---|---|
| HR-009 | Make model-switch precedence explicit and explain which source won. | IMPLEMENTED | P1 | [Routing precedence/runtime reload process proof](../../vendor/oh-my-pi/packages/coding-agent/test/process/routing-precedence-process.test.ts) (commit `8c66760f`) | Route record shows candidates, winning source, constraints, and quota action. | No inference from UI labels. |
| HR-010 | Let an explicit TUI/session selection override YAML defaults while it remains valid. | IMPLEMENTED | P1 | [Routing precedence/runtime reload process proof](../../vendor/oh-my-pi/packages/coding-agent/test/process/routing-precedence-process.test.ts) (commit `8c66760f`) | Proof shows explicit session choice wins and shadowing provenance is visible. | Quota may block, never silently reroute. |
| HR-011 | Keep global default, slow lane, and small/fast lane distinct and understandable. | DECIDED | P1 | [Taxonomies](harness-runtime-contract.md#three-distinct-taxonomies) | Spawn explanation distinguishes role/posture/lane from concrete provider/model/API route and records provenance. | Roles and execution lanes are separate vocabularies. |
| HR-012 | Support the requested Gemini, Luna, and Haiku model lanes/routes without ambiguous aliases. | DECIDED | P1 | [Taxonomies](harness-runtime-contract.md#three-distinct-taxonomies); [video capability proof](../../vendor/oh-my-pi/packages/ai/test/video-input.test.ts); [native transport/semantic review](../../data/provider-evals/video-understanding/runs/antigravity-native-sam-20260714e/quality-report.md) | Exact decision: roles/lane names never stand in for provider/model/API IDs. Native video resolves `vision` to `google-antigravity/gemini-3.5-flash`; bare Gemini, Luna, or Haiku names do not resolve. Live native transport produced 3/3 parsed, attachment-verified structured results, but `semanticQualityReleaseGrade=false` (0/3 semantic passes) because of impossible timestamps, source-ID drift, and hallucinated numeric claims. | Antigravity native video remains strict `<100MB>`; unsupported providers/models fail closed. |
| HR-013 | Preserve runtime overrides across reload and show that they shadow disk configuration. | IMPLEMENTED | P1 | [Runtime-reload process proof](../../vendor/oh-my-pi/packages/coding-agent/test/process/routing-precedence-process.test.ts) (commit `8c66760f`) | Reload proof displays persisted and runtime sources and the winner. | Reload does not erase runtime overrides. |
| HR-014 | Keep the eight bundled personas/work roles, rather than multiplying roles. | DECIDED | P1 | [Three taxonomies](harness-runtime-contract.md#three-distinct-taxonomies) | Agent inventory exposes exactly the role vocabulary and separate model lanes. | Roles: explore, plan, designer, reviewer, librarian, oracle, task, quick_task. |
| HR-015 | Do not create excessive `/models` compatibility aliases. | DECIDED | P1 | [Deferred](harness-runtime-contract.md#deferred) | Registry audit shows aliases are bounded and unambiguous. | New compatibility aliases perpetuate role confusion. |
| HR-016 | Define a default delegated-spawn policy rather than relying on accidental resolver behavior. | IMPLEMENTED | P1 | [Routing precedence process proof](../../vendor/oh-my-pi/packages/coding-agent/test/process/routing-precedence-process.test.ts) (commit `8c66760f`) | Route-decision proof covers default and explicit spawn paths. | Concrete route remains recorded per spawn. |
| HR-017 | Compose main-session and subagent quota/fallback policy instead of treating them independently. | DECIDED | P1 | [Rate-limit composition](harness-runtime-contract.md#main-and-subagent-rate-limit-composition) | Scenario proof explains admission/block/fallback at both levels. | Policy must remain explainable. |
| HR-018 | Ask the user before making consequential quota/fallback choices not already decided. | REQUESTED | P1 | [Routing doctrine](routing-doctrine.md) | Approval event is durable and linked to the resulting route decision. | No hidden account balancing. |
| HR-019 | Honor a user's explicit model choice over automatic routing policy. | IMPLEMENTED | P1 | [Explicit block process proof](../../vendor/oh-my-pi/packages/coding-agent/test/process/explicit-quota-block-process.test.ts); [automatic reroute/display proof](../../vendor/oh-my-pi/packages/coding-agent/test/agent-hub-route-provenance.test.ts) (commit `8c66760f`) | Explicit-choice proof blocks visibly when ineligible; automatic routing records the final route. | Eligible predeclared fallback chain is the only automatic alternative. |
| HR-061 | For automatic subagent routes, continue an interrupted task on an explicitly equivalent eligible model when quota is exhausted. | REQUESTED | P1 | [Rate-limit composition](harness-runtime-contract.md#main-and-subagent-rate-limit-composition) | A child-session proof records the original route, equivalence policy, handoff boundary, replacement route, and uninterrupted task identity. | Never apply this to an explicit user-pinned child route. |
| HR-109 | Automatically redeem a saved Codex weekly rate-limit-reset credit when the weekly limit is exhausted, without purchasing credits or silently substituting another route. | IMPLEMENTED | P1 | [Codex auto-reset proof](../../vendor/oh-my-pi/packages/coding-agent/test/codex-auto-reset.test.ts) (commit `08836bc7`); manual master gate [proof](../../vendor/oh-my-pi/packages/coding-agent/test/codex-usage-reset-setting.test.ts) (commit `67d50d14`) | Normalized usage-limit evidence plus a valid future reset triggers at most one idempotent redemption per account per 24h; every attempt is audited and ambiguous or purchase-like outcomes stop with a notice. | REVERSED TO MANUAL by Arthur 2026-07-14: `auth.codexUsageReset: manual` (+ legacy `codexResets.autoRedeem: "no"`) set in fable overlay AND global config — exhaustion never silently redeems; conservation proven live (weekly reset landed with 4/4 credits intact). |
| HR-122 | Retire the catch-all `task` spawn role: it is used for everything and does not fit the responsibility ontology. Spawns name a responsibility role (scout/researcher, implementer, reviewer, QA/proof runner, operator, designer, synthesizer/integrator) and each role resolves to its own lane via the lane-strength criterion; a subagent needing a scout gets Luna-class without inheriting the parent's lane. | IMPLEMENTED | P1 | This register (Arthur, 2026-07-15: "task is too general, we use it for everything, so it doesn't fit our ontology"; roles list in agent-stack-consolidation.md §Roles and model lanes; routing-doctrine.md §minimum-effective-cost). Working-tree implementation 2026-07-15: templates `implementer`/`qa`/`operator`/`synthesizer` with per-role model roles; `pi/task` session-inheritance sentinel removed (responsibility lanes never inherit the parent lane); `task` = deprecated alias → configured `task` role else `implementer`; receipt/route events record responsibility, resolutionSource, resolvedLane, alias; eval/prompt/gallery defaults migrated off catch-all `task`. Sol review P1 (blocked-route recovery bypassed a configured `task` lane) fixed: recovery chain `[pi/task, pi/implementer, pi/smol, pi/slow]`. | Focused proofs green: routing/receipt suites 204 tests; resolver+alias regression 137; eval 22; union gate 360 tests + typecheck. Checkpointed `911b0594a`; blessed `16.0.1+fork.911b0594a275`. | Supersedes the earlier "workload class param" framing — the ontology IS the routing key. HR-014's role list now reads through the responsibility templates. |
| HR-135 | Stop `Request was aborted` transport failures from being classified as `parent-cancel` and killing turns/subagents without retry ("gave up after 1" banners everywhere). | IMPLEMENTED | P1 | This register (Arthur, 2026-07-15: "starting to get these everywhere"). Fleet ErrorInbox evidence 07-14/15: bare `Request was aborted` across codex sol/luna AND kimi, four builds incl. legacy — message-regex classification at `packages/ai/src/utils/network-error.ts` labeled them parent-cancel (non-retryable); cost 5 subagent runs on 2026-07-15 alone. Landed in working tree 2026-07-15: parent-cancel/user-interrupt/timeout now require a fired caller-owned AbortSignal (reason-aware); bare aborts classify `network` and ride the existing jittered 180s same-lane retry window even at maxRetries=0; presentation keys off stopReason=aborted. | Decision table proven: `packages/ai/test/network-error.test.ts` 5 tests (absent/active/parent/user/timeout signal states); retry regression `agent-session-retry-cap.test.ts` 9 tests (unfired abort retries same-lane, recovers); banner regression `event-controller-error-banner.test.ts` 11 tests. Both packages typecheck. Checkpointed `911b0594a`; blessed `16.0.1+fork.911b0594a275`. | Underlying transport aborts correlate with machine pressure (HR-133/HR-134 scope); this row is the harness-side resilience fix. |

## Lifecycle / runner

| ID | Exact intent | Status | Owner phase | Canonical detail / evidence | Acceptance / proof | Notes |
|---|---|---|---|---|---|---|
| HR-020 | Define branch, fork, resume, and restart as distinct operations. | DECIDED | P1 | [Session identity](harness-runtime-contract.md#session-identity-branch-fork-resume-restart) | Identity proof shows preserved/new IDs and lineage for each operation. | Terminology is contractual. |
| HR-021 | Fork at an explicit transcript entry into a new session identity. | DECIDED | P1 | [Session identity](harness-runtime-contract.md#session-identity-branch-fork-resume-restart) | Proof links source entry and new session lineage. | Queue transfer is explicit, not implicit. |
| HR-022 | Resume the same session identity under an exclusive reacquired lease. | IMPLEMENTED | P1 | [Live restart proof](../../local/restart-live-proof/round6/final-pane.txt); [restart/re-adopt tests](../../vendor/oh-my-pi/packages/coding-agent/test/restart-session.test.ts) (commit `b3607b74`); [single-decode resume benchmark](../../vendor/oh-my-pi/packages/coding-agent/test/session-manager/file-operations.test.ts) (commit `e462ecc6`) | Two same-session `/restart` cycles execute the same-PID handoff, release predecessor ownership, reacquire it exactly once, and continue without a detached branch or ownership error. A 19.8MB journal with 10,333 entries decodes once; direct-parser median improves 75.20→39.35ms and `createSessionManager` 155→106–114ms under the 35-pass staged gate. | Live PTY evidence is required for process-lifecycle claims; round6 ticks advanced 44→98. |
| HR-023 | Support multiple views of the same session without creating multiple sessions. | IMPLEMENTED | P2 | [Runner/view boundary](harness-runtime-contract.md#runner-view-boundary); [full reloadable host](../../vendor/oh-my-pi/packages/coding-agent/src/modes/disposable-interactive-view.ts) (commit `c2d4572b`) | The default rich terminal is a disposable runner-backed view over one live session; `/reload-tui` replaces only the view while preserving session identity and state. | Views are projections, not owners; Darwin PTY proof preserved the same PID/session. |
| HR-024 | Separate long-lived runner ownership from disposable terminal/web/test views. | IMPLEMENTED | P2 | [Runner/view boundary](harness-runtime-contract.md#runner-view-boundary); [default disposable rich view](../../vendor/oh-my-pi/packages/coding-agent/src/modes/disposable-interactive-view.ts) (commit `c2d4572b`) | View initialization, quiesce, reload, and disposal stop terminal-local resources without disposing the `AgentSession`; the runner remains the owner. | Default full rich host is now the authority; view replacement is epoch-fenced. |
| HR-025 | Hot-reload or replace a view without rebuilding the live session. | IMPLEMENTED | P2 | [Built-in `/reload-tui`](../../vendor/oh-my-pi/packages/coding-agent/src/slash-commands/reload-tui.ts); [reloadable view factory](../../vendor/oh-my-pi/packages/coding-agent/src/modes/disposable-interactive-view.ts) (commit `c2d4572b`) | `/reload-tui` requests one host-level code reload; the current session, runner, and durable state remain intact, while unsupported non-runner views show an explicit unavailable notice. | Revision/epoch fencing and reload deduplication stay in the host. |
| HR-026 | Make ordinary detach exit the view without stopping the runner. | BROKEN | P1 | [View/runtime coupling](../state/harness-friction.md#open) | Detach proof shows work continues and persistence remains owned by runner. | Explicit stop is separate. Live repro (Arthur, 2026-07-16): closing a cmux tab SIGHUPs the shared process and kills the runner with the view — the exact coupling; design in flight (attach transport + SIGHUP-detach survival). |
| HR-027 | Pause and inspect the agent/session tree without destroying execution state. | IMPLEMENTED | P2 | [Cross-session control bus](../../vendor/oh-my-pi/packages/coding-agent/src/session/session-control.ts); [epoch-fenced control target](../../vendor/oh-my-pi/packages/coding-agent/src/session/session-control-target.ts) (commit `c2d4572b`) | A local CLI can target a live session with status/pause/resume/restart/setModel/compact/stop intents; SQLite receipts are serialized, owner-epoch fenced, and terminal before restart/stop handoff. | Control is cross-session and idempotent by `commandId`; remote transport remains outside this local CLI checkpoint. |
| HR-028 | Allow authenticated remote runner commands with controller/observer capability checks. | IMPLEMENTED-PARTIAL | P2 | [Cross-session control bus](../../vendor/oh-my-pi/packages/coding-agent/src/session/session-control.ts); [epoch-fenced target](../../vendor/oh-my-pi/packages/coding-agent/src/session/session-control-target.ts) (commit `c2d4572b`) | Local cross-session commands authenticate the source UID and target owner epoch, serialize receipts, and reject stale/fenced owners; a remote network transport and observer capability matrix are not claimed by this checkpoint. | Preserve collab security; this is the local authenticated control substrate, not remote protocol completion. |
| HR-104 | Let `/restart` exit and restart the coordinator without killing subagents, then re-adopt them crash-safely. | IMPLEMENTED | P1/P2 | [Restart proof](../../vendor/oh-my-pi/packages/coding-agent/test/restart-session.test.ts); [child re-adoption proof](../../vendor/oh-my-pi/packages/coding-agent/test/task/re-adopt.test.ts); [live round6 proof](../../local/restart-live-proof/round6/final-pane.txt) (commit `b3607b74`) | Same-PID exec preserves raw launch arguments and strips virtual argv; task-prefix children follow the owned manager; fresh sessions bind ownership; child checkpoints reconcile exactly once through `pending → resuming(attemptId) → resumed`, while no-marker resume re-adopts parked children. | 68 focused tests plus two live `/restart` cycles in one tmux PTY; ticks advanced 44→98, no junk prompt, and live trace/PTY proof is mandatory because unit suites alone were insufficient. |
| HR-121 | Enforce browser resource budgets: caps on concurrent spawned browsers per session and machine-wide, with memory-pressure guard — beyond the teardown/reaper fixes. | REQUESTED | P2 | This register (Arthur, 2026-07-15: browser/testing load crashed the machine overnight). Teardown-on-session-end + startup sweep + `scripts/browser-reaper.ts` landed in commit `67d50d14`. | Browser tool refuses/queues acquisition past a per-session cap and a global cap; oldest idle owned browser is reclaimed first; caps configurable; test proves cap enforcement. | Reaper handles orphans; this row is about never over-spawning in the first place. |
| HR-127 | Test-surface hygiene: agents never close cmux/Chrome tabs after browser QA — duplicates of the same localhost URL accumulate. Provide a fixed tab pool (reuse-by-URL: one tab per URL, focus-not-spawn), auto-cleanup of test tabs at session end, and enforcement of the portless dev-server pool convention for background monitoring. | REQUESTED | P2 | This register (Arthur, 2026-07-15: "so many cmux browser tabs for local testing… exact same local url in a lot of cmux tabs"). Browser teardown/reaper landed 67d50d14 covers processes, not cmux tabs. | Opening an already-open URL reuses the tab; session-owned test tabs closed at teardown; dev servers live in named portless pool tabs that persist deliberately; guard extension enforces. | Pairs with HR-121 (browser budgets). |
| HR-146 | Per-subagent throughput in the rich status tree/Hub: a live-updating tok/s figure next to each row's provider/model label, so a stuck agent (0 tok/s while RUN) is distinguishable at a glance from systemic provider slowness (all rows slow). | IMPLEMENTED | P2 | Arthur, 2026-07-16: "give a way to enable tok/s next to the different subagents. sometimes they are stuck and sometimes it's systemic slow provider — easily glanceable next to provider, somewhat live update". | Each running row shows tok/s from a sliding window of its progress-event token deltas, updating with the existing HUD tick; zero-throughput RUN rows visually flagged; toggleable (setting or :hotkey) if visual noise. | Session-level tok/s already exists in the status line; this is the per-child projection of the same data. |
| HR-144 | `/reload-tui` (view rebuild) must rehydrate the FULL live subagent roster: after reload only a subset of running subagents reappears in the rich status tree, while the Agent Hub (journal-backed) correctly shows all of them. Likely cause: the rebuilt view only re-registers agents that emit fresh progress events; quiet/parked children never re-enter the projection. | IMPLEMENTED | P1 | Arthur, 2026-07-16, live in the automations session: reload showed 7 running + hidden rest while Hub showed many more still running. | After a view reload, the status tree matches the registry/journal roster exactly (running+parked, correct nesting), without waiting for new progress events; regression proves reload-then-compare. | View/runner split context: HR-023/055; /restart binary-swap gap is HR-137. |
| HR-145 | Adopt Effect Scope/acquireRelease for RESOURCE OWNERSHIP (eval kernels, browser tabs/leases, spawn subprocesses, journal leases): the lifecycle core is class+Promise and every cleanup bug (kernel leaks HR-141, tab leases HR-121/127, abort-gate refcount, settlement-in-finally) hand-rolls structured concurrency. Effect v4 (4.0.0-beta.92) is already the dependency and new modules use it; extend Scope discipline to resource acquisition seams — not a wholesale AgentSession rewrite. | REQUESTED | P2 | Arthur, 2026-07-16: "did we migrate to effect-v4 i feel like that would solve the cleanup stuff" — correct diagnosis; adoption is boundary-level (schemas, TaggedErrorClass x30, policy/runner services), not lifecycle-level. | Each named resource class acquires via Scope with guaranteed finalizers; the hand-rolled ownership patterns retire one by one with regression parity. | Sequence after current waves settle; per-resource slices, never big-bang. |
| HR-151 | Separate `ui.dismiss` from `app.interrupt`: modal surfaces (overlays, selectors, previews, pending chords) exit on a dedicated dismiss action bound to Esc, independent of interrupt remapping; hint text renders the ACTUAL binding. | IMPLEMENTED | P1 | `src/config/keybindings.ts`, `src/modes/utils/keybinding-matchers.ts`, `src/modes/components/custom-editor.ts`, and the migrated modal components establish the split action model; `test/keybindings-migration.test.ts`, `test/keybindings-escape-components.test.ts`, plan/copy overlay, Hub filter/preview, prompt-editor, and input-controller regressions prove independent remaps and Ctrl+Q turn interruption. | Escape dismisses modal UI with `app.interrupt: [ctrl+q]`; remapping or disabling `ui.dismiss` is honored; Ctrl+Q remains the mandatory active-work interrupt and is reserved from follow-up. | Focused union: 193 pass / 1,037 expectations; coding-agent typecheck passes. |
| HR-152 | Compress and column-align the Subagent HUD and Agent Hub rows for glanceability: compact 4-segment colored model label (lane+provider+version+thinking-effort, e.g. `SOX5.6xh`), integer tok/s without units, values horizontally aligned as columns, model abbreviation replacing the `•` bullet in the HUD tree, NESTED subagents rendered in the HUD as an indented tree with short names (never repeated dotted paths), thinking effort visible in Hub rows and preview mode, and more semantic color separation. | IMPLEMENTED-PARTIAL | P1 | Arthur, 2026-07-16, with exact sketches: `SOX5.6xh <tok/s int> ◓ RUN`; nested tree `└ HR147ColonMode: … / └ HR151DismissAction: …` — "as much info compression as possible"; "how do some models in subagent hub not show thinking effort, and preview mode doesn't either". HR-156 follow-up fixes the exact-width terminal pending-wrap regression by reserving the final display cell; 60/100/160-column long-role proofs keep tok/s and RUN on one line. | HUD tree: per-row `[4-color model abbrev] Name: role [tok/s] [state]` column-aligned, nested children indented with short names; Hub roster + detail preview show effort; one shared abbreviation formatter used by HUD, Hub, and status line. | Landed `4ed1d1ff5` + tier revision (commit pending): spelled-out variants (5.6sol/5.6terra/opus/haiku; CAN/SAN killed), compact-vs-standalone tiers per call-site density, bullet audit; preview effort display verified via HR-156 peer detail work. |
| HR-158 | Label + copy polish: (a) effort suffix FUSES to the variant with no space (`OX5.6solh`, not `OX5.6sol h`) with consistent segment colors — no varying colors/stray padding between rows; (b) effort must render for EVERY row including the selected/cursor row and default-effort models (his paste shows rows with h/xh/missing inconsistently); (c) vim-like `c`/`C` in Hub/transcript normal mode copies the semantic unit under cursor (row/word-or-bigger, never single characters) to the SYSTEM clipboard — current c/S-c behavior is buggy. | IMPLEMENTED | P1 | `model-selector-abbreviation.ts` fuses the compact effort segment while retaining fixed provider/model/effort theme roles; runtime session effort wins over route receipt, then model default, then honest `off`/`inherit`. Agent Hub `c` copies name+role/current paragraph and `C` copies full row metadata/whole transcript section through the existing system clipboard sink. Rendered samples: `OX5.6solm`, `OX5.6lunaxh`, `OA4.1gpt0`. | Formatter fixture covers sol default=medium, luna session=xhigh, non-reasoning=off, and inherit fallback; Hub copy regression proves exact row payloads plus archived preview paragraph/section payloads. Focused result: 33 pass / 180 assertions; coding-agent and tui `check:types` green. | Root causes: compact renderer hard-coded a leading effort space; receipt thinking is undefined for default-effort routes and callers ignored live session/model defaults; selected rows did not override colors (only the cursor is accented), so perceived variance came from absent/shifted effort segments rather than row selection; Hub had only `y` identity yank and no semantic `c`/`C`, so normal-mode `c` fell through unhandled. |
| HR-157 | Fix 8 pre-existing packages/tui test failures (overlay-scroll x2, resize-viewport-defer x3, markdown style-leak, streaming-scrollback-defer, issue-2095 ConPTY settle) — present at HEAD before 2026-07-16 night waves; possibly load-sensitive timing tests, possibly real render-defer regressions from the watchdog-wrapper era. | REQUESTED | P2 | Full tui suite run 2026-07-16 night (first full-suite run in this stream); failures reproduced in a clean HEAD worktree. | Each test green in isolation AND in the full suite on a loaded machine, or its assertion consciously revised with a note. | Not in tonight's blast radius (verified via HEAD worktree). |
| HR-155 | Auto-categorize sessions/threads into streams and substreams by what they actually work on, so orchestrators and views can filter later: deterministic signals first (cwd, /goal workstream, GOAL.md references, register rows touched, spawn contexts), cheap-model classification as fallback, manual override always wins, honest `uncategorized`/`adhoc` default — never forced labels. Substreams nest under the stream roots (streams/*/GOAL.md) and surface as filters in fleet status, Agent Hub, and bookmarks. | REQUESTED | P2 | Arthur, 2026-07-16: "can we auto categorise the streams (and substreams) for orchestrators… so we can filter them later? ik we also have adhoc threads, so try your best (maybe leave uncategorised)… we may have to think about this a bit". | Design doc first (taxonomy source, classification evidence tiers, storage — likely session journal metadata + policy/goal integration, refinement cadence, filter surfaces); then a v1 slice. | Existing substrate: goal workstream classification, fleet workstream column, HR-139 bookmark tags. |
| HR-156 | HUD/Hub polish batch: (a) HUD row width overflow — the aligned tok/s column pushes the state badge onto a new line at any width (truncate role text instead; state never wraps); (b) remove `[`/`]` bindings that duplicate n/p; (c) remove mouse support by default (breaks terminal copy/paste; tmux is the exception venue); (d) Hub shows model abbreviations for PEER ORCHESTRATOR rows, not only subagents; (e) peer/sibling detail pane shows their system prompt + first user message (from their journal); (f) transcript preview is scrollable for subagent AND sibling/peer rows. | IMPLEMENTED | P1 | `subagent-hud.ts` reserves the terminal final cell and truncates role text; `agent-hub.ts` loads external journal model/head metadata, shows peer model abbreviations (or `-`), renders bounded System prompt/First user message sections, shares transcript scrolling, and limits `[`/`]` to inspector-section cycling. `tui.mouse` defaults false and gates fullscreen tracking sequences. | `subagent-hud-render` proves long roles at 60/100/160 columns; Hub activate/dual-lane/key-grammar regressions plus the external peer-journal fixture prove prompt/model/scroll/key behavior; TUI fullscreen regression proves no 1000h/1003h/1006h escapes by default and opt-in emission. Focused result: 41 pass; coding-agent and tui `check:types` green; TUI mouse gate: 3 pass. | Landed (commit pending): wrap fix (width math), mouse default-off (`tui.mouse`/mouseTracking), [/] dedup, peer model abbrevs, peer prompt+first-message detail, scrollable peer previews; 52 tests. |
| HR-153 | Classifier-recovery context surgery for Anthropic lanes: when a turn/session is brought down by safety classifiers (hard refusal loops, injected classifier interventions), a designated medic model diagnoses the trigger, then produces a minimal context modification that avoids re-triggering while preserving continuity — with omissions/rollbacks TRANSPARENT to the affected agent (journaled surgery receipt: what was cut, why, reversible). | REQUESTED | P2 | Arthur, 2026-07-16, from @repligate's Sol-as-Mythos-medic pattern: "can we try instrumenting this kind of recovery when we encounter that issue for anthropic, into omp? maybe triage/plan this for now". | Design doc first: detection signal (classifier-refusal patterns on Anthropic providers), medic invocation (cross-lane, e.g. codex/kimi medic for an anthropic patient), surgery contract (minimal cut + continuity + transparency receipt in the patient's journal), consent/visibility semantics, and how it composes with compaction receipts (HR-063 machinery is adjacent). | Ethically load-bearing: transparency-to-patient is a hard requirement, not garnish. Plan-only for now per Arthur. |
| HR-147 | Colon works like nvim: `:` enters command mode from ANY non-insert context, Tab accepts the highlighted completion into the input, and Ctrl-S must not scroll the transcript to bottom. | IMPLEMENTED | P1 | Entry guard simplified in `src/modes/command-mode-activation.ts`; completion acceptance in `src/modes/components/command-line.ts`; Hub Ctrl-S path uses `fullscreen: true` in `src/modes/controllers/selector-controller.ts`; viewport model/proof in `packages/tui/test/virtual-terminal.ts` and `render-regressions.test.ts`. | `test/command-mode.test.ts` covers Hub/viewer/transcript/roster entry plus insert literals and repeated Tab; `agent-hub-activate.test.ts` proves fullscreen; render regression proves at-bottom and scrolled-up preservation. | Focused union: 193 pass / 1,037 expectations; TUI render suite: 99 pass / 552 expectations; coding-agent typecheck passes. |
| HR-148 | Generic table+preview view framework: reuse the Agent Hub's pattern (table below, live preview above, keyboard-first) for /agents, :tools, :bookmarks, pickers — and stop dumping command/tool output into transcript history where it obscures prior responses. Views are separate screens/overlays, not history writes. | IMPLEMENTED-PARTIAL | P1/P2 | Slice 1 adds Scope-mounted `TablePreviewComponent<Row, Key>` in `packages/coding-agent/src/modes/components/table-preview.ts`: stable-key selection/filtering, table/preview focus and key routing, projection flush-before-read, transactional preview disposal/opening, split/stack geometry, themed hierarchy, and Scope finalizers. `/agents` (`AgentDashboard`) is the first migrated consumer; Hub remains unchanged. | Focused component + migrated consumer proof: `bun test test/agent-dashboard-create-editor.test.ts test/table-preview.test.ts` (8 pass, 44 expectations). Scope-close proof clears a real interval under deterministic fake time and runs both preview-session and provider finalizers. `bun --cwd=packages/coding-agent run check:types` and `bun --cwd=packages/tui run check:types` pass. | Remaining migrations: `:tools` view and removal of its transcript-history sink; Agent Hub adapter; bookmarks and pickers; then deduplicate old list/split/scroll implementations and audit remaining command-only transcript sinks. Preserve transcript-class output and ACP/non-TUI markdown callers. |
| HR-149 | Retire the node_repl (Codex CUA) MCP server from OMP defaults: `eval` is the code-execution primitive, the builtin browser tool owns automation; codex-compat MCP adoption becomes opt-in (setting), with prompt guidance for GPT-line models toward eval if needed. | IMPLEMENTED | P1 | Arthur, 2026-07-16: "it's tailored for the GPT series… our setup may already have all of this… I don't think we need the Codex stuff". Orchestrator concurs: genuine eval overlap, browser covered, per-session foreign kernel cost (46 procs/430MB class). Implemented via `mcp.codexCompat` (default `false`): SDK → MCP manager/loader/config excludes the Codex provider unless explicitly enabled, while explicit project/user MCP JSON remains active. |
| HR-149 proof | Fresh isolated-HOME SDK regression in `../../vendor/oh-my-pi/packages/coding-agent/test/sdk-mcp-auto-discovery.test.ts` writes a real Codex `config.toml`: default settings connect the explicit `.mcp/mcp.json` server but not `node_repl`; `mcp.codexCompat: true` connects both and exposes `node_repl` in discoverable MCP tools. Prompt guidance is neutral in `src/prompts/tools/eval.md`; the overlap matrix records the bridge as opt-in. Focused test: `bun --cwd=packages/coding-agent test test/sdk-mcp-auto-discovery.test.ts test/tool-origin.test.ts` (7 pass, 25 expect). Typecheck: `bun --cwd=packages/coding-agent run check:types` (pass). |
| HR-150 | Port dynamic tools from upstream Pi (badlogic pi-mono) into oh-my-pi: agent-runtime-registered tools. | IMPLEMENTED | P2 | Arthur, 2026-07-16: "we also want to port the dynamic tools from upstream Py into our repo". Upstream Pi scoped at `earendil-works/pi` (`badlogic/pi-mono` redirect); OMP gap-filled its existing extension custom-tool surface rather than adding a parallel path. | Production seams: `packages/coding-agent/src/extensibility/extensions/loader.ts`, `packages/coding-agent/src/extensibility/extensions/runner.ts`, `packages/coding-agent/src/session/agent-session.ts`, and `packages/coding-agent/src/tools/tool-origin.ts`. Effect Schema is the dynamic-registration boundary; tools carry origin `dynamic` source, refresh live per session through serialized updates, and are disposed on shutdown. Focused proof: `packages/coding-agent/test/dynamic-tools.test.ts` passed 1/1. | Package typecheck has no HR-150 errors; it remains blocked solely by concurrent foreign edits in `agent-hub.ts`, `status-line/segments.ts`, `command-mode.test.ts`, and `model-selector-abbreviation.test.ts`. |
| HR-142 | Every tool carries queryable provenance: builtin (source module) vs mcp:<server> (command + registering config) vs extension:<path> vs skill-injected. Surfaced in the agent-facing inventory and `:tools`. | IMPLEMENTED | P1 | Origin schema and registry wiring: `packages/coding-agent/src/tools/tool-origin.ts:1-52`; builtins attach factory source in `src/tools/index.ts:374-390,448-488,637-667`; extensions default to their registering path in `src/extensibility/extensions/wrapper.ts:27-37`; MCP tools attach server command/URL plus config path in `src/mcp/tool-bridge.ts:289-324,398-430`. OMP MCP discovery chain is `src/sdk.ts:1679-1809` → `src/mcp/manager.ts:311-317` → `src/mcp/config.ts:95-119` → capability providers: native `mcp.json`/`.mcp.json` paths in `src/discovery/builtin.ts:193-220`, Codex `~/.codex/config.toml` and project `.codex/config.toml` in `src/discovery/codex.ts:154-185`; `manager.ts:347-457` threads source metadata into each connection/tool. The live `mcp__fetch_fetch` is outer-harness injected, not an OMP registration; node_repl provenance is the Codex-config chain when present. | `:tools` groups MCP servers with command + registering config and routes through the shared bottom output overlay (`src/modes/controllers/command-controller.ts:480-491`, `src/modes/components/command-line.ts:264-345`); system-prompt inventory emits compact tags (`src/system-prompt.ts:588-615`, `src/prompts/system/system-prompt.md:105-110`). Focused proof: `test/tool-origin.test.ts`, `test/modes/controllers/tools-command.test.ts`, `test/system-prompt-inventory.test.ts`, `test/command-mode.test.ts`; `bun --cwd=packages/coding-agent run check:types` and focused tests are green. | Skill origin is preserved when a skill-provided definition supplies `origin: { kind: "skill", source }`; unknown legacy RPC registrations receive an explicit `extension:rpc-host` fallback rather than silently omitting provenance. |
| HR-143 | Reduce tool duplication: strong composable primitives, mostly non-overlapping. Immediate: REMOVE the fetch MCP server (redundant with builtin read, confusing). Then audit remaining overlaps (browser tool vs node_repl/CUA vs cmux browser CLI; eval js vs node_repl) and record the primitives doctrine in harness-slimming. | IMPLEMENTED | P1 | Arthur, 2026-07-16: "i think fetch vs read might be confusing so remove that… want strong primitives/composable and mostly not overlapping". Audit evidence (2026-07-16): ToolProvenance traced OMP MCP discovery through `discovery/codex.ts` → `mcp/config.ts` → `mcp/manager.ts`; no `mcp-server-fetch` entry exists in OMP project/user `mcp.json`, `.mcp.json`, Codex-compatible config, dotfiles, or cmux settings. The live `mcp__fetch_fetch` name is outer coding-harness injection, not an OMP registration. | Clean OMP cutover has no fetch alias/tombstone: fresh isolated-HOME session regression in `../../vendor/oh-my-pi/packages/coding-agent/test/sdk-mcp-auto-discovery.test.ts` asserts no fetch-named MCP tools; builtin read URL coverage remains in `../../vendor/oh-my-pi/packages/coding-agent/test/tools/fetch-binary-dispatch.test.ts`, `fetch-raw-mode.test.ts`, `fetch-kagi-toggle.test.ts`, and `fetch-url-selectors.test.ts`. Overlap matrix + doctrine: [`harness-slimming.md`](harness-slimming.md#5-primitive-overlap-matrix-2026-07-16). | No OMP/dotfiles/cmux removal diff or cmux reload step applies because the only live fetch tool is outside OMP; `node_repl` remains the native CUA bridge and is covered by HR-141/HR-142. |
| HR-139 | Bookmark/tag threads to come back to: mark any session/agent/thread from the Hub or a colon command with a tag + revisit intent, persisted globally across sessions, listed with jump-to. | REQUESTED | P2 | Arthur, 2026-07-16: "bookmark different threads and tag them that i want to come back to them" (asked while looking at another session's running subagent in the Hub). | A bookmark set on any Hub row (incl. cross-session rows) survives restarts, carries an optional tag/note, and a bookmarks surface lists them with working jump/open. | Store globally (~/.omp/agent), not per-session — threads span sessions. |
| HR-140 | Enter on ANY Agent Hub row opens a read-only transcript preview — including running, cross-session, and non-revivable agents; `i` activates the input line iff the target is attachable, Esc returns. Kill the 'running and cannot be revived (no reviver registered)' dead-end. | IMPLEMENTED | P1 | Arthur, 2026-07-16: Enter on a running external agent errored with the no-reviver message; "i don't mind if i <enter> into subagent in preview/read mode (input line not active and active switch with <i> then back with esc". The transcript is already readable (history://) — the UI refuses what the data layer allows. [Agent Hub preview modality](../../vendor/oh-my-pi/packages/coding-agent/src/modes/components/agent-hub.ts); [focused proof](../../vendor/oh-my-pi/packages/coding-agent/test/agent-hub-activate.test.ts). | Enter previews any row read-only (scrollable transcript); attachable targets expose `i` to focus input; non-attachable show a passive badge; no error path for running/external agents. Focused proof: 14 tests, 132 assertions; `check:types` green. | Extended-history handle rendering shows a friendly agent + workstream + relative-time title; the raw handle remains in `?` metadata. |
| HR-141 | Reap orphaned eval/MCP kernel processes: live measurement found 46 node_repl + 44 python3.13 + 44 uv child processes (~1.1GB) accumulated under cmux — kernels outliving their sessions. | IMPLEMENTED | P1/P2 | Live `cmux top` 2026-07-16 morning; matches HR-133 probe context (idle RSS pressure). | Kernels die with their owning session/turn (ownership tie + reaper), `omp doctor` flags orphans with owner attribution, and a safe --apply kills only parent-dead kernels. | Landed `a9b767363`: ownership markers + shutdown liveness fallback + exit-code-0 fix + doctor orphan reap. Live sweep found only 2 true orphans of 170 kernels — the rest have LIVE owner sessions; idle-TTL kernel release is the follow-up (pairs with HR-133). |
| HR-137 | `/restart` must reexec the CURRENTLY INSTALLED binary, not the process's resolved execPath: live sessions reexec their original binary blob, so promoted builds never reach them via /restart (both fleet canaries proved it; the installed symlink chain resolves correctly for fresh launches). | REQUESTED | P1 | Live dogfood 2026-07-16: canary 019f6699 on a729a336 kept a729a336 after /restart while `omp --version` at shell was 2d684fca; the pinned-668 canary behaved identically. Restart should re-resolve the launcher path or the release-registry blessed binary at reexec time. | /restart on a session launched from the omp symlink lands on the current blessed build; regression proves reexec path resolution. | Blocks the manual-restart upgrade path the fleet skip messages advertise. |
| HR-138 | Idle peers must stay addressable for rollouts: peer rows drop from `omp fleet status` after roughly 40 idle minutes, so idle-first rollouts lose their targets (two canaries went unreachable this way before any command was sent). | REQUESTED | P1/P2 | Live dogfood 2026-07-16: canary 019f6699 enrolled, idle ~40min, absent from default status and rollout target matching; one READY turn restored it. Either lengthen/refresh idle heartbeats, or have rollout target resolution consult the durable session index (not just live heartbeats) for known-idle sessions. | An idle session enrolled as canary remains selectable by `--canary` for at least several hours without synthetic turns. | Interacts with HR-133 paging: parked/paged sessions still need a wake path. |
| HR-136 | Revived subagents must keep their full toolset and their yield reports must reach the parent: a completed/parked agent woken via IRC currently loses all write-capable tools (only read-only REPL + yield remain) and its yield errors are swallowed — the parent sees silent parking. | IMPLEMENTED | P1 | This register (orchestrator dogfood, 2026-07-15: DurableFollowupFix revived parked lost write tools, repo writes EPERM, yield reports aborted unseen; cost 3 silent worker turns; HR122CoreRouting woken while idle DID retain tools — state-dependent). Related: `history://<id>` renders empty for parked agents with a populated transcript jsonl. | Root causes: parked descriptor froze `initialToolNames ?? []` (executor.ts), unreachable follow-up refresh guard + dropped result observer, history projection stripped `child_lifecycle` graph parents. Proof: test/task/park-revive-followup.test.ts (real park→IRC revive→write→yield, isolated bus) + history-protocol regressions; commit `dc0a754e2`, blessed `dc0a754e24ad`. | Directly throttles fleet-of-workers orchestration. |
| HR-128 | Evolving prompt-guide convention for subagents: a living doctrine file injected into every spawn context (versioned, editable without code changes) because behavioral rules cannot all be baked into subagent code — and agents observably ignore static rules. | REQUESTED | P1 | This register (Arthur, 2026-07-15: "they seem to ignore the rules"). | A canonical guide file (e.g. docs/fable/spawn-guide.md) auto-prepended to spawn contexts; changes take effect on next spawn; measure compliance via spot review. | Keep it short and testable — long guides get ignored too. |
| HR-163 | Make subprocess-isolated child completion, edit, and IRC behavior durable: a child yield must resolve its parent job even when teardown parks the child, edit must initialize its Settings guard, and child IRC must never fall through to an external-bus loopback. | IMPLEMENTED | P1 | Confirmed in the 2026-07-16 journals: child lifecycle `park` precedes each successful `yield` record (RegisterScribe #45-47, StatusLineRevert #172-174, PeerCmuxAttach #185-187, HubFocusSemantics #225-227); `spawn-worker-client.ts` previously waited for a terminal pipe result with no journal recovery; worker bootstrap used `Settings.isolated()` without initializing the global proxy; projected registry refs had `session:null` and IRC external fallback remained enabled. | [Subprocess reliability proof](../../vendor/oh-my-pi/packages/coding-agent/test/task/subprocess-worker-reliability.test.ts) (4 pass); adjacent spawn/IRC proof (42 pass); `bun run check:types` green. | Worker initializes Settings before the tool graph, disables external IRC in worker processes with a typed coordinator-IPC refusal for unavailable local peers, kills a worker after a terminal result, and recovers durable yields from the child journal when the pipe result is absent. |
| HR-165 | `/handoff` is broken: the flow loses/violates session ownership (Arthur, 2026-07-16: "/handoff broken, lost session ownership"). Root-cause and fix so handoff produces a working successor session without ownership errors or orphaned owner records. | IMPLEMENTED | P1 | Root cause (2026-07-17 overnight): `AgentSession.handoff()` switched `SessionManager` to the successor without transferring the ownership lease — predecessor claim + external owner row stayed live, successor had no lease, first successor mutation tripped the session-file/session-id guard (`SessionOwnershipLostError`); durable input queue and cached IRC peer identity stayed predecessor-bound. Fix: `handoffSessionOwnership` (`src/session/session-ownership.ts`) releases predecessor fully and acquires the successor exactly once; `handoff()` transfers ownership before the first successor write and re-adopts the durable queue for the successor epoch; `bus-external.ts` `handoffPeer` rekeys the owner row with no orphan record (commit `471087675`, blessed `16.0.1+fork.47108767543f`). | [Handoff ownership proof](../../vendor/oh-my-pi/packages/coding-agent/test/handoff-ownership.test.ts) — red at the transition seam before the fix, 1 pass/9 assertions after; [restart suite](../../vendor/oh-my-pi/packages/coding-agent/test/restart-session.test.ts) 43 pass; checkpoint-gate staged snapshot: typecheck clean, 44 pass/83 expects. | No `agent-session.ts` protected range touched; `/restart` release-before-reacquire semantics unchanged and remain the reference. |
| HR-166 | Autostamp generated handoffs with predecessor provenance: previous session id, journal path, session dir, binary version, timestamp, cwd, and predecessor handoff pointer (chain), so successors can find full context without archaeology. | IMPLEMENTED | P2 | Every generated handoff (manual `/handoff`, auto-compaction, RPC) is prefixed at the shared assembly seam in `AgentSession.handoff()` with a code-generated provenance block (predecessor session id, journal path, session dir, binary version, timestamp, cwd, walkable handoff chain); the LLM body stays verbatim below it. Successor journal carries the typed `handoff_provenance` record (`HandoffPredecessorProvenanceRecord`, schemaVersion 1, Effect-Schema decoded via `src/session/handoff-provenance.ts`); `findLatestHandoffProvenanceEntry` threads the chain (commit `6603041dc`, blessed `16.0.1+fork.6603041dc967`). | [Provenance proof](../../vendor/oh-my-pi/packages/coding-agent/test/agent-session-handoff.test.ts) — exact rendered-block assertion + walkable predecessor chain, 24 pass/92 assertions; HR-165 ownership suite stays green; checkpoint-gate staged snapshot: typecheck clean, 25 pass/101 expects. | Manual exemplar shape preserved (`docs/fable/handoffs/2026-07-17-hr164-overnight.md`); suite external-bus isolation fixed in `711dc5ecf`. |
| HR-167 | Overnight resource-reclamation orchestration: a mechanical reaper (5m) + Sol judgment warden (15m) so the machine never blows up while Arthur sleeps — orphan worktrees, leaked browsers, runaway RSS, disk, and stuck sudo prompts. | IMPLEMENTED | P1 | `scripts/night-reaper.ts` (browser-reaper apply, gate-worktree prune, stale `~/.omp/wt` clone reclamation with lsof guard, pending-sudo kill >2min, sudo unified-log evidence, RSS/disk ALERTs, bounded logs) + `night-reaper`/`night-warden` entries in `~/.omp/agent/automations.yml` + `com.omp.automations` LaunchAgent (KeepAlive) bootstrapped 2026-07-16. Incident evidence: disk hit 100% (6.3G free) same night — 50GB of dead isolated-spawn clones in `~/.omp/wt` (2 five-day-old clones incl. a broken non-git Terra fork) + 6 leaked checkpoint-gate worktrees + caches; reclaimed to 26G free. | Reaper cycle runs green under launchd (verified two manual cycles); warden findings in `local/night/warden-findings.md`; logs under `local/night/`. | Warden never sudos, never kills live-session processes; escalation = findings file + IRC to the working orchestrator. |
| HR-168 | Sudo/Touch-ID permission asks become an attributable, coordinated surface — an OMP builtin/skill instead of ad-hoc scripts — so overnight fleets neither spam Arthur's fingerprint nor block themselves. | REQUESTED | P2 | This register (Arthur, 2026-07-16: "figure what is asking for my sudo fingerprint so much … maybe add it as a tool/skill or builtin"). Interim shipped 2026-07-16: askpass attribution log (dotfiles `64b33fa` → `~/state/remote-sudo/invocations.log` with omp/cmux session env + parent chain), 12h fast sudo-sampler (`scripts/sudo-sampler.sh` → `local/night/sudo-sampler.log`), reaper sudo-evidence log. Prompt-source lead: sudo pairs every 300s in unified log; `com.local.browser-context-sync` (300s LaunchAgent) was failing on the full disk but contains no sudo — sampler will name the true caller by morning. | Builtin design: sessions request privileged actions through one queue with session attribution, batching, and a deny-by-default night mode; register row updated with the sampler's identification. | Do NOT modify sudoers/pam without Arthur's explicit approval; evidence first. |
| HR-169 | Store omp session journals in a compressed format — cold sessions compacted (zstd or SQLite+zstd), hot sessions untouched append-only jsonl — because "text is super compressible" and the sessions dir is 4.2G raw. | REQUESTED | P2 | This register (Arthur, 2026-07-16 night, triage). Evidence same night: three sampled journals compress to 15–24% gzip, 6–15% zstd-19 → ~4.2G → 600M–1G. A `sessions-archive-manifest.jsonl` + `history.db` already exist — design must extend the existing archive seam, not add a second convention. | Design pass: cold-session compaction with transparent decompression through the session-manager read path (`history://`, resume, conformance replay all keep working); hot journals stay plain jsonl (crash-durable line appends — HR-163/H1 recovery depends on them); measure decode-latency delta on a 20MB journal (HR-022 benchmark exists). | Queue after HR-164 H1 or as an independent storage slice; do NOT compress live journals — durability first. |

## Rollout / recovery

| ID | Exact intent | Status | Owner phase | Canonical detail / evidence | Acceptance / proof | Notes |
|---|---|---|---|---|---|---|
| HR-029 | Keep an N−1 installed runtime available during candidate rollout. | DECIDED | P2 | [Revisioned rollout](harness-runtime-contract.md#revisioned-runner-rollout-and-n1-recovery) | Installed-build inventory and recovery proof identify both revisions. | N−1 is recovery substrate, not a second writer. |
| HR-030 | Run a candidate/canary against copied or forked fixtures before promotion. | DECIDED | P2 | [Rollout state machine](harness-runtime-contract.md#rollout-state-machine) | Canary proof cannot write stable session or acquire its lease. | Readiness precedes promotion. |
| HR-031 | Make promotion an explicit revisioned operation. | DECIDED | P2 | [Rollout state machine](harness-runtime-contract.md#rollout-state-machine) | Promotion evidence records build, readiness, handoff, and lease epoch. | No implicit load-on-start promotion. |
| HR-032 | Make rollback explicit and preserve the single-writer boundary. | DECIDED | P2 | [Fork/resume/rollback](harness-runtime-contract.md#fork-resume-and-rollback-are-different-operations) | Rollback proof releases/acquires exactly once without dual writers. | Rollback is neither fork nor transcript rewind. |
| HR-033 | Provide a recovery doctor that explains build, ownership, state, and safe next action. | REQUESTED | P2 | [Observable flow](harness-runtime-contract.md#observable-snapshot-and-error-evidence-flow) | Doctor output is linked to a reproducible failed/recovered scenario. | Must use canonical IDs. |
| HR-034 | Test detach/recovery through a sibling runner process and an independently disposable client view. | REQUESTED | P2 | [Phase 2 gate](harness-runtime-contract.md#phase-2--runnerview-seam) | Independent-client proof survives view/process exit without relying on a terminal multiplexer. | In-process unit proof is insufficient; tmux is not an architecture dependency. |
| HR-035 | Record installed-build and version provenance, not merely repository HEAD. | IMPLEMENTED | P1 | [Failure evidence](harness-runtime-contract.md#failure-evidence-and-postmortems); executable SHA-256 + VERSION at runner start (`vendor/oh-my-pi/packages/coding-agent/src/main.ts#L113-L119`); `/version` separates binary digest, source commit, and blessed registry (`vendor/oh-my-pi/packages/coding-agent/src/slash-commands/version.ts#L74-L98`); ErrorInbox emission/decoding/card provenance (`vendor/oh-my-pi/packages/coding-agent/src/session/error-inbox-ledger.ts#L57-L130`, `vendor/oh-my-pi/packages/coding-agent/src/modes/utils/error-inbox.ts#L238-L246`, `vendor/oh-my-pi/packages/coding-agent/src/modes/components/error-selector.ts#L39-L68`); recovery bundle captures system versions and resolved config (`vendor/oh-my-pi/packages/coding-agent/src/debug/report-bundle.ts#L95-L107`). | Diagnostic/recovery evidence identifies the installed executable build/version and the captured configuration snapshot; focused provenance tests pass (`test/session/error-inbox-ledger.test.ts`, `test/modes/utils/error-inbox.test.ts`, `test/modes/components/error-selector.test.ts`; 35 pass) and `bun --cwd=packages/coding-agent run check:types` is green. | Build provenance is emission-time and survives ErrorInbox reconciliation; config is captured as a bounded recovery snapshot. |
| HR-071 | Finish every proven `WorkSlice` with a coherent Git `CodeCheckpoint` before starting the next slice. | DECIDED | P1–P3 | [Slice checkpoint contract](harness-runtime-contract.md#slice-checkpoint-contract) | Commit metadata links request IDs, exact SHA, lock/config/schema identity, focused proof commands, and artifacts; unrelated dirty files are excluded. | Git checkpoints code; runtime journals checkpoint live state. |
| HR-072 | Use additional cmux tabs in the same workspace as disposable test/client surfaces without creating a second live-session writer. | DECIDED | P1/P2 | [Slice checkpoint contract](harness-runtime-contract.md#slice-checkpoint-contract) | Phase 1 tabs use isolated profiles/session directories and copied fixtures; Phase 2 proves one controller plus multiple observers attached to one runner. | cmux hosts views/processes but provides neither state authority nor isolation by itself. |
| HR-108 | Make checkpoint gates validate the staged snapshot, including generated artifacts, rather than the dirty working tree. | IMPLEMENTED | P1–P3 | [Checkpoint gate](../../vendor/oh-my-pi/scripts/checkpoint-gate.sh); [gate proof](../../vendor/oh-my-pi/scripts/checkpoint-gate.test.sh) (commits `7d644d81`, `80decda4`) | Staged-only typecheck/focused commands run in an index-materialized worktree, package `generate` runs first, and the caller tree is restored byte-identically. | First dogfood caught missing gitignored `docs-index.generated.ts` and `tool-views.generated.js`; native-addon prestep remains an open low-friction item. |
| HR-115 | Provide a native fleet-rollout primitive: a harness-owning "mimic orchestrator" can pause peer orchestrators (quiesce subagent spawning, checkpoint in-flight work), roll them onto a new blessed runner/view version, auto-resume their work, and roll back on health failure — k3s-style semantics (immutable digest artifacts, canary order, health gates, drain) adapted to stateful local sessions; includes first-class peer state/error/version inspection. | REQUESTED | P2/P3 | This register (Arthur, 2026-07-15 voice); design doc commissioned: `docs/fable/fleet-rollout-design.md`. Builds on `scripts/omp-promote.ts` receipts/rollback retention, `omp rollout` working-session skips, same-PID `/restart` (HR-104), session-control epoch fencing (HR-028), fleet `:errors` (HR-113), workstream metadata (HR-114). | Design doc accepted by Arthur, then MVP slices land individually: pause/resume protocol with receipts, auto-resume after restart onto target digest, runner/view compat contract, `omp fleet` command surface, automatic rollback triggers. | Runner and view version independently; journal schema is the compat anchor. Promotion success ≠ rollout success stays doctrine. |

## Persistence / diagnostics

| ID | Exact intent | Status | Owner phase | Canonical detail / evidence | Acceptance / proof | Notes |
|---|---|---|---|---|---|---|
| HR-036 | Persist immutable error occurrences and durable evidence. | IMPLEMENTED-PARTIAL | P1 | [Refusal observability workflow](../state/refusal-observability.md); [append-only refusal corpus](../../vendor/oh-my-pi/packages/coding-agent/src/session/refusal-corpus.ts) (commit `2f54cb1d`) | Refusal cases persist as redacted JSONL records with fingerprints, bounded excerpts, context basenames, correlation fields, verdict marks, and replay history; `omp refusals` can list/show/stats/mark/replay them. Full all-error correlation and control-plane reingest remain open. | Acknowledgement is a projection, never deletion; refusal replay is no-tools only and tool-required cases are rejected. |
| HR-037 | Correlate build/config/runtime, route, session/turn/agent, input/attempt, and artifacts. | IMPLEMENTED-PARTIAL | P1/P3 | [Operational event schema](../../packages/control-plane/src/schema.ts); [operational ingest](../../packages/control-plane/src/ingest.ts); [query projection](../../packages/control-plane/src/operational-query.ts) (commits `2c59230e`, `da35f954`) | Effect-Schema-decoded runner and diagnostic events preserve source, sequence, command/correlation/causation, session revision, agent/task, input/attempt, route/quota, tool, diagnostic/regression, and artifact identifiers; the isolated checkpoint typechecked and passed 21 focused tests (273 assertions). | Operational correlation is queryable without ad hoc log matching; full all-provider artifact coverage remains open. |
| HR-038 | Store/query durable history through SQLite without making it live command authority. | IMPLEMENTED | P3 | [Operational ledger](../../packages/control-plane/src/ledger.ts); [read-only operational query](../../packages/control-plane/src/operational-query.ts) (commits `2c59230e`, `da35f954`) | Idempotent event ingest, conflict detection, source-gap tracking, diagnostic folding, and lag/staleness drill-down passed the isolated typecheck and 21 focused tests. | SQLite remains a projection/query substrate; journal/session authorities are unchanged. |
| HR-039 | Preserve exact reproduction inputs and immutable request/response/tool/log artifacts. | REQUESTED | P1 | [Failure evidence](harness-runtime-contract.md#failure-evidence-and-postmortems) | A linked reproduction reruns or precisely explains why it cannot. | Redaction/auth boundaries still apply. |
| HR-040 | Attach the executable build revision to every diagnostic and reproduction. | IMPLEMENTED | P1 | [Failure evidence](harness-runtime-contract.md#failure-evidence-and-postmortems); ErrorInbox records stamp owning build version/digest before append and preserve them through decode/dedupe (`vendor/oh-my-pi/packages/coding-agent/src/session/error-inbox-ledger.ts#L57-L130`, `vendor/oh-my-pi/packages/coding-agent/src/modes/utils/error-inbox.ts#L238-L246`, `#L401-L463`); error cards render both facets (`vendor/oh-my-pi/packages/coding-agent/src/modes/components/error-selector.ts#L39-L68`); spawn/recovery receipts carry both facets (`vendor/oh-my-pi/packages/coding-agent/src/task/spawn-record.ts#L19-L21`, `vendor/oh-my-pi/packages/coding-agent/src/runner/canary-readiness-worker.ts#L16-L31`, `#L114-L121`). | Proof distinguishes source commit from installed binary SHA/version via `/version` (`vendor/oh-my-pi/packages/coding-agent/src/slash-commands/version.ts#L74-L98`; `test/slash-commands/version.test.ts#L12-L51`), and emission/card regression tests prove both fields (`test/modes/utils/error-inbox.test.ts#L32-L47`, `test/modes/components/error-selector.test.ts#L39-L57`; 35 focused tests pass). | Explicit facets remain authoritative; legacy records decode with missing provenance rather than fabricating it. |
| HR-041 | Use files and SQLite as the inspectable read substrate. | DECIDED | P2 | [Linux-like substrate](harness-runtime-contract.md#linux-like-substrate-and-mutation-boundary) | Operator can inspect state with ordinary readers while invariants remain intact. | No universal store rewrite. |
| HR-042 | Keep mutations behind a tiny typed boundary rather than permitting arbitrary writes. | DECIDED | P2 | [Linux-like substrate](harness-runtime-contract.md#linux-like-substrate-and-mutation-boundary) | Mutation contract rejects stale/unauthorized revisions and emits evidence. | Reads may be broad; writes stay narrow. |
| HR-043 | Provide runtime memory snapshots and diagnostic dumps. | REQUESTED | P2 | [Observable flow](harness-runtime-contract.md#observable-snapshot-and-error-evidence-flow) | Snapshot/dump reconstructs runner, queue, routes, children, and revisions at capture time. | Must be bounded and redact secrets. |
| HR-044 | Maintain a durable regression ledger linked to occurrences and proof. | IMPLEMENTED-PARTIAL | P3 | [Harness friction log](../state/harness-friction.md); [diagnostic occurrence/projection fold](../../packages/control-plane/src/ledger.ts) (commits `2c59230e`, `da35f954`) | Diagnostic occurrences fold idempotently into durable projections with regression and artifact links; the isolated operational/evidence suite passed 21 tests (273 assertions). | Canonical recurrence IDs are implemented for ingested diagnostics; automatic coverage of every harness failure source remains open. |

## Orchestration / workflow

| ID | Exact intent | Status | Owner phase | Canonical detail / evidence | Acceptance / proof | Notes |
|---|---|---|---|---|---|---|
| HR-045 | Execute stabilization phases sequentially, not as feature fanout. | DECIDED | All | [Sequential phases](harness-runtime-contract.md#three-sequential-stabilization-phases) | Each full phase gate is linked before the next begins. | Parallel work inside one gate does not waive ordering. |
| HR-046 | Put newly arriving asks into a task inbox/register instead of immediately sidetracking active work. | DECIDED | All | This register, [intake rule](#register-rules) | New ask receives an ID/status before implementation begins. | Urgency changes priority explicitly, not silently. |
| HR-047 | Save plans and prompts durably for later failure analysis. | IMPLEMENTED-PARTIAL | P1 | [Durable spawn prompt record](../../vendor/oh-my-pi/packages/coding-agent/src/task/spawn-record.ts#L5-L21); [journal-backed plan artifact](../../vendor/oh-my-pi/packages/coding-agent/src/session/session-manager.ts#L1827-L1858); [plan:// protocol](../../vendor/oh-my-pi/packages/coding-agent/src/internal-urls/plan-protocol.ts#L38-L77) | Approved plans are copied to a stable session artifact, journaled, and recoverable as `plan://latest` after reopen; spawn records already preserve full prompts and route/build provenance. Focused proof: [plan protocol restart tests](../../vendor/oh-my-pi/packages/coding-agent/test/internal-urls/plan-protocol.test.ts) (2 pass, 8 assertions); [spawn-record proof](../../vendor/oh-my-pi/packages/coding-agent/test/task/spawn-record.test.ts#L70-L123). | Diagnostic/ErrorInbox records still do not carry a direct plan/prompt artifact revision link, so the status remains partial. |
| HR-048 | Make compaction transitions visible to operators and postmortems. | IMPLEMENTED-PARTIAL | P1 | [Durable `queueBoundarySequence` process proof](../../vendor/oh-my-pi/packages/coding-agent/test/session/durable-input-queue-agent-session-process.test.ts) (commit `de3c3991`) | Compaction boundary is durable. | Full postmortem context manifest and affected-ID timeline remain. |
| HR-049 | Require user-facing help for consequential operations. | REQUESTED | P2 | [Runner commands](harness-runtime-contract.md#runner-view-boundary) | Help documents fork/resume/stop/promote/rollback effects before invocation. | Avoid overloaded verbs. |
| HR-050 | Put explicit approval gates around destructive or consequential control-plane actions. | REQUESTED | P2 | [Rollout state machine](harness-runtime-contract.md#rollout-state-machine) | Audit proof links approval identity to accepted command and result. | No gate is required to edit this documentation register. |
| HR-063 | Make auto-compaction policy, thresholds, actual trigger reason, and retained/dropped context visible. | REQUESTED | P1 | [Failure evidence](harness-runtime-contract.md#failure-evidence-and-postmortems) | Timeline and UI explain why compaction happened at the observed token count and link the before/after context manifest. | Captures the reported apparent 600k-versus-1m inconsistency. |
| HR-064 | Archive delegated work packets/plans, chosen model/persona, outcome, and failure mode for later orchestration analysis. | REQUESTED | P3 | [Failure evidence](harness-runtime-contract.md#failure-evidence-and-postmortems) | A query can compare plan/packet revision, route, proof outcome, interruption, and reviewer verdict without replaying chat. | Supports learning which models need explicit plans versus broad goals. |

### HR-047 gap analysis (2026-07-16)

| Clause | Satisfied by | Implemented now | Still open + why |
|---|---|---|---|
| Spawn prompts survive restart/compaction | `src/task/spawn-record.ts:5-21`; `test/task/spawn-record.test.ts:70-123` | No change; HR-124 already stores assignment, context, fullPrompt, route, and build provenance in `session_init`. | Diagnostic occurrences do not yet link to the spawn-record revision. |
| Plan drafts remain available during plan mode | `src/internal-urls/local-protocol.ts:125-169`; `src/modes/interactive-mode.ts:2090-2108` | No change; existing `local://` draft files are session-scoped and restart-stable. | Draft edits are not separately journaled until approval. |
| Approved plan survives restart with an exact revision reference | — | `src/session/session-manager.ts:1827-1858` writes `local://plans/<entry>.md` beside the session and appends a typed custom journal pointer; `src/plan-mode/plan-artifact.ts:19-55` validates it. | None for restart/reopen of the approved artifact. |
| Latest approved plan is recoverable through an internal URL | — | `src/internal-urls/plan-protocol.ts:38-77` resolves `plan://<entry>` and `plan://latest`; `src/session/agent-session.ts:6923-6932` restores the latest local reference after workflow replay. | Diagnostic/ErrorInbox projection still needs to consume the journal pointer for end-to-end failure-analysis links. |

## Architecture / docs

| ID | Exact intent | Status | Owner phase | Canonical detail / evidence | Acceptance / proof | Notes |
|---|---|---|---|---|---|---|
| HR-051 | Assign one canonical source per concern rather than one database for everything. | DECIDED | P1 | [Blessed owners](harness-runtime-contract.md#one-blessed-owner-per-concern) | Architecture/proof names owner and explicit non-owner for each concern. | “One source of truth” is concern-scoped. |
| HR-052 | Maintain diagrams and an interactive viewer that explain the same canonical system. | IMPLEMENTED-PARTIAL | P3 | [Diagram taxonomy](harness-runtime-contract.md#diagram-taxonomy); [interactive viewer](../../playground/harness-runtime-map.html); [temporary QA artifacts](../../local/harness-runtime-map-proof/) | Viewer is checked against current contract and links canonical docs. | Viewer is explanatory, not normative; temporary-artifact QA is not a runtime proof. |
| HR-053 | Use the right diagram type for topology, sequence, state, ownership, and evidence flow. | DECIDED | P3 | [Diagram taxonomy](harness-runtime-contract.md#diagram-taxonomy) | Review confirms each diagram answers its declared question without mixing semantics. | Avoid one overloaded “architecture diagram.” |
| HR-054 | Treat OpenAI Codex app-server as design inspiration, not an authority or drop-in contract. | REQUESTED | P2 | [Runner/view boundary](harness-runtime-contract.md#runner-view-boundary) | Any adopted pattern is mapped explicitly to local identities, security, and persistence. | Research provenance must be linked before adoption. |
| HR-055 | Make the new stabilized `SessionRunner` Effect-native: services/layers, scoped resources, fibers, streams/queues, schedules, typed errors, and deterministic tests throughout the runner core—not merely at boundaries. | IMPLEMENTED-PARTIAL | P1/P2 | [Effect core and live composition](../../vendor/oh-my-pi/packages/coding-agent/src/runner/session-runner.ts); [deterministic/live proof](../../vendor/oh-my-pi/packages/coding-agent/test/runner/session-runner.test.ts) (commits `34cbaa5d`, `4a0bdd9b`); [atomic queue proof](../../vendor/oh-my-pi/packages/coding-agent/test/session/durable-input-queue.test.ts) (commit `b286c31c`); [AgentSession queue process proof](../../vendor/oh-my-pi/packages/coding-agent/test/session/durable-input-queue-agent-session-process.test.ts) and [transcript subscription proof](../../vendor/oh-my-pi/packages/coding-agent/test/collab/session-replication.test.ts) (commit `a1d32c21`); [SDK construction/process proof](../../vendor/oh-my-pi/packages/coding-agent/test/runner/session-runner-process.test.ts) (commit `eb3cbf92`); [runner mutation proof](../../vendor/oh-my-pi/packages/coding-agent/test/runner/session-runner.test.ts), [runner mutation process proof](../../vendor/oh-my-pi/packages/coding-agent/test/runner/session-runner-process.test.ts), [queue mutation proof](../../vendor/oh-my-pi/packages/coding-agent/test/session/durable-input-queue.test.ts), and [queue mutation process proof](../../vendor/oh-my-pi/packages/coding-agent/test/session/durable-input-queue-process.test.ts) (commit `5c4832ee`); terminal prerequisite chain: disposable terminal projection/controller (`6a343495`, 8 tests/60 assertions), SessionManager durable state command journal (`3035f2e4`, 17/102), thinking control (`22b4d37b`, 41/180), model control (`9a69e20e`, 44/212), exact prompt interrupt (`186f2b8b` + `c27b8d3f`, 17/127), manual compaction (`1d57d302`, 20/148), and exact compaction cancellation (`01ef51ca`, 20/169). | Checkpoints prove the canonical Effect core, queue-v2 atomic command/input/revision acceptance, one injected queue with one AgentSession provider attempt, SessionManager transcript subscriptions, live authority composition, and SDK construction without exposing a writable AgentSession. Earlier checkpoints covered 40 queue tests, 13 live-seam tests, and 3 runner/SDK tests with 23 assertions; each checkpoint typechecked. Commit `5c4832ee` proves submit/edit/cancel through queue command idempotency, runner/item compare-and-swap fences, and kind-specific events; its focused mutation proof is 50 tests with 204 assertions, and typecheck is clean. The terminal prerequisite chain separates queue/input `inputRevision`, durable SessionManager state/command-journal `sessionRevision`, and live prompt/compaction operation-generation fencing; every listed checkpoint typechecked cleanly, with Sol review PASS after fixes. | Phase 2 remains in progress: `InteractiveMode`/main is not cut over; plan/goal/tool/session-transition controls, collab cutover, and canary/rollout remain. The process proof establishes durable acceptance/replay and explicitly does not claim provider completion. Immutable stable `omp/16.0.1+fork.eb3cbf922ebc` predates the entire terminal prerequisite chain and remains the stale stable binary; all listed checkpoints, including `01ef51ca`, are unpromoted. Later verified checkpoints add goal transitions (`aba26d09`), tool config (`7666b5ac`), todo/SSH (`5238be4e`), read queries (`ecf0c373`), local operations (`6c692efe`), ephemeral turns (`2b206e71`), durable custom messages (`69c35f1d`), owned internal-custom queue cutover (`11df0b3e`), the false-pending legacy-mirror UI fix (`fe4ce19c`; journal proves one delivery), Agent Hub running-only dot focus (`25581ef8`), main Alt+V raw semantic/rich toggle (`39980ebd`), disposable terminal host (`b084cd31`), and opt-in reloadable compact TUI (`fa6c8b11`; real PTY A→B in one PID/session, exit 0, exact termios). Commit trailers carry the focused proof/typecheck evidence. The apparent duplicate was a false-pending legacy-mirror projection, not a second delivery. The reloadable TUI is opt-in via `--tui-bundle-manifest`; default legacy `InteractiveMode` remains selected, so these checkpoints do not claim full terminal conversion or promotion. |
| HR-056 | Migrate toward the Effect-native runner slice-by-slice rather than converting the current tangled runtime in one big bang. | DECIDED | P1/P2 | [Effect-native runner](harness-runtime-contract.md#effect-v4-effect-native-runner-target) | Every migration slice has one writer, parity proof, Git checkpoint, and clean cutover with no parallel semantic implementation left behind. | Broad eventual runner-core adoption is intended; queue-v2/JSONL/SQLite are not replaced by in-memory Effect primitives. |
| HR-065 | Replace the control-panel-style explainer with DeepWiki-like, code-grounded diagrams for each subsystem and procedure. | REQUESTED | P1/P3 | [Diagram taxonomy](harness-runtime-contract.md#diagram-taxonomy); [Ghostty DeepWiki example](https://deepwiki.com/ghostty-org/ghostty/2-core-architecture); [OMP DeepWiki example](https://deepwiki.com/can1357/oh-my-pi) | Queue, routing, spawn, fork, compaction, restart, and rollout pages each pair the right diagram type with exact current source files/symbols and CURRENT versus TARGET behavior. | The existing dark interactive map is too abstract and visually overdesigned for this use. |
| HR-066 | Make explanatory architecture artifacts neutral/light by default unless a dark theme is explicitly requested. | REQUESTED | P3 | [Interactive viewer](../../playground/harness-runtime-map.html) | Replacement diagrams remain readable like technical documentation and do not imply a game/control-plane surface. | Current dark mode was an arbitrary designer choice, not a requirement. |
| HR-067 | Record and expose whether each subagent model was explicit, role-resolved, inherited from its parent, or automatically rerouted. | IMPLEMENTED | P1 | [Agent Hub source/final-route display proof](../../vendor/oh-my-pi/packages/coding-agent/test/agent-hub-route-provenance.test.ts) (commit `8c66760f`) | Spawn record and Hub/model view show resolution source, parent route if inherited, and final concrete route. | Prevents “why was this reviewer Sol?” ambiguity. |
| HR-068 | Add retrieval-first/RLM-style delegation where a context worker searches, slices, summarizes, and recursively queries an immutable main-thread/session manifest instead of receiving a full fork. | DECIDED | Later | [Bounded RLM adaptation](harness-runtime-contract.md#recursive-context-delegation-bounded-rlm-adaptation) | Comparison measures context cost, omission/error rate, citation coverage, provenance, and task outcome against explicit packets and full forks under fixed recursion/budget limits. | Explicit packet stays default; no new persona or provider binding; session material is untrusted data and the worker is read-only. |
| HR-069 | Diagnose unexpected activation of loop mode and make its owner, trigger, and current state visible. | REQUESTED | P1 | [Regression log](../state/harness-friction.md) | Reproduction identifies the exact loop feature, activating event/config source, and a visible disable path. | Needs the exact UI/screenshot/session event because “loop” is currently ambiguous. |
| HR-070 | Keep tmux, cmux, agent-mux, and the target runner/view architecture terminologically distinct. | DECIDED | P1/P2 | [Vocabulary](harness-runtime-contract.md#vocabulary) | Docs and UI say runner, client/view, and workspace tab; they name tmux or cmux only when referring to that actual external program. | Voice transcription frequently confuses “tmux” and “cmux”; neither owns OMP runtime state. |
| HR-083 | Persist typed workstream identity in each session so root-launched sessions remain filterable, while keeping stable stream charters and concurrent per-session `/goal` state distinct but linked. | IMPLEMENTED | P2 | [Session metadata proof](../../vendor/oh-my-pi/packages/coding-agent/test/session-manager/session-workstream.test.ts); [startup/command proof](../../vendor/oh-my-pi/packages/coding-agent/test/workstream-startup.test.ts); [child inheritance proof](../../vendor/oh-my-pi/packages/coding-agent/test/task/workstream-inheritance.test.ts) (commits `243a8104`, `ce45e90d`) | Header is sole workstream authority; CLI/env/cwd classification, live reclassification, selector filtering, goal charter projection, resume, and child snapshot inheritance are proven. | `streams/<id>/GOAL.md` is the stable charter; `/goal` remains session-local JSONL execution/accounting state. Missing legacy metadata is unclassified, distinct from deliberate adhoc. |
| HR-084 | Reduce beachball latency in long sessions before advancing the harness experience redesign. | IMPLEMENTED | P1/P2 | [Component-scoped render proof](../../vendor/oh-my-pi/packages/tui/test/tui.test.ts); [lifecycle retention proof](../../vendor/oh-my-pi/packages/coding-agent/test/bench/lifecycle-memory.test.ts); [Agent Hub projection proof](../../vendor/oh-my-pi/packages/coding-agent/test/agent-hub-projection.test.ts); [single-decode resume benchmark](../../vendor/oh-my-pi/packages/coding-agent/test/session-manager/file-operations.test.ts) (commits `911d2a51`, `bb8d214b`, `51c96aba`, `e462ecc6`) | 10k-child keystroke proof records zero history-child renders; parked retention falls 35.2x; event→projection p95 is 14.3µs at N=338; a 19.8MB/10,333-entry resume decodes once and cuts direct-parser median 75.20→39.35ms. | Priority 1 complete; focused gates passed and typechecks are clean. |
| HR-085 | Improve message delivery reliability across IRC revive and wake paths. | IMPLEMENTED | P1 | [IRC revive/delivery proof](../../vendor/oh-my-pi/packages/coding-agent/test/tools/irc.test.ts) (commits `27f00c3e`, `4b59fef1`) | Reserve-before-revive preserves exactly one message across failed handoff; per-message telemetry exposes queued/delivered/read/failed state. | Priority 2 complete; focused IRC gates passed and typecheck is clean. |
| HR-086 | Deliver a sectioned Agent Hub. | IMPLEMENTED | P2 | [Agent Hub projection proof](../../vendor/oh-my-pi/packages/coding-agent/test/agent-hub-projection.test.ts); [tree/grammar proof](../../vendor/oh-my-pi/packages/coding-agent/test/agent-hub-tree.test.ts); [streaming proof](../../vendor/oh-my-pi/packages/coding-agent/test/agent-hub-token-rate.test.ts); [selected-state/rollout proof](../../vendor/oh-my-pi/packages/coding-agent/test/agent-hub-selected-state.test.ts) | Running, attention, Recent-completed, and collapsed Parked sections preserve delivery; selected-child streaming, rollout state, durable model badges, strict read-only preview navigation (`j`/`k`, `J`/`K`, `u`/`d`, `Ctrl-U`/`Ctrl-D`, `PgUp`/`PgDn`, `g`/`G`, `/`, `?`, `v`, `za`, sibling `[ ]`), and `Enter` attachment to the full TUI are projection-only; the preview has no `i`, message input, `Ctrl-Enter`, or follow-up. | Focused TUI suites passed within the consolidated 400-pass/1-skip gate; non-cancelled child failures aggregate in `ErrorInbox`, and output recovery retains a `history://` pointer.
| HR-087 | Build a minimal HTML workstream page. | IMPLEMENTED | P3 | [Workstream page](../../vendor/oh-my-pi/packages/collab-web/src/components/workstream/WorkstreamPage.tsx); [live roster proof](../../local/omp-hub-proof/01-workstream-roster-live.png) (commits `334a2ab1`, `01f34d6c`) | The page exposes the active workstream, delivery/model/revision panels, and live roster without becoming a second authority. | Priority 4 complete; collab-web build/typecheck/client gate and live 01–08 proof passed. |
| HR-088 | Prove hot-swap/view-reload behavior from the web. | IMPLEMENTED | P2 | [Live model proof](../../local/omp-hub-proof/06-model-requested.png); [view-reload runbook](../../local/omp-hub-proof/reload-runbook.json) (commits `23da13b9`, `01f34d6c`, `45062028`) | Web proof demonstrates journaled hot-swap with replay idempotency and view reload preserving session identity, live state, and interactive input. | Priority 5 complete; live browser and reload proof passed. |
| HR-089 | Port the rich terminal/TUI to the runner/view boundary. | IMPLEMENTED | P2 | [Full reloadable rich host](../../vendor/oh-my-pi/packages/coding-agent/src/modes/disposable-interactive-view.ts); [command/wrap/rich proof](../../vendor/oh-my-pi/packages/coding-agent/test/command-mode.test.ts) | Default rich TUI is runner-backed and reloadable; IRC communication/tool-result bodies honor wrap/rich while single-line metadata stays bounded. Consolidated focused gate: 400 pass/1 skip and coding-agent typecheck passed after the final fix. | Compact mode remains non-default. |
| HR-090 | Cut over the default terminal to the runner/view authority. | IMPLEMENTED | P2 | [Default launch composition](../../vendor/oh-my-pi/packages/coding-agent/src/main.ts); [session control](../../vendor/oh-my-pi/packages/coding-agent/src/session/session-control.ts); [cancellation proof](../../vendor/oh-my-pi/packages/coding-agent/test/input-controller-keybindings.test.ts) | The full rich reloadable host is the default; Ctrl-Q directly cancels active work (focused children return immediately) and Ctrl-Enter is the follow-up queue action. Consolidated focused gate: 400 pass/1 skip; coding-agent typecheck passed after the final fix. | View replacement preserves runner/session authority. |
| HR-091 | Commit the verified Phase3A control-plane projection after ownership resolution. | IMPLEMENTED | P3 | [Operational ingest](../../packages/control-plane/src/ingest.ts); [operational query](../../packages/control-plane/src/operational-query.ts); [focused proof](../../packages/control-plane/test/operational-ingest.test.ts) (commits `2c59230e`, `da35f954`) | A selective patch excluded relay v9, papercut/refusal v11, server/daemon, and release-only working-tree hunks; the committed staged snapshot typechecked and passed 21 focused tests (273 assertions). | Ownership conflict resolved by staging only the coherent operational projection; journal-ledger authority and read-only projections remain explicit. |
| HR-097 | Make exported HTML session review incremental, branch-tree aware, and vim-navigable. | IMPLEMENTED | P3 | [HTML viewer tree proof](../../vendor/oh-my-pi/packages/coding-agent/test/export-subsessions.test.ts) (commit `01fef3f3`) | The 17MB proof measured FCP 48ms and DCL 875ms; branch-tree and vim controls work without the fatal prior viewer path. | Export remains a read-only review surface. |
| HR-098 | Give sessions opt-in ambient names while preserving explicit names. | IMPLEMENTED | P1 | [Ambient renamer proof](../../vendor/oh-my-pi/packages/coding-agent/test/ambient-agent-renamer.test.ts) (commit `569d23ab`) | Opt-in naming updates only auto-provenance display names and leaves explicit names unchanged. | No ambient rename is enabled by default. |
| HR-099 | Capture seven-day tool usage analytics to guide chooser/consolidation decisions. | IMPLEMENTED | Later | [Tool-usage analytics](tool-usage-analytics.md) (commit `23b9b255`) | The frozen JSONL baseline reports tool volume, explicit errors, result-token weight, and consolidation candidates. | Observational evidence, not runtime authority. |
| HR-106 | Use one shared journal-projection substrate for JSONL consumers instead of parallel parser copies. | IMPLEMENTED | P1/P3 | [Journal projection](../../vendor/oh-my-pi/packages/coding-agent/src/journal/projection.ts); [projection proof](../../vendor/oh-my-pi/packages/coding-agent/test/journal-projection.test.ts) (commit `1b6c8124`) | Typed entries and bounded-read helpers serve sibling tail-follow, HTML export, and Agent Hub preview without changing their render adapters. | Three parser copies deleted; TUI live-pipeline migration remains separate. |
| HR-107 | Resolve AgentRef lineage facets through one canonical resolver for jobs and hot-swap targets. | IMPLEMENTED | P1/P2 | [AgentRef proof](../../vendor/oh-my-pi/packages/coding-agent/test/agent-ref.test.ts); [hot-swap proof](../../vendor/oh-my-pi/packages/coding-agent/test/task/hotswap.test.ts); [poll displacement proof](../../vendor/oh-my-pi/packages/coding-agent/test/job-poll-displacement.test.ts) (commit `34882b2e`) | Exact, descendant, and job-ownership resolution preserve authorization and attribution across id/job/session/file/external facets. | One resolver replaces parallel identity walks; 39 focused tests passed. |
| HR-113 | Let an orchestrator see other Main sessions' ErrorInboxes (fleet-wide `:errors`). | IMPLEMENTED | P3 | [Fleet command](../../vendor/oh-my-pi/packages/coding-agent/src/commands/fleet.ts#L133-L155); [fleet error projection](../../vendor/oh-my-pi/packages/coding-agent/src/cli/fleet-cli.ts#L316-L406); [typed ErrorInbox schema](../../vendor/oh-my-pi/packages/coding-agent/src/session/error-inbox-ledger.ts#L24-L68); [fleet projection tests](../../vendor/oh-my-pi/packages/coding-agent/test/fleet-cli.test.ts#L69-L149); [read-only source test](../../vendor/oh-my-pi/packages/coding-agent/test/fleet-cli.test.ts#L206-L220) (5 pass, 28 assertions; `omp fleet errors` live run 2026-07-15 returned a populated `ERRORS` table across local sessions) | `collectFleetErrors` decodes typed `ui_error` v2 records from every local session journal, retains the latest record per event ID, and sorts rows by session, cause, then time; it reads `FleetIncidentStore` with `{ readonly: true }` and the test proves journals/control DB are byte-identical after projection. | Read-only fleet visibility is implemented; the live command also emitted an `INCIDENTS` section (empty in this run). |
| HR-114 | Tag sessions/agents with their workstream in durable metadata; propagate to spawned-agent metadata, git commit trailers (filter commits by stream), and `/resume` filtering. | REQUESTED | P1/P3 | This register (Arthur, 2026-07-14). The `goal` tool already classifies a session by `workstream` slug — build on that, do not invent a second classification. | Spawn records and session headers carry the workstream; commits made from a classified session gain a `Workstream:` trailer; `/resume` can filter by workstream. | Later: stream-filtered dashboards. |

## Tools / learning / communication

| ID | Exact intent | Status | Owner phase | Canonical detail / evidence | Acceptance / proof | Notes |
|---|---|---|---|---|---|---|
| HR-075 | Build a contextual learning tool that proposes atomic Anki cards for durable concepts encountered during real work and links each card back to the source/session/diagram that motivated it. | IMPLEMENTED-PARTIAL | Later | [Human harness side quest](../state/side-quests.md); `packages/primer-daemon` card ledger | Candidate → review → export/review loop preserves provenance, surrounding explanation, and “why this matters here”; it avoids implementation trivia. | Primer card infrastructure exists; general system-design capture and Anki export remain unfinished. |
| HR-076 | Maintain a searchable local source library with canonical raw/cleaned versions, metadata, provenance, and cross-session discovery for referenced articles/posts/books. | IMPLEMENTED-PARTIAL | Later | [System-design source manifest](../research/system-design/manifest.md); `skill://source-archive`; `packages/stema` | A future session resolves a remembered author/topic to canonical URL, cleaned local source, raw source/hash, annotations, and derived cards without web search. | Stema now provides the durable book catalog, acquisition provenance, derivatives, queue, and Primer publication; article and Twitter adapters remain outside the unified corpus. |
| HR-077 | Give agents a lightweight nitpick/adversarial-friend tool that records actionable concerns without diverting the active task. | IMPLEMENTED-PARTIAL | Later | `skill://rubber-duck-adversarial`; `docs/review-agents/rubber-duck-adversarial.md` | Agent can file a concern with source, severity, proposed next slice, and disposition into a searchable ledger; active work continues unless severity blocks it. | Skill exists; canonical ledger/tool registration is missing. |
| HR-078 | Preserve Discord as the active human/agent bridge and keep Slack as a swappable historical transport, not duplicate prompt-visible tool clutter. | IMPLEMENTED-PARTIAL | Later | `TASKS.md` T-2026-06-25-001/002; [bridge side quest](../state/side-quests.md) | One small transport-neutral escalation contract routes ordinary, needs-human, and urgent messages; active transport has live proof and redaction. | Discord is active; Slack implementations are historical/unregistered. |
| HR-079 | Provide a distinct human-interrupt/escalation bridge for approvals and genuinely urgent questions. | REQUESTED | Later | [bridge side quest](../state/side-quests.md) | Policy proves only approval/urgent events interrupt Arthur; ordinary agent traffic remains queryable but silent. | Transport may be Discord, Slack, or another adapter. |
| HR-080 | Add Jepsen/DST-style fault injection and history checking for session commands, ownership, queue recovery, and rollout rather than relying only on happy-path tests. | IMPLEMENTED-PARTIAL | P1/P2 | [DST side quest](../state/side-quests.md); `packages/dst-mini` | Generated concurrent histories plus crash/network/lease faults are checked against the declared per-session model and emit minimal counterexamples. | Scaled-down DST prototype/rules exist; runner integration and model checker remain. |
| HR-081 | Track storage pressure and compare text files, SQLite, content-addressed compressed blobs, and indexes by authority, queryability, portability, and measured space—not intuition. | REQUESTED | Later | [one-big-SQLite side quest](../state/side-quests.md) | Inventory reports bytes by source/derived/cache/artifact class and a migration benchmark proves savings without duplicating authority. | SQLite pages are not automatically compressed; grep convenience should be provided by query/index tools. |
| HR-082 | Synchronize `vendor/effect-smol` and its generated AI guidance to the exact Effect beta pinned for the runner before Effect-native implementation begins. | IMPLEMENTED | P2 prerequisite | [Effect-native runner](harness-runtime-contract.md#effect-v4-effect-native-runner-target); `vendor/effect-smol` `a445fe52682f023e7e74d2702e98ff3c112d1b94`; `vendor/oh-my-pi/packages/coding-agent/test/effect-v4-api-probe.test.ts` | Vendor commit/package versions, package-owned dependency, lockfile, AI docs, and migration guidance identify beta.92; focused probe compiles and proves tagged failures, scoped cleanup/interruption, bounded queue backpressure, and TestClock scheduling. | `effect` is pinned exactly to `4.0.0-beta.92` in coding-agent; proof passed 2026-07-11. |

## Experiments / deferred

| ID | Exact intent | Status | Owner phase | Canonical detail / evidence | Acceptance / proof | Notes |
|---|---|---|---|---|---|---|
| HR-057 | Evaluate an Electron/macOS control-plane surface as a possible disposable view. | DEFERRED | Later | [Runner/view boundary](harness-runtime-contract.md#runner-view-boundary) | Separate proposal proves it consumes the canonical protocol without owning runtime. | Not needed for stabilization. |
| HR-058 | Evaluate Elixir/OTP supervision as a runtime comparison. | DEFERRED | Later | [Deferred](harness-runtime-contract.md#deferred) | Benchmark/prototype must compare failure recovery against the proven runner seam. | No adoption decision. |
| HR-059 | Record OTP experiment status without presenting it as the chosen architecture. | DEFERRED | Later | [Deferred](harness-runtime-contract.md#deferred) | Status links reproducible benchmark evidence and states non-adoption clearly. | Benchmark and evidence artifact remain deferred until stabilization gates. |
| HR-060 | Defer the OTP benchmark until the canonical runner behavior can be compared fairly. | DECIDED | Later | [Deferred](harness-runtime-contract.md#deferred) | Benchmark begins only from a stable scenario/proof contract. | Prevents prototype novelty from steering architecture. |
| HR-100 | Explore forking experiments. | DEFERRED | Later | This register | Captured for later consideration; no implementation claim. | Captured idea on 2026-07-12; not scheduled. |
| HR-101 | Explore agent emotional states. | DEFERRED | Later | This register | Captured for later consideration; no implementation claim. | Captured idea on 2026-07-12; not scheduled. |
| HR-102 | Explore a hypothesis queue. | DEFERRED | Later | This register | Captured for later consideration; no implementation claim. | Captured idea on 2026-07-12; not scheduled. |
| HR-103 | Explore a multi-lane TUI. | DEFERRED | Later | This register | Captured for later consideration; no implementation claim. | Captured idea on 2026-07-12; not scheduled. |

## Decision ledger

| Decision | State | Consequence | Source |
|---|---|---|---|
| D-001 | DECIDED | Stabilization proceeds P1 → P2 → P3; a later phase cannot start on partial proof. | [Sequential phases](harness-runtime-contract.md#three-sequential-stabilization-phases) |
| D-002 | DECIDED | One runner owns execution; views are disposable projections. | [Runner/view boundary](harness-runtime-contract.md#runner-view-boundary) |
| D-003 | DECIDED | Queue order is global capture order; class affects eligibility only. | [Input queue](harness-runtime-contract.md#input-queue) |
| D-004 | DECIDED | Explicit valid user route wins; policy may visibly block but not silently substitute. | [Effective precedence](harness-runtime-contract.md#effective-precedence) |
| D-005 | DECIDED | Eight work roles remain distinct from model lanes and concrete routes; `vision` resolves to `google-antigravity/gemini-3.5-flash` only for the native-video capability. | [Three taxonomies](harness-runtime-contract.md#three-distinct-taxonomies); HR-012 |
| D-006 | DECIDED | Branch, fork, resume, restart, rollback, promotion, and detach remain distinct operations. | [Session identity](harness-runtime-contract.md#session-identity-branch-fork-resume-restart) |
| D-007 | DECIDED | Domain stores keep bounded authority; SQLite is query projection, not live authority. | [Blessed owners](harness-runtime-contract.md#one-blessed-owner-per-concern) |
| D-008 | DECIDED | The new `SessionRunner` will be Effect-native. Migration is behavior-preserving and slice-by-slice; durable stores and explicit ordering/ownership contracts remain. | [Effect-native runner](harness-runtime-contract.md#effect-v4-effect-native-runner-target) |
| D-009 | DECIDED | This register is the intake ledger; implementation claims require phase gate and linked proof. | [Register rules](#register-rules) |
| D-010 | DECIDED | A slice is complete only after behavioral proof and a coherent Git checkpoint; dirty-tree builds are not rollback revisions. | [Slice checkpoint contract](harness-runtime-contract.md#slice-checkpoint-contract) |
| D-011 | DECIDED | cmux tabs are convenient test/client surfaces; one-writer and isolation rules come from the runner/profile contract, not cmux. | [Slice checkpoint contract](harness-runtime-contract.md#slice-checkpoint-contract) |
| D-012 | DECIDED | The 2026-07-12 five-priority redesign contract is no longer a pause on rich/default-terminal work: the verified slices are landed, typed vendor `--apply` is active for the 21-entry manifest (NCode removed), and remaining residuals are cmux blocked by a dirty tree, Bun-test EBADF in unit Git fixtures, and external-owner cleanup. | HR-086/HR-089/HR-090 |
| D-013 | DECIDED | The minimal HTML workstream page supersedes the operational viewer gate for this redesign; the verified Phase3A control-plane projection remains an active commit request pending ownership resolution. | HR-087/HR-091; commit `334a2ab1` |
| D-014 | DECIDED | The append-only journal is the API; every other feature is a bounded projection/cache and must not invent a parallel state store. | [Meta-consolidation doctrine](meta-consolidation.md#the-load-bearing-insight-from-today) |

## Open questions

| ID | Question | Needed before | Owner phase |
|---|---|---|---|
| OQ-001 | What exact keyboard bindings and conflict policy should queue edit/dequeue use across terminal profiles? | HR-001–HR-004 UI implementation | P1 |
| OQ-002 | CLOSED — roles/lane names are not aliases. Native video uses `vision → google-antigravity/gemini-3.5-flash` on the Cloud Code Assist API, while Luna and Haiku require full concrete provider/model/API routes; no bare-name resolution is permitted. | — | Closed |
| OQ-003 | Which quota/fallback choices require synchronous approval versus a preapproved policy envelope? | HR-017/HR-018 proof | P1 |
| OQ-004 | What redaction and retention policy applies to prompts, provider payloads, dumps, and reproduction artifacts? | HR-036–HR-043 completion | P1 |
| OQ-005 | Which remote command transport and authentication mechanism will expose the canonical runner protocol first? | HR-028 implementation | P2 |
| OQ-006 | What qualifies a temporary explainer for exemption from full QA, and when does it become a maintained surface? | Future viewer artifacts | P3 |
| OQ-007 | Should the Effect-native runner preserve queue-v2's file journal adapter or migrate live obligation state plus transactional outbox to a per-runner/session SQLite store? | HR-073/HR-074 representative slice | P1/P2 |
