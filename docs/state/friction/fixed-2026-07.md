# Fixed harness friction — July 2026

Source: `docs/state/harness-friction.md`; moved 2026-07-13.

| Date | Symptom | Fix commit/file |
| --- | --- | --- |
| 2026-07-12 | **Stable binary promotion:** blessed `74dac97b40deba870ecd07fcd757954c8bd73912e076dd23312e64be62778eb1` (`fork.b3607b74`) is promoted with `previous=697fb421`; receipt and isolated rollback proof recorded. Arthur's `/restart` P0 is fixed in this binary. | promotion receipt; isolated rollback proof |
| 2026-07-12 | **Live `/restart` same-PID exec and child continuation:** five stacked root causes are fixed: execve handoff, task-prefix parentId lineage, fresh-session ownership binding, virtual argv stripping, and owned-manager child checkpoint/re-adoption. Live round6 proved two cycles with exactly-once `pending → resuming → resumed`, no junk prompt, and ticks advancing 44→98; live-PTY proof is now mandatory for process-lifecycle claims because unit suites alone were insufficient. | commit `b3607b74`; 68 tests; `local/restart-live-proof/round6` |
| 2026-07-12 | **Codex weekly auto-reset:** saved rate-limit-reset credits redeem by default only after a normalized weekly limit, with a one-attempt-per-account/24h cap, idempotency, and structured audit; purchase-like or ambiguous outcomes stop with a notice. | commit `08836bc7`; 28 codex-auto-reset tests; staged-snapshot gate + typecheck |
| 2026-07-12 | **Pending-message editing over queue-v2:** Alt+Up durably cancels the newest contiguous same-delivery-class queued text group into the editor; resubmission is one normal fresh idempotent admission, with empty-queue history fallback. | commit `de3dd3b1`; 75 tests across five suites; staged-snapshot gate + typecheck |
| 2026-07-12 | **Full-height Agent Hub preview:** single- and dual-lane preview fills terminal height minus the compact roster strip and dynamic chrome while accounting for editor space. | commit `57a58256`; 52 hub tests including 24/40/60-height regressions; staged-snapshot gate + typecheck |
| 2026-07-12 | **Stacked narrow Agent Hub lanes:** below the 160-column breakpoint, Prompt/Route/Comms stack above transcript preview in equal halves with a transcript-only floor for short tracks. | commit `ac12320f`; 57 hub tests including 80/120-column stacked and floor regressions; staged-snapshot gate + typecheck |
| 2026-07-12 | **Primitive orthogonality bar:** new primitives must extend an existing owner rather than twin it, compose without consumer knowledge, and leave exactly one hole when deleted; later overlap is a bug with a deletion deadline. | commit `2f16791`; `docs/fable/meta-consolidation.md` |
| 2026-07-12 | **Stable binary promotion:** blessed stable digest `697fb4219f62b0147bcdaa8b73f883e0a584ac607d5038732ea076d6b8da0349` is version `16.0.1+fork.34882b2e764c`; registry `previous=708b4a8f`; receipt digest `42a0ae3e…`; isolated rollback proof passed. The restart P0 is fixed in this binary. | promotion receipts; stable digest and version recorded above |
| 2026-07-12 | **Crash-safe `/restart` with child re-adoption:** ordered teardown, epoch-fenced handoff, and `pending → resuming(attemptId) → resumed` manifest reconciliation eliminate the ownership-error/TUI-corruption P0; review fixed empty-manifest, resumed-before-continue, and unreconciled-manifest P1s. | commit `95bf8dd7`; restart-session + re-adopt 45 tests; detached owner→replacement process proof |
| 2026-07-12 | **Canonical IRC delivery provenance:** `origin:user\|agent\|system` is carried by one `IrcDeliveryRecord` schema across internal/external buses, including legacy external rows and receiving-side projections. | commit `4fe85ac4`; 67 focused IRC/sibling/renderer tests; staged-snapshot gate + typecheck |
| 2026-07-12 | **Shared journal projection substrate:** one JSONL→typed-entry module replaces three parser copies for sibling tail-follow, HTML export, and Agent Hub preview; bounded reads stay in the library. | commit `1b6c8124`; journal-projection plus sibling/export/Hub suites; 77 duplicated LOC removed |
| 2026-07-12 | **Canonical AgentRef lineage resolution:** job ownership and hot-swap authorization share exact→descendant→job resolution across id/job/session/file/external facets, eliminating split identity walks. | commit `34882b2e`; agent-ref/hotswap/displacement tests; typecheck clean |
| 2026-07-12 | **Staged-snapshot checkpoint gate:** the gate typechecks staged files in an index-materialized temp worktree, optionally runs focused commands, restores the caller tree byte-identically, and now runs package `generate` first; first dogfood caught two missing gitignored generated artifacts. | commits `7d644d81`, `80decda4`; checkpoint-gate test; caught `docs-index.generated.ts`/`tool-views.generated.js` |
| 2026-07-12 | **Review doctrine reconfirmed:** restart-wave review found and fixed three P1s, including the empty production manifest no-op; restart was not claimed complete until manifest reconciliation and the resumed-before-continue window were proven. | commit `95bf8dd7`; restart wave review; 45 tests |
| 2026-07-12 | **Final-response reveal after cursorless tool collapse:** completion reflow now reveals the final response while preserving intentional reader scroll; bisect proof isolates the fix from 8 independent baseline failures. | commit `2314a461`; `vendor/oh-my-pi/packages/tui/test/abort-collapse-gap.test.ts` |
| 2026-07-13 | **Semantic tail/tool-reflow regression verified:** production-shaped fixtures invalidate the tool root before component-scoped progress and assert semantic ordering plus final nonblank visibility across historical reflows. The focused staged-snapshot gate was green for `packages/tui check:types` and `semantic-scroll-repro` (2 pass); prior reds were invalid fixture assumptions (blank/tool exactness and omitted production component invalidation), not a new TUI core defect. Existing render/tail fixes remain authoritative. | commit `cee905d2`; `vendor/oh-my-pi/packages/tui/test/semantic-scroll-repro.test.ts` (`keeps final tail after two historical reflows and progress append`; `does not reuse a stale historical root during component-scoped progress`) |
| 2026-07-13 | **Default full rich TUI is now reloadable without rebuilding the session:** runner-backed disposable host is default, `/reload-tui` requests a host-level view reload, cross-session control is owner-epoch fenced, and restart captures a child manifest; compact mode remains non-default and startup `Spacer` placement is fixed. | commit `c2d4572b`; staged package typecheck; 60 pass/1 skip; phase-two gate 7/7 with zero residuals; Darwin PTY same PID/session with termios restored |
| 2026-07-13 | **Refusal observability has a local replay laboratory:** append-only redacted JSONL records live at `~/.omp/agent/refusals.jsonl` or `OMP_REFUSALS_PATH`; `omp refusals list/show/stats/mark/replay` exposes bounded cases, marks by appending, and records no-tools replay history while rejecting tool-required replay. | commit `2f54cb1d`; 8-pass staged gate; corpus and real-CLI coverage |
| 2026-07-13 | **Resume startup decodes the journal once:** a 19.8MB session with 10,333 entries reduced direct-parser median 75.20→39.35ms and `createSessionManager` 155→106–114ms. | commit `e462ecc6`; 35-pass staged gate; `test/session-manager/file-operations.test.ts` |
| 2026-07-13 | **Transcript/Hub retention is bounded without stale projections:** push invalidation, Hub-close cleanup, and u/d paging/gutter behavior hold under embedded 2k/5k workloads. | commit `4bbd655a`; 68-pass staged gate; Agent Hub/transcript-container focused suites |
| 2026-07-13 | **Phase3A operational evidence is selectively checkpointed without absorbing unrelated dirty-tree work:** Effect-Schema-decoded runner/diagnostic events enter an append-only SQLite projection with idempotency, conflict/source-gap tracking, diagnostic folding, and read-only lag/staleness CLI queries. The first selective patch duplicated existing ledger helpers; the isolated typecheck caught it and the corrective checkpoint removed 73 repeated lines. | commits `2c59230e`, `da35f954`; isolated typecheck; 21 focused tests, 273 assertions |
| 2026-07-12 | **Agent Hub cockpit preview and modal navigation:** preview pane, modal scroll/input modes, tree/flat navigation, and drill-down return paths preserve hub selection/cursor. | commit `78057121`; 90 tests |
| 2026-07-12 | **Opt-in ambient agent renamer:** glanceable names are generated through the existing IRC/session path only when enabled, preserving explicit names. | commit `569d23ab`; `vendor/oh-my-pi/packages/coding-agent/test/ambient-agent-renamer.test.ts` |
| 2026-07-12 | **Incremental HTML viewer v2:** branch tree and vim keys are rendered incrementally; the 17MB proof measured FCP 48ms and DCL 875ms after the prior viewer path was fatal. | commit `01fef3f3`; `vendor/oh-my-pi/packages/coding-agent/test/export-subsessions.test.ts`; 17MB QA |
| 2026-07-12 | **Ownership-loss typed read-only transition:** a lost session lease now becomes an explicit read-only state instead of continuing mutable writes. | commit `a701199a`; `vendor/oh-my-pi/packages/coding-agent/test/session-manager-durable-custom-message.test.ts` |
| 2026-07-12 | **Idempotent input admission:** cross-turn duplicate admission no longer emits a second follow-up; the double-`followUp` path is fixed. | commit `d35aef8c`; `vendor/oh-my-pi/packages/coding-agent/test/main-interactive-input.test.ts` |
| 2026-07-12 | **Resume papercuts:** role metadata no longer crashes resume and the takeover banner identifies the owner. | commit `91eb3f18`; session ownership, selector-status, durable-queue, and settings tests |
| 2026-07-12 | **Dual-lane spawn inspector:** wide hub previews expose Prompt, Route, and Comms lanes with bounded prompt materialization and keyboard lane/section navigation. | commit `23044434`; `vendor/oh-my-pi/packages/coding-agent/test/agent-hub-dual-lane.test.ts` |
| 2026-07-12 | **Read-only sibling session cockpit:** sibling journals tail-follow bounded history, and cockpit input is tagged `origin:user` across external IRC bus instances. | commit `9d9782c2`; `vendor/oh-my-pi/packages/coding-agent/test/sibling-session.test.ts` |
| 2026-07-12 | **Release registry prior adoption:** blessing adopts the prior stable as registry `previous`, making rollback executable and dry-verifiable. | commit `5f982ad1`; `vendor/oh-my-pi/scripts/link-omp.test.ts` |
| 2026-07-12 | **Agent-stack consolidation:** role/lane, packet, proof, skill, and spawn doctrine is recorded without changing live model-routing values. | commit `f1ae784b`; `docs/fable/agent-stack-consolidation.md`; `docs/fable/charter.md` |
| 2026-07-12 | **Tool-usage analytics baseline:** seven-day all-session JSONL analysis captures tool volume, error/result-token costs, and consolidation candidates. | commit `23b9b255`; `docs/fable/tool-usage-analytics.md` |
| 2026-07-12 | **Advisor double-send allegation exonerated:** journal evidence found 22 distinct cards, none near the complaint timestamps; the stale-lifecycle defect was already fixed. | commits `68c0e3a`, `3b5a13c3`; journal evidence |
| 2026-07-12 | **Stable binary promotion:** blessed stable digest `708b4a8fc1d1f68bc037243d541f8581bf6c46b2fdfe9960d991d2d13241509` is version `16.0.1+fork.9d9782c2b919`; receipt digest `c99aa115…`; registry `previous=42ac9b54…`; rollback dry-verified; detached-HEAD compile gate passed. Arthur gets everything on restart. | commit `5f982ad1`; promotion receipts |
| 2026-07-12 | **Phase-two terminal runner/view gate is fail-closed:** 7/7 checks run, then the gate exits 1 with residuals `default-terminal-selection` and `interactive-view-authority`; rich migration is paused. | commit `71098639` |
| 2026-07-12 | **Session transition coordinator:** session transitions use the closed `InteractiveHostIntent` set; 6 tests passed. | commit `8c9d486c`; 6 tests |
| 2026-07-12 | **Runner terminal parity substrate:** semantic terminal command fidelity checkpoint passed 51 tests with 1 skip; three review P2s were fixed: scoped `cycleModel`, reload cancellation, and checkpoint invalidation. | commit `f49a1549`; 51 pass/1 skip; 3 review P2s fixed |
| 2026-07-12 | **macOS beachball / blocked OMP input in long sessions:** live samples showed sustained periodic main-loop work (10–14% CPU) and 724–776MB footprint/1.3GB peak, not an infinite loop. Tool/assistant animations now request component-scoped paints; forced renders yield through an I/O-fair scheduler with one trailing reentrant frame; Agent Hub and assistant subtree teardown release parsed transcripts, stale peer identities, timers, and subscriptions. Stable binary: `omp/16.0.1+fork.e30d14766143`. | commits `fed55308`, `97e873bc`, `e30d1476`; focused scheduler/TUI/retention proof: 83 tests; typechecks clean |
| 2026-07-12 | **Root-cwd sessions lost workstream identity and `/goal` drifted conceptually from stream `GOAL.md`:** sessions now persist one typed workstream/adhoc header, dynamically project the derived charter into session-local goals, expose launch/live classification, render/filter selector badges, and snapshot identity into task children. | commits `243a8104`, `ce45e90d`; stable `omp/16.0.1+fork.ce45e90d880b`; 82 focused tests across core/UX plus typecheck |
| 2026-07-12 | **IRC revive reserve-before-revive:** parked-recipient sends reserve mailbox entries before revival, follow replaced registry identities, and re-enqueue on failed handoff; injected external bus keeps `op:list` tests hermetic. IRC revive failures recurred ~6 times for Main before restart this session and self-clear after restart onto the fixed binary. | commit `27f00c3e`; 35 tests (106 assertions) |
| 2026-07-12 | **Nested descendant job operations:** poll/cancel/interrupt/setModel now authorize registry-subtree descendants and preserve caller attribution; foreign trees remain rejected. | commit `f58e928a`; 28 tests (99 assertions); typecheck clean |
| 2026-07-12 | **Component-scoped rendering and phase telemetry:** component paints replace global invalidation, dirty-root membership avoids subtree scans, and a 10k-child keystroke proof records zero history-child renders. | commit `911d2a51`; 10 tests; typechecks clean |
| 2026-07-12 | **Immediate cold-park with an IRC-safe fence:** completed children retain only bounded projections after JSONL persistence; parked memory drops 35.2x (264,448 B/child live versus 7,522 B/child parked), with zero parked sessions/subscriptions/timers. | commit `bb8d214b`; 69 tests (253 assertions); typecheck clean |
| 2026-07-12 | **Sectioned incremental Agent Hub:** Running, attention, bounded Recent-completed, and collapsed Parked sections update incrementally and render only viewport rows; event-to-projection p95 is 14.3µs at N=338 (9.9µs at N=1000). | commit `51c96aba`; 45 tests (283 assertions); typecheck clean |
| 2026-07-12 | **IRC delivery-state telemetry:** bounded per-message queued/delivered/read/failed DTOs with timestamps and redacted bodies are exposed through the runner snapshot and `op:list`. | commit `4b59fef1`; 37 tests; typecheck clean |
| 2026-07-12 | **Goal `set-core` lifecycle:** `setGoal` converges create/update/resume on every nonterminal state while complete/drop remain explicit terminals. | commit `36589a00`; 37 tests (186 assertions); typecheck clean |
| 2026-07-12 | **Restart argv secret stripping:** restart reconstruction removes API-key flags from child argv, carries the value through `OMP_RESTART_API_KEY`, and consumes/deletes it at startup. | commit `8f6d32b3`; focused spec pass; typecheck clean |
| 2026-07-12 | **Compaction inclusive threshold:** fixed, percentage, and reserve-derived thresholds now schedule compaction at the exact boundary; the direct context-limit incidents remain open because this is preventative only. | commit `3dbda1d6`; 30 pass/1 skip; typechecks clean |
| 2026-07-12 | **Journaled Main-session hot-swap:** Main model changes commit a journaled `SetModel` receipt at the safe boundary, and same-tool-call retries replay idempotently without duplicate effects. | commit `23da13b9`; 28 tests (105 assertions); typecheck clean |
| 2026-07-12 | **Runner-backed `--collab-host` and workstream web page:** live SessionRunner hosting, join URL persistence, delivery/model/revision panels, and GuestClient `setModel` round-trip are shipped. | commit `334a2ab1`; coding-agent 47 pass; collab-web build/typecheck and 6 client tests pass |
| 2026-07-12 | **Local-first relay, plaintext attach, and fenced controller preemption:** relay routing, AES-GCM plaintext attach decoding, and stale-controller fencing are proven live across the 01–08 proof set. | commit `01f34d6c`; coding-agent 62 pass/1 skip; collab-web 28 pass; both typechecks clean |
| 2026-07-12 | **Demoted-view detach and live view reload:** a demoted terminal view tears down without clearing the active browser controller; reload preserves session identity, live page state, and interactive input. | commit `45062028`; live reload proof; session-runner 40 pass/1 skip; typecheck clean |
| 2026-07-12 | **Promotion compile gap closed:** `runner/errors.ts` and `async/job-manager.ts` changes missed staging for the `f49a1549`/`23da13b9` slices and are now included. | commit `6d47bcbf`; package check:types clean at HEAD |
| 2026-07-12 | **Durable harness issue taxonomy:** five-day failure mining is recorded as ranked clusters with frequency, impact, subsystem, fixed flag, and reproduction pointers; compaction recovery latency remains open. | commit `a7d2b5df` |
| 2026-07-12 | **Stable binary promotion:** blessed stable digest `42ac9b54273443a762d44826737c37f1fb4306edc0fad2b4f85757af493e53a` is version `16.0.1+fork.45062028fd95`; readiness receipt `ab5abf91…`, bless `receiptDigest` `e1d075bb…`, registry SHA `f46dfec9…`. Arthur gets fixes on the next OMP restart. | promotion receipts; stable digest and version recorded above |
| 2026-07-11 | **Split durable authority / double-enqueue risk resolved through the Phase 2 `SessionRunner` checkpoints:** queue-v2 atomically accepts command + input + runner revision; the same queue is injected rather than reopened; `AgentSession` is the sole provider-attempt owner; `SessionManager` owns transcript publication; and the SDK returns the runner without a writable `AgentSession`. Commit `5c4832ee` additionally proves submit/edit/cancel through queue command idempotency, runner/item compare-and-swap fences, and kind-specific events. The verified terminal prerequisite chain continues through disposable terminal projection/controller (`6a343495`, 8 tests/60 assertions), SessionManager durable state command journal (`3035f2e4`, 17/102), thinking control (`22b4d37b`, 41/180), model control (`9a69e20e`, 44/212), exact prompt interrupt (`186f2b8b` + `c27b8d3f`, 17/127), manual compaction (`1d57d302`, 20/148), and exact compaction cancellation (`01ef51ca`, 20/169). The split is explicit: queue/input `inputRevision`, durable SessionManager state/command journal `sessionRevision`, and live prompt/compaction operation-generation fencing; every checkpoint typechecked cleanly, with Sol review PASS after fixes. Process proof covers durable acceptance/replay and explicitly does not claim provider completion. Residual: `InteractiveMode`/main is not cut over; plan/goal/tool/session-transition controls, collab cutover, and canary/rollout remain, so the Phase 2 gate is still in progress. Later verified checkpoints add goal transitions (`aba26d09`), tool config (`7666b5ac`), todo/SSH (`5238be4e`), read queries (`ecf0c373`), local operations (`6c692efe`), ephemeral turns (`2b206e71`), durable custom messages (`69c35f1d`), owned internal-custom queue cutover (`11df0b3e`), the false-pending legacy-mirror UI fix (`fe4ce19c`; journal proves one delivery), Agent Hub running-only dot focus (`25581ef8`), main Alt+V raw semantic/rich toggle (`39980ebd`), disposable terminal host (`b084cd31`), and opt-in reloadable compact TUI (`fa6c8b11`; real PTY A→B in one PID/session, exit 0, exact termios). Commit trailers carry the focused proof/typecheck evidence. The apparent duplicate was a false-pending legacy-mirror projection, not a second delivery. The reloadable TUI is opt-in via `--tui-bundle-manifest`; default legacy `InteractiveMode` remains selected, so full terminal cutover/parity, broader Agent Hub UX, collab, and canary/rollout remain unfinished. Review heuristic remains provisional: core reviews caught many latent defects; visual UI uses focused tests/direct play by default, not promoted doctrine. The immutable stable `omp/16.0.1+fork.eb3cbf922ebc` predates this checkpoint chain and remains stale; no listed checkpoint is promoted. | commits `34cbaa5d`, `b286c31c`, `a1d32c21`, `4a0bdd9b`, `eb3cbf92`, `5c4832ee`, `6a343495`, `3035f2e4`, `22b4d37b`, `9a69e20e`, `186f2b8b`, `c27b8d3f`, `1d57d302`, `01ef51ca`; core: [`src/runner/session-runner.ts`](../../vendor/oh-my-pi/packages/coding-agent/src/runner/session-runner.ts); SDK: [`src/sdk.ts`](../../vendor/oh-my-pi/packages/coding-agent/src/sdk.ts); queue proof: [`test/session/durable-input-queue.test.ts`](../../vendor/oh-my-pi/packages/coding-agent/test/session/durable-input-queue.test.ts); live proof: [`test/runner/session-runner.test.ts`](../../vendor/oh-my-pi/packages/coding-agent/test/runner/session-runner.test.ts); process proof: [`test/runner/session-runner-process.test.ts`](../../vendor/oh-my-pi/packages/coding-agent/test/runner/session-runner-process.test.ts); mutation process proof: [`test/session/durable-input-queue-process.test.ts`](../../vendor/oh-my-pi/packages/coding-agent/test/session/durable-input-queue-process.test.ts); earlier checkpoint proof: 40 queue tests, 13 live-seam tests, 3 runner/SDK tests with 23 assertions, and checkpoint typechecks; `5c4832ee` focused mutation proof: 50 tests, 204 assertions, and clean typecheck; terminal prerequisite checkpoints: 8/60, 17/102, 41/180, 44/212, 17/127, 20/148, and 20/169 respectively, all typecheck-clean with Sol review PASS after fixes; immutable stable `omp/16.0.1+fork.eb3cbf922ebc` predates the entire chain and remains the stable binary; every listed checkpoint is unpromoted. |
| 2026-07-11 | **Out-of-order delivery:** mixed steer, follow-up, and compaction input now preserves one global capture order, including restart. | queue checkpoint commit `de3c3991`; core: [`src/session/durable-input-queue.ts`](../../vendor/oh-my-pi/packages/coding-agent/src/session/durable-input-queue.ts); process: [`test/session/durable-input-queue-agent-session-process.test.ts`](../../vendor/oh-my-pi/packages/coding-agent/test/session/durable-input-queue-agent-session-process.test.ts) |
| 2026-07-11 | **Invisible, non-editable durable queue:** durable and legacy obligations are ordered and visible; identity/revision edit and `cancel(inputId)` have process proof. Residual: generic pending-next-turn internal messages are not fully projected, and the queue edit/cancel keybinding remains [OQ-001](../fable/harness-request-register.md#open-questions). | queue checkpoint commit `de3c3991`; core: [`src/session/agent-session.ts`](../../vendor/oh-my-pi/packages/coding-agent/src/session/agent-session.ts); UI: [`test/agent-hub-queue.test.ts`](../../vendor/oh-my-pi/packages/coding-agent/test/agent-hub-queue.test.ts); process: [`test/session/durable-input-queue-process.test.ts`](../../vendor/oh-my-pi/packages/coding-agent/test/session/durable-input-queue-process.test.ts) |
| 2026-07-11 | **Restart ownership race:** `/restart` now releases the predecessor lease before the replacement acquires it. | restart checkpoint commit `a0737944`; core: [`src/cli/restart-session.ts`](../../vendor/oh-my-pi/packages/coding-agent/src/cli/restart-session.ts); process: [`test/restart-session.test.ts`](../../vendor/oh-my-pi/packages/coding-agent/test/restart-session.test.ts) |
| 2026-07-11 | **Role-switch precedence opacity:** routing precedence, runtime reload, explicit quota block, automatic reroute, and Agent Hub source/final-route display are proven. | routing checkpoint commit `8c66760f`; core: [`src/config/model-resolver.ts`](../../vendor/oh-my-pi/packages/coding-agent/src/config/model-resolver.ts); process: [`test/process/routing-precedence-process.test.ts`](../../vendor/oh-my-pi/packages/coding-agent/test/process/routing-precedence-process.test.ts), [`explicit-quota-block-process.test.ts`](../../vendor/oh-my-pi/packages/coding-agent/test/process/explicit-quota-block-process.test.ts); display: [`test/agent-hub-route-provenance.test.ts`](../../vendor/oh-my-pi/packages/coding-agent/test/agent-hub-route-provenance.test.ts) |
| 2026-07-07 | Compiled fork binary cannot resolve bare npm deps from external extension files (bun isolated-store symlink layout) — control-plane AND web-access extensions silently failed every session start ("Cannot find package 'effect'"; web-access had been failing for days unnoticed). Workaround SHIPPED: extensions ship as bundled dependency-free dist files (`bun run build:extension`); real fix queued: loader-level bare-specifier resolution fallback in the fork. web-access still needs its bundle. | commit 0d426225; vendor/oh-my-pi/packages/coding-agent/src/extensibility/plugins/legacy-pi-compat.ts:636-645 |
| 2026-07-07 | Worker budget kills root-caused: base `.omp/config.yml` still had `softRequestBudget: 40` — the 2026-07-04 raise only touched the fable overlay, and sessions running under base config killed workers at 60 req (RoutingStore died mid-packet; work survived via report-file-first). Base raised to 80. Also: `quick_task` lane dies at ~10 req — too small for anything beyond one-file transforms; route bigger mechanical work to kimi-implementer. | commit 0d426225; .omp/config.yml |
| 2026-07-07 | Session resume "suddenly broken" + fork/tree view misbehavior — ROOT CAUSED (forensics: docs/qa/resume-robustness-20260707.md): (1) active-leaf pointer was memory-only, resume rebuilt to the last physical JSONL entry → wrong branch after tree navigation (also made the tree view select/sort wrong); fixed with additive `leaf_change` metadata entries, replayed by index/read-only loads/collab wire, old files byte-identical. (2) `--continue` breadcrumb keyed to volatile terminal id → silent wrong session; now falls back to newest same-cwd breadcrumb from any terminal, then most-recent-in-cwd, with provenance notice. (3) unreadable session files were silently invisible; scans now count+warn and errors say so. Review caught read-only-history and collab-replication gaps pre-commit. | commit ca51962d; vendor/oh-my-pi/packages/coding-agent/src/session/session-manager.ts; src/session/session-loader.ts; src/collab/host.ts |
| 2026-07-07 | `task` job results went stale after irc wakes (`job poll` returned pre-wake payload forever; observed 4×). `AsyncJobManager.refreshResultText` (terminal jobs only, no re-delivery) driven by post-completion `agent_end` on adopted sessions; poll/snapshot now reflects the latest yield with a `[refreshed after follow-up turn]` marker. Isolated jobs excluded. | commit f5c07cdb; vendor/oh-my-pi/packages/coding-agent/src/async/job-manager.ts; src/task/executor.ts |
| 2026-07-07 | No soft interrupt: `job cancel` was the only intervention and it evicts (aborted + disposed). New `job interrupt {ids, interruptReason?}`: aborts the in-flight turn, keeps the agent idle/adopted/irc-addressable with a next-turn note, job completes with `[interrupted]` partial text. Hard cancel authoritatively overrides a prior interrupt (hardCancelled flag — AbortSignal.reason is immutable); isolated jobs reject interrupt honestly. | commit f5c07cdb; vendor/oh-my-pi/packages/coding-agent/src/async/job-manager.ts; src/task/executor.ts; src/tools/job.ts |
| 2026-07-07 | No way to hot-swap a live subagent's model (quota death / lane misbehavior mid-packet meant losing the worker's context). Added `hotswapAgentModel` primitive (`src/task/hotswap.ts`): selector resolution incl. `:thinking` suffix, idle=immediate / streaming=agent_end boundary with last-wins queue, JSONL `model_change` role `hotswap`, hidden next-turn `<system-warning>` so the child knows, park→revive restores swapped model + persisted thinking (auth-gated, spawn fallback). Surfaced as `job setModel {id, model, reason?}` with poll/cancel ownership. Adversarial review caught two revive-restore bugs pre-commit (thinking loss, unauthenticated pin). 15-test coverage. | commit b5d9db5a; vendor/oh-my-pi/packages/coding-agent/src/task/hotswap.ts; src/tools/job.ts; src/task/executor.ts |
| 2026-07-07 | Fast mode couldn't be scoped per provider at runtime — required editing the persisted `fastModeScope` setting, then `/fast on`. `/fast` now takes an inline scope: `/fast on gpt`, `/fast off claude`, bare `/fast gpt`; aliases gpt/openai/oai/codex, claude/anthropic/opus, both/all. `setFastMode(enabled, scope?)` does session-scoped tier set arithmetic (on=union, off=subtract, empty=off); scope-less behavior unchanged; never writes the setting. 76-test coverage (fast-mode-scope + acp-builtins). | commit 781cf956; vendor/oh-my-pi/packages/coding-agent/src/session/agent-session.ts:6713; src/slash-commands/builtin-registry.ts |
| 2026-07-04 | `omp irc` one-shot CLI commands hung forever: `runIrcCommand()` borrowed the global `IrcExternalBus` singleton whose open SQLite WAL handle kept the process alive. CLI now creates a CLI-owned bus and closes it in `finally`; agent-side polling lifecycle unchanged. Verified: `timeout 10 omp irc list </dev/null` exits 0 in <1s (was 124), inbox path clean, 9-test coverage. | vendor/oh-my-pi/packages/coding-agent/src/cli/irc-cli.ts |
| 2026-07-04 | Presence `waiting_input` covered post-turn prompt only; mid-turn `ask`-tool waits reported `working` on the irc bus. Added ToolSession `beginIrcWaitingInput` seam delegating to AgentSession's `#updateExternalIrcPeerState`; AskTool wraps each blocking question in begin/finally-restore incl. abort paths. 37-test coverage. | vendor/oh-my-pi/packages/coding-agent/src/tools/ask.ts; src/session/agent-session.ts; src/sdk.ts |
| 2026-07-04 | `task` runs aborted on wall-clock timeout with a bare "operation timed out" and no partial results. Added per-spawn `timeoutSec` (60–3600, threads into existing `options.maxRuntimeMs` seam), and timeout results now carry `timeoutPartial` (files created/modified from the session tool-call log + last assistant text) plus a text summary; non-isolated timed-out sessions stay on the keep-alive path (irc-addressable). Root-cause note: default `task.maxRuntimeMs` is 0/unlimited and nothing local configures it — the observed "~400s cap" was the 420000ms `task.agentIdleTtlMs` or older-build behavior. 15-test coverage. | vendor/oh-my-pi/packages/coding-agent/src/task/types.ts; src/task/index.ts; src/task/executor.ts |
| 2026-07-03 | Codex auto-refresh consumed subscription tokens without consent. Added `auth.codexRefresh: auto\|manual`; under manual, cached-fresh credentials are used, refresh short-circuits with a gated notice, explicit `forceRefresh` path remains available. Manual set in all four ~/agents overlays. 28-test coverage. | vendor/oh-my-pi/packages/coding-agent/src/config/codex-refresh-policy.ts; src/config/api-key-resolver.ts:67; src/config/model-registry.ts:1861,1892 |
| 2026-07-03 | subagent hard-fails on Anthropic content-filter stream error | vendor/oh-my-pi/packages/coding-agent/src/session/agent-session.ts:9166; vendor/oh-my-pi/packages/coding-agent/src/session/agent-session.ts:9533; vendor/oh-my-pi/packages/coding-agent/src/session/agent-session.ts:9632 |
| 2026-07-03 | Escape remapped to Ctrl-Q orphaned cancel/clear/close paths that hardcoded Escape. Fixed TUI-hosted paths to resolve through `app.interrupt`. | vendor/oh-my-pi/packages/coding-agent/src/modes/components/agent-hub.ts:566; vendor/oh-my-pi/packages/coding-agent/src/modes/components/agent-hub.ts:858; vendor/oh-my-pi/packages/coding-agent/src/modes/components/hook-editor.ts:100; vendor/oh-my-pi/packages/coding-agent/src/tools/bash-interactive.ts:204; vendor/oh-my-pi/packages/coding-agent/src/debug/log-viewer.ts:497; vendor/oh-my-pi/packages/coding-agent/src/debug/raw-sse.ts:95; vendor/oh-my-pi/packages/coding-agent/src/autoresearch/dashboard.ts:95; vendor/oh-my-pi/packages/coding-agent/src/modes/components/plugin-settings.ts:46; vendor/oh-my-pi/packages/coding-agent/src/modes/setup-wizard/wizard-overlay.ts:106; vendor/oh-my-pi/packages/coding-agent/src/modes/setup-wizard/scenes/sign-in.ts:66 |
| 2026-07-03 | Word-left / word-right after typing failed on Ghostty macOS Option-arrow because the terminal reports Option-arrow as `super+alt+left/right`, while defaults only included alt/ctrl variants. | vendor/oh-my-pi/packages/tui/src/keybindings.ts:69; vendor/oh-my-pi/packages/tui/src/keybindings.ts:73; vendor/oh-my-pi/packages/tui/test/keybindings.test.ts:55 |
| 2026-07-03 | Ctrl-S appeared broken in OMP because `app.session.observe` and unused `app.session.toggleSort` both defaulted to Ctrl-S. Moved toggleSort to Ctrl-Shift-S and moved dead tree defaults off editor word-navigation chords. | vendor/oh-my-pi/packages/coding-agent/src/config/keybindings.ts:175; vendor/oh-my-pi/packages/coding-agent/src/config/keybindings.ts:183; vendor/oh-my-pi/packages/coding-agent/src/config/keybindings.ts:199; vendor/oh-my-pi/packages/coding-agent/test/keybindings-display.test.ts:29 |
| 2026-07-03 | Stale subagent model roles — `setModelRole` mirrored persisted writes into runtime overrides which shadow every `reloadFromDisk`; all subagents resolved old-role Kimi regardless of config refresh. | fork commit 90c64256 |
| 2026-07-03 | Advisor polarity — `advisor.enabled: false` killed subagent advisors too; no way to express "advise workers, not the Fable main". | fork commit 90c64256 |
| 2026-07-03 | Install pipeline patched upstream instead of the fork. | docs/plans/omp-fork-install.md |
| 2026-07-03 | Skill leak — multi-source discovery loaded about 64 skills vs 20 intended. | docs/fable/harness-slimming.md |
| 2026-07-03 | `gpt-implementer` pinned to `pi/default`, breaking when default became Fable. | role config |
| 2026-07-13 | **Revived children bypass live-child admission cap** | Revival now acquires the originating TaskTool semaphore (FIFO, ≤5s bounded wait then bypass-with-warning — no-wedge rule); lease held until park, released on failed revival; IRC reservations survive the wait. 11+21+40 focused tests; union-gated in-tree, uncommitted. | ReviveAdmissionFix |
| 2026-07-13 | **Rich/non-rich preview switching undiscoverable** | `v` toggles rich/plain preview; search/modal/section/preview bindings documented in the `?` overlay. Union-gated in-tree, uncommitted. | HubUxFix |
| 2026-07-13 | **Sub-agent view `/` search trap** | Backspace deletes query chars; empty-query Backspace/Esc exits search cleanly; agent pages default to nav mode with `i` to insert and Esc unwinding one level. 63/63 hub suites. Union-gated in-tree, uncommitted. | HubUxFix |
| 2026-07-13 | **Paste pill not editable in place** | Contextual Enter on `[Paste #N]` expands raw text inline; undo-safe; no re-collapse during editing; discoverable via prompt-action autocomplete. Union-gated in-tree, uncommitted. | EditorStatusFix |
| 2026-07-13 | **tok/s needed on more surfaces** | Existing main-status token_rate segment was reading the focused child instead of stable main — repaired (main-token-rate.ts); Hub preview badge fixed off the false isStreaming gate and live-proven at 36.8/21.4 tok/s with post-completion suppression. Union-gated in-tree, uncommitted. | EditorStatusFix + HubUxFix |
| 2026-07-17 | **`omp --resume` hung forever at `SessionManager.list` (Arthur blocked twice, ~/automations and ~/agents):** lease liveness probe awaited hello negotiation with no deadline; any lease owner that accepted but never answered (event-loop-blocked daemon, or build-revision-skewed wire server silently ignoring undecodable hello) froze startup — 14/135 live `-agents` leases were wedged. Fixed: `helloTimeoutMs` transport option (probe: 500ms) with socket destruction + timer cleanup, plus 2s defense-in-depth cap on owner inspection during listing. Live proof: `~/agents` listing infinite→3.7s (135 sessions) on the blessed binary. Remaining server-side silent-tolerance stays in Open. | commit `0ad0c420`; blessed `16.0.1+fork.e95a3e74ae42` (digest `aa518cb8…`); tests `test/runner/wire/socket-transport.test.ts`, `test/session-manager/session-listing-skipped.test.ts` (9 pass) |
| 2026-07-19 | **HR-210 follow-up — draft-destructive generic Escape:** the normal non-vim main-editor Escape branch cleared typed drafts; it now preserves nonblank drafts and resets `lastEscapeTime`, disarming the double-Escape timer. Observed proof: `bun --cwd=packages/coding-agent test test/input-controller-escape.test.ts` passed 29 tests, 0 failed, 85 expect calls. | `vendor/oh-my-pi/packages/coding-agent/src/modes/controllers/input-controller.ts`; `vendor/oh-my-pi/packages/coding-agent/test/input-controller-escape.test.ts` |
| 2026-07-19 | **HR-195 follow-up — `/brief` omitted artifacts/live review surfaces:** the AWAY-BRIEF review section now requires a complete artifact inventory, separate human review URL and health endpoint, fresh service health/ownership evidence, exact bring-up command and applicable error-log state, plus bounded away-window pointer fallback when the substrate omits artifact pointers. Observed proof only: `bun scripts/streams.ts status harness` exited 0 with fleet-observer healthy (tmux) and fleet-board healthy (external/down); this current health is not durable policy. | `skills/fleet/thread-brief/SKILL.md`; `.omp/commands/brief.md`; `services.yml`; `scripts/streams.ts`; `packages/fleet-board/src/server.ts` |
| 2026-07-19 | **Explicit Haiku scout selection after the generic `fast` selector failed:** the harness permitted a scout to pin Haiku. | Fixed with the project/global `explore` override to `openai-codex/gpt-5.6-luna:xhigh`, the config-driven `*haiku*` subagent block, and Luna→Sonnet small-model priority with Terra absent from slow priority. Observed proof: model-resolver + model-registry coverage passed 209 tests, 0 failed, with 3,312 expect calls; four YAML configs parsed; 28 active routes and 36 priority entries checked; active Haiku/Terra references: 0. Project config: `../../../.omp/config.yml`; global config: `~/.omp/agent/config.yml`; priority: `../../../vendor/oh-my-pi/packages/coding-agent/src/priority.json`. |
| 2026-07-19 | **cmux Command-P override:** the active `~/.config/cmux/cmux.json` overrode `goToWorkspace` to `Cmd-D,P` even though repo-managed source omitted that override. The active override was removed; `cmux reload-config` returned `OK`; no UserDefaults override exists; the built-in cmux default remains `Cmd-P`. OMP has no super+P binding. | `~/.config/cmux/cmux.json`; `cmux reload-config` |
