# Agent Tooling Preferences

Durable preferences for local automation, browser control, and computer-use tooling.

## Current Defaults

- When Arthur says “cooler driver”, “cuadriver”, or “C-U-A driver”, interpret it as CuaDriver / `cua-driver`.
- Prefer CuaDriver for local macOS GUI automation that should run in the background without stealing focus. This includes native apps, browser windows that need visual/AX interaction, Messages, Telegram, Preview, Figma, YouTube, and similar app surfaces.
- When Arthur is using the computer, automation must be background-first: do not foreground browser tabs, steal focus, move the active cursor/window, or use visible Computer Use unless Arthur explicitly asks for it.
- Treat CuaDriver as the maintained boundary for private macOS/SkyLight behavior. Do not reimplement or vendor those private APIs in this repo unless Arthur explicitly asks.
- Keep CDP, Playwright, and Puppeteer for what they are best at: DOM JavaScript, protocol events, network capture, cookies/session inspection, and provider-specific browser frontend adapters.
- For logged-in browser/API reversal work, prefer direct APIs, saved sessions, CDP network/DOM access, CuaDriver, and background browser-use flows over `bringToFront`, visible clicking, or foreground screenshots.
- Avoid reactive focus-restoration hacks as the primary solution. AeroSpace/PID guards can remain as seatbelts, but new automation should not depend on stealing focus and jumping back.
- Keep `TASKS.md` current during substantial repo work so agents can recover active, next, blocked, and done tasks without relying on conversation memory.
- Keep the main thread in engineering-manager/collaborator mode: lead with the decision, material risk, and next action; put detailed reasoning and inventories on disk for optional review instead of making Arthur read them inline. For context-heavy delegation, the parent narrows decision rights and retains final synthesis/review, while workers retrieve exact source turns/artifacts and return short receipts; ordinary bounded tasks keep the simpler packet ([context-bearing-delegation.md](../fable/context-bearing-delegation.md) is the doctrine source — this bullet is the concise operating preference, not a second explanation). Generator: Arthur was fatigued by long architecture responses and wants to start while checking direction, not review every implementation detail (2026-07-20, provenance A).
- Treat explicit thread gardening as a human-controlled commit point: when Arthur asks to serialize/garden/sync a high-signal thread, reconcile decisions, fleshed-out deferred designs, backlog/research/errors, non-functional requirements, preferences, proofs, superseded docs, and continuation state into their owner-local authorities; substantial deferred designs get a linked design note, not only a compressed register row. The parent owns synthesis and review; read-only workers may recover transcript evidence. Generator: repeated loss of `/continue`/`/fork` design detail after extensive correction loops (2026-07-22, provenance A).
- In OMP prompts and spawned-session instructions, reference repo context with native `@<file>` syntax instead of spending prose/output tokens saying “read <file>”; add only the decision-relevant instruction around the reference. Generator: Arthur has repeatedly corrected verbose file-loading prompts (2026-07-22, provenance A).
- For React UI work, avoid unit tests that render owned components to raw HTML strings or snapshot full markup. Prefer pre-render state/view-model assertions, interaction/keyboard tests where behavior matters, and visual/browser QA for rendered layout. Use snapshots for stable normalized data/contracts, generated reports, or focused design-system variant outputs when they protect an intentional invariant.
- For UI/UX implementations, produce reviewer-saving proof artifacts after QA: a short recorded walkthrough video when practical, screenshots or visual diffs, and a concise report with commands and pass/fail findings. The artifact should show the changed workflow being exercised, not merely prove that a process exited.
- For API reversal, provider setup, integrations, and backend behavior, choose proof tests that demonstrate the actual accomplishment: live or dry-run contract tests as appropriate, snapshot/fixture tests, decoded boundary assertions, and saved outputs/logs that let Arthur or a reviewer verify the work quickly.
- For external APIs and provider JSON, validate runtime response shapes at the boundary with a permissive schema parser. Enforce the contract paths the pipeline relies on, allow additive extra fields, and record schema/proof artifacts so provider drift can be rerun and diagnosed later.
- For TypeScript API/client work, prefer Vitest snapshots for stable contract artifacts and generated reports. Use file snapshots for Markdown or large report output, object snapshots for normalized JSON contracts, and direct assertions for small branch behavior. `@effect/vitest` is useful for `it.effect` and Effect layers/scopes, but Vitest itself is the snapshot engine.
- For provider/API reversal, choose next work by user-visible workflow value first, implementation speed second, and no-spend/ease only as a tie-breaker or safety gate. If the highest-value step needs spend, mutation, visible UI, or capture approval, ask for approval with the exact command and proof plan instead of drifting into lower-value no-spend endpoints.
- For frontend API reversal, prefer a hybrid loop over pure static bundle reading: passive/background CDP capture for actual requests, `capture-analyze` or equivalent ranking for endpoint evidence, `discovery-worklist` or equivalent prioritization for the next small slice, structural/static search for request-builder semantics, explicit replay/probe for body variants, then a typed schema-backed command once the contract is stable.
- For Twitter/X research capture, use Arthur's logged-in X Pro/Grok account only through public or authorized surfaces. Preferred discovery mode can be a headful supervised browser on the Framework laptop, including direct prompts in `grok.com` to find relevant public X threads; archive the underlying tweet URLs, not Grok's unsupported claims. Prefer official/exported APIs when feasible; otherwise use a dedicated logged-in browser profile with CDP/CuaDriver to inspect the main tweet/search column only. Do not inspect DMs, private/locked accounts, sidebars/trends, account settings, or unrelated personal notifications. Preserve source URLs, timestamps, and author handles; distill strategy mechanics rather than storing bulk copyrighted/social content.
- When a Python utility needs third-party libraries, run it through `uv` with explicit dependencies, for example `uv run --with pillow python ...`; do not call `python3` directly for ad hoc library-backed scripts.
- Model routing (rewritten 2026-07-15 per `docs/fable/epistemics.md` — role→model maps are config, not prose): resolve live lanes from the session's `.omp/*.yml` overlay and `docs/fable/routing-doctrine.md`; availability from `docs/state/model-availability.md`. Durable generators only: creative/taste/persona work prefers Fable/Opus-class lanes; bounded constrained implementation goes to the cheap capable lane of the day; never spawn Fable subagents (hard-guarded). Model-specific do/don't rules in this file are band-aids — date them, name the model, and delete them when the lane rotates.
- Temporary routing posture (Arthur, 2026-07-20, provenance A; revisit after the next subscription reset): keep the main/orchestrator lane independent from subagent lanes; route settled implementation and open-ended worker packets to Opus 4.6 High for now, route bounded exploration/review/QA to Luna XHigh, reserve Sol for explicit difficult escalation rather than inheritance, and prohibit Fable subagents unless Arthur explicitly reverses the hard gate. The live role map belongs only in tracked `~/agents/.omp/config.yml`; do not copy its snapshot into prose. Falsifier: measured acceptance quality, latency, or changed quota posture supports a different lane.
- Privileged-command autoreview generator (Arthur, direct, 2026-07-23, provenance A): load-bearing security-policy work and privileged/destructive command paths should produce independent, bounded model autoreview evidence through the configured reviewer lane, without replacing independent accountable security review. Deterministic policy remains the authority: the reviewer may veto or escalate, but never widen scope, downgrade risk, approve unregistered actions, or replace mandatory Touch ID; review failures fail closed. The parent or accountable security owner disposes findings, and denials must not be rerouted. This records the DESIGN/INACTIVE contract, not an activation claim.
- Security-review quota separation generator (Arthur, direct, 2026-07-23, provenance A; revisit after the next Codex reset): during an explicit urgent functional unblock, stop nonessential security review and hardening once known functional blockers are isolated; use Luna XHigh for any review that remains mandatory, and defer broader security work to the separate security-review quota or the next Codex reset. Do not spend Sol XHigh on discretionary security review in this posture. This changes scheduling/model spend, not deterministic policy gates, mandatory Touch ID, or fail-closed runtime behavior.
- Keep the complete personal OMP configuration in tracked `~/agents/.omp/config.yml`; `~/.omp/agent/config.yml` symlinks to it so routing, extensions, skills, and UI settings share one diffable/revertible history. This deliberately makes `~/agents` the canonical control-plane workspace; use a genuinely separate overlay only when another repository needs different policy.
- For large risky harness changes, preserve experiments as named jj changes/bookmarks without creating permanent sibling workspaces; stream-owned paths remain the primary convention. Use a temporary sparse workspace only for concrete concurrent filesystem isolation, keep it under `~/.omp/workspaces/agents/`, and remove it after landing.
- For a missing CLI or developer tool, first check repository helpers and the `mise` registry, then install through `mise` when it can supply the tool. Use Homebrew only when `mise` cannot supply the required macOS formula or app. Generator: prefer scoped, reusable tool installs over incidental global package mutation; the 2026-07-17 `brew` experiment for `rbw`/`pinentry` unnecessarily upgraded unrelated `gnupg`, `gpgme`, and `poppler` dependencies even though the repository already had a Bitwarden helper.
- Use the reviewed broker/password-manager channel for exact credential operations; ordinary browser, CDP, GUI, and accessibility tools must never inspect, type, or recover passwords. Trigger routine Touch ID/system authorization prompts directly and continue when the active policy requires them. If the required broker/password-manager capability is inactive, unavailable, or incomplete, report the exact missing prerequisite and repair or activate the infrastructure rather than asking Arthur to perform routine authentication.
- Authentication-prompt UX generator (Arthur, direct, 2026-07-23, provenance A; urgent-unblock scope): do not precede a blocking system/Touch ID authorization with a redundant `ask` confirmation; start the exact authorized action and let it block on the native prompt. When Arthur may be away but has headphones connected, emit a brief local cue immediately before the native prompt only when the active output route is reliably identified as headphones; never play it through speakers on an ambiguous route. Prefer one bounded session grant for the exact recurring capability over repeated biometric prompts once that grant mechanism is active; this does not authorize blanket sudo while `signedSudo` is inactive.
- Routine-authentication automation generator (Arthur, direct, 2026-07-23, provenance A): repeated manual GDM and ordinary account authentication defeats terminal-driven end-to-end automation, so routine GDM login, ordinary website login, unambiguous account selection, Bitwarden unlock, and exact-origin official-extension autofill are automation responsibilities using the matching active, reviewed broker/password-manager path. Within a live closed broker grant, routine exact requests should redeem unattended; Touch ID should gate standing-grant creation/expansion and destructive or irreversible exact one-shots, and receipts must distinguish delegated use from a fresh biometric event. Scope: Arthur's terminal-initiated authentication on approved targets, origins, profiles, and accounts under the [canonical broker policy](../fable/remote-authentication-broker.md); secret handling stays confined to the reviewed private channel, and target quarantine lasts through terminal receipt and revalidation. Capability absence is faulty or incomplete infrastructure to report and repair, never a reason to hand routine steps to Arthur. Exceptions: ask Arthur only for CAPTCHA, 2FA, passkey confirmation, account recovery, consent, payment, security-setting changes, suspicious-login challenges, or material account ambiguity. Falsifier/narrower: evidence that an operation cannot keep its target closed, revocable, auditable, and drift-safe removes that operation from unattended grants and narrows it to a reviewed exact one-shot; explicit human control applies only when the resulting event is one of the listed nondelegable exceptions.
- Structured-automation shell generator (Arthur, direct, 2026-07-24, provenance A): prefer Nushell for readable typed orchestration, metadata parsing, manifests, status aggregation, and maintainable multi-step automation whenever the target already has a reviewed Nu runtime. Keep POSIX/Bash only where it is the actual compatibility boundary: shebang/bootstrap before Nu exists, PAM/systemd/SSH forced-command surfaces, vendor installers, or a fixed minimal transport substrate. Do not migrate working bootstrap code during an urgent product unblock; record it and cut over with native Nu validation afterward. Both Mac and desktop currently run Nu 0.114.1 from the dotfiles `aqua:nushell/nushell = latest` lane. Falsifier: Nu adds deployment/runtime dependencies or protocol ambiguity greater than its typing/readability benefit.
- Authentication trust-location generator (Arthur, direct, 2026-07-24, provenance A): OMP/desktop fingerprint and Touch ID implementation belongs with its canonical broker package and signed release in `~/agents`, not split across executable logic in `~/dotfiles`, authority prose in skills, and runtime state elsewhere. Dotfiles should converge to declarative host configuration invoking the installed public interface. Skills should trust a stable signing identity plus installed public status/release/policy digests; volatile source-file SHA pins are temporary first-bootstrap evidence, not the steady-state design. `jj` proves reviewed history but not which privileged bytes are loaded; SQLite may record state but does not replace signed runtime attestation. Scope: remote-sudo, GDM broker, Bitwarden/Google login helpers, and future desktop auth capabilities. Falsifier: a required pre-install bootstrap cannot be made self-authenticating without one narrowly reviewed immutable pin.
- Human-presence coordination generator (Arthur, direct, 2026-07-24, provenance A): OMP sessions should share a global advisory presence signal so background automation can distinguish likely-active from likely-AFK and choose whether to defer visible UI or present a prompt. Store source-specific fresh observations in the existing owner-only OMP control-plane SQLite substrate behind a versioned API/CLI; include source, machine/user, observed/expiry timestamps, idle duration, lock/session state, confidence, and generation. Multiple sources remain separate rows and consumers derive the current view—never let one heuristic overwrite truth. Presence may schedule or suppress interruptions, but it is never authentication evidence, approval, grant authority, or permission to weaken a security boundary. Falsifier: if shared-DB contention or stale cross-host clocks make the view less reliable than direct host probes, keep per-host stores and aggregate through the same public API.
- Route browser, extension, and resource-heavy test work to the Ubuntu desktop by default when the work has no Mac-specific dependency; use persistent dedicated profiles there. Keep macOS/TCC, Keychain, Safari, Apple Mail, and other Apple-specific behavior on the Mac. Review remote work only through a private Tailscale Serve route or SSH tunnel—never Tailscale Funnel or public exposure by default. A portless name is local to its serving host and needs an explicit private bridge before the Mac can reach it.
- Laptop resource cap (Arthur, 2026-07-20, provenance A, prompted by watchdog panic): sustained browser matrices, fuzz/property campaigns, full TUI stress runs, and other non-Mac-specific heavy QA belong on the Ubuntu desktop. Keep local child concurrency/browser tabs bounded and never render the entire inherited dirty-repo diff through Difftastic. Falsifier: measured local pressure stays comfortably bounded and remote dispatch adds greater cost than value.
- For Arthur's mailbox workflows, do not use `gmcli`; use Apple Mail as the preferred mailbox surface. Generator/rationale: keep mail identity in the native configured account rather than maintaining a parallel Gmail OAuth token store. Scope: routine account establishment and refresh remain automation responsibilities through the reviewed credential path; interrupt Arthur only for provider-required 2FA, passkey, recovery, consent, security changes, or material account ambiguity. Apple Mail automation must still be implemented and verified before use. (Arthur, 2026-07-18; narrowed by Arthur's direct 2026-07-23 routine-authentication decision; provenance A.)
- Prefer Kagi for web search while Arthur is paying for it and its quality and value remain worthwhile. The live authority is [`.omp/config.yml` `providers.webSearch`](../../.omp/config.yml), not this prose. Prefer the browser-session path so the consumer subscription is used; Kagi API usage is separately metered and must not be inferred from a paid consumer subscription.
- Persist browser identities in dedicated Ubuntu browser profiles backed by their keyrings. Establish or refresh routine authenticated sessions with the matching active broker/password-manager automation; missing automation is infrastructure to report and repair, not a request for Arthur to log in manually. Never copy or `rsync` browser userdata or cookies across operating-system keychains.
- Standard local developer-tool license prompts are pre-approved when acceptance is required to use software Arthur already requested or installed; proceed without asking. This does not pre-approve purchases, subscriptions, data/content licensing, or unusual third-party commitments. Generator: refusing the standard license makes the requested tool unusable, and Arthur does not want ideological license review overhead. (Arthur, 2026-07-17, provenance A.)

- For OMP subagents on Gemini, use the Antigravity subscription lane, never the raw Google Gemini API lane: an inherited `GEMINI_API_KEY`/`GOOGLE_API_KEY` can silently route work onto paid API billing. Guard the billing boundary in config/code, not convention. (Which roles, if any, currently run Gemini is config — read the overlay; the old `gemini-3.5-flash-low` worker default is retired.)
- Generalize the Jimeng worker-packet pattern across workstreams: coordinator owns the task packet, exact owner paths, excluded paths, acceptance checks, and integration validation; workers avoid root manifests/configs and return proof commands rather than running project-wide gates.
- Treat speech-to-text variants like “Gming” or “Jming” as Jimeng/Dreamina context when the surrounding request is about provider/API/UGC generation work.
- For CLI work, prefer Effect APIs and Effect CLI for new command surfaces or substantial CLI refactors. Keep handwritten parsers only for tiny legacy surfaces or when converting them would distract from the requested change. Add lint guardrails for new custom parsers when practical.
- For external JSON/file/process/API data, use Effect Schema boundary decoders before data enters core code. Avoid raw `JSON.parse` in core implementation; if raw parsing is unavoidable at an IO edge, immediately decode and return typed data.
- For browser/UI verification, run headless/background first. Prefer CuaDriver for background visual/AX/browser flows that would otherwise steal focus; use CDP/Playwright/Puppeteer headless for DOM/network checks. Do not open foreground/visible browser automation while Arthur is using the machine unless he explicitly asks for it.
- For operations in owned `packages/*` and `apps/*`, keep dependencies, runtimes, setup, and task entrypoints in the package's mise configuration where available, and have agents use `mise run` rather than ad hoc `uv`, `npm`, `bun`, or global commands. Existing `package.json` scripts may remain as implementation behind mise tasks where already established. Generator: one package-owned interface makes setup reproducible and prevents agents from bypassing declared tooling. Scope: owned package/app operations only, not standalone utilities or third-party repositories. (Arthur, 2026-07-18, provenance A.)
- Packages are self-contained: each `packages/*`/`apps/*` package owns its scripts (`typecheck`, `test`, `check`, `dev`, CLI aliases) in its own package.json. Do not add per-package scripts or convenience aliases to the root package.json, and do not extend the root `typecheck`/`test` mega-chains for new packages — new packages self-gate with in-package `bun run check`. The root chains are legacy; migrating them to workspace-filtered scripts (e.g. `bun run --filter`) is a harness-stream task, not something to grow further. Rerun commands in docs/proofs should be package-local or full-path (`bun packages/<pkg>/src/cli.ts ...`), never root-script names.
- Product-surface LLM calls (dashboard ask-synthesis, card generation, any always-on endpoint) run on Arthur's subscription lanes via OMP oneshot (`omp -p --no-session --no-tools --model <lane>`), defaulting to cheap lanes (`google-antigravity/gemini-3.5-flash`, Kimi k2.5 fallback). NEVER route product calls through the Fable/orchestrator model or other expensive lanes; guard against it in code, not just convention.
- Dashboard/product UI: Arthur prefers shadcn-style components (chatcn for chat surfaces) when a UI graduates beyond a single static page; UI design work goes to the Opus/designer lane. Primary viewport is a cmux half-screen pane (~650–900px wide) — design narrow-first, wide is the afterthought. Reviewer surfaces need vim-style keyboard navigation (j/k/Enter/Esc/g/G, ? for keymap).
- Stream dashboards show only their own stream's artifacts: proof docs follow `docs/qa/<stream>-<task>.md` naming and viewers filter by that prefix; cross-stream synthesis is a separate, explicitly-labeled surface.


- For authenticated website debugging/reversal, start headless/background when possible, but switch quickly to a logged-in non-headless profile when state only settles in the real browser. Use CuaDriver for background UI state, headed CDP for DOM/network truth, save the winning request plus artifacts, then replay it directly to separate payload-parity bugs from provider denial.

## Decision Log
### 2026-07-17

Arthur clarified that standard license acceptance for requested local developer software is operationally pre-approved. Agents should accept and continue without interrupting him; spend and nonstandard contractual commitments remain separate approval boundaries.

Arthur clarified the interruption boundary (A, direct, 2026-07-17): routine approval gestures are not collaboration. Agents should present the system Touch ID/authorization prompt themselves and continue after approval; a chat interruption is justified only when Arthur must supply information, complete a nondelegable identity challenge, accept material risk/spend, or make a real decision. Falsifier: the prompt cannot be safely triggered in the background or proceeding would commit beyond the already requested operation.

**Preference triage (A~, Arthur paraphrased, 2026-07-17).** Generator: minimize avoidable global machine mutation, credential exposure, identity copying, and public network exposure while maximizing safe end-to-end automation on the host that best fits the work. **Reason:** an unnecessary Homebrew `rbw`/`pinentry` experiment upgraded unrelated global dependencies despite an existing repository helper; the same review favored official credential and browser primitives over duplicated private protocols or copied identity stores. **Scope:** missing developer tools, credential-assisted exact operations, browser/extension/resource-heavy testing, private review surfaces, browser identity persistence, and web-search provider selection. **Falsifier:** choose another path when `mise` cannot supply a required macOS formula/app, the behavior is genuinely Apple-specific, the exact operation cannot be safely bounded behind biometrics, a private bridge is unavailable, or Kagi's subscription value/quality no longer justifies the preference; live configuration always wins over this record.


### 2026-06-09

Arthur decided that background macOS/browser automation should default to CuaDriver rather than the older focus-restore window guard approach. Future agents should try CuaDriver first, especially when asked to automate apps like Messages or Telegram, and should only drop down to CDP/Playwright when the task actually needs browser protocol access.

Arthur also asked for top-level task tracking instead of relying on agents to remember all open threads. Use `TASKS.md` as the index. For React UI work, prefer UI snapshot testing when adding or changing UI tests.

Arthur clarified that proof of work should be planned as part of implementation, especially for UI/UX and integration work. UI changes should usually leave behind a visual QA report plus a video walkthrough artifact. API and provider work should leave behind tests, logs, snapshots, or saved outputs that directly prove the reversed/setup behavior works and reduce reviewer effort.

Arthur clarified that ad hoc Python scripts needing libraries should run via `uv run --with <package> python ...`, not direct `python3`.

### 2026-06-10

Arthur clarified during Jimeng/Dreamina API reversal that agents must not interrupt his active computer use. For browser/login flows, use background CDP, CuaDriver, browser-use, direct API calls, saved sessions, and passive network capture first. Do not call `bringToFront`, foreground tabs, or use visible UI automation unless he explicitly opts in.

Arthur clarified that missing developer CLI tooling should preferably be installed through `mise`. For example, use the existing mise-managed `ast-grep` CLI for structural bundle/code searches instead of adding `@ast-grep/cli` as a project dependency unless CI needs it.

Arthur clarified that API/provider integrations should use runtime schema validation, not just TypeScript interfaces or lint. The schema should be permissive about extra JSON fields but strict about the response paths the pipeline depends on, so future provider changes fail with clear contract-drift errors and can be rerun from saved proof commands.

Arthur clarified that API reveng should be sped up with mixed dynamic/static tooling. Use CDP network truth, `ast-grep`/targeted bundle search, and explicit replay/probe tools together; avoid spending long stretches reading minified bundles when a captured request plus replay comparison can prove the contract faster.

The Jimeng/Dreamina workflow now has `jimeng-browser-proxy capture-analyze` and `jimeng-browser-proxy discovery-worklist` for this loop. After each meaningful `jimeng-network-recorder` capture, run `capture-analyze` first to rank endpoints, classify replay risk, summarize shapes, and emit local candidate JSON; then run `discovery-worklist` to merge analyzer/probe/static evidence into a prioritized next-slice queue before reading frontend bundles by hand.

### 2026-06-11

Arthur clarified that the Jimeng/Dreamina client is prototype infrastructure, so agents should optimize for a fast, clean architecture rather than compatibility-preserving micro-slices. Avoid proof ritual for backend refactors: passing tests, typecheck, schema fixtures, snapshots, and replayed cassettes are enough unless a live provider contract needs discovery or drift refresh.

For provider/API clients, prefer a shared HTTP transport with `live`, `record`, `replay`, and `fixture` modes over command-specific `--dryRun`/proof flags everywhere. Use dependency injection through Effect `Context`/`Layer` or a thin equivalent so tests swap HTTP/cache/clock/filesystem services directly.

For CLI work, prefer Effect CLI for new or substantially refactored surfaces instead of continuing to grow hand-written argument parsing. For large analyzer/inventory outputs, prefer structured endpoint registries plus snapshot/fixture tests over long inline string fixtures and assertion walls.

Arthur clarified that Vitest snapshots feel significantly better after the Jimeng endpoint-registry trial. For TypeScript provider/client packages, use Vitest snapshot tests from now on for generated Markdown, endpoint registry gaps, normalized response summaries, request plans, CLI summary output, and cassette metadata. Keep existing Bun tests where they are already concise or testing tiny behavior; convert verbose contract/report tests as they become painful.

Arthur clarified that "no-spend" should not be the primary work selector for Jimeng/Dreamina. It is a safety/proof constraint, not the priority function. The priority function is highest-value UGC API/workflow first, then speed/ease as a tie-breaker. When the valuable next step is paid or mutating, ask for explicit approval with concrete commands and expected artifacts.

### 2026-06-13

Arthur clarified the OMP model split: GPT-5.5 stays the parent/orchestrator; `gemini-3.5-flash` should be the default for simple non-core implementation workers and packet review; latest Kimi is the fallback only when Gemini is unavailable or rate-limited. Core abstractions and surfaces other work will build on should stay with GPT-5.5/main or a stronger implementation agent.

> **Status 2026-07-03:** Superseded for Fable-orchestrated sessions — Fable routes by fit (see `docs/fable/charter.md`); this split remains only a default for non-Fable GPT-orchestrated sessions.

Arthur also clarified the tooling default for ongoing Jimeng work: prefer Effect APIs and Effect CLI for command surfaces, and keep browser/UI checks headless/background. Use CuaDriver when visual/browser automation needs to run in the background without disrupting his active desktop.

Broader lessons about decomposition, caching validated layers, and parallel agents live in `docs/state/agent-iteration-lessons.md`.

### 2026-06-13 follow-up

Arthur clarified that the Jimeng fast packet workflow should become the general repo workflow: packetized owner paths, exact validation, and coordinator-owned merge/QA. He also wants GPT-5.5 to be the fallback worker while subscription usage is acceptable, with Kimi demoted to explicit/unavailable fallback.

Arthur wants repo implementation SOPs to converge on Effect: Effect CLI for command surfaces, Effect Schema for external JSON and file/process/API boundaries, and AST lint rules to prevent new bespoke CLI parsers or unvalidated JSON parsing.

Arthur wants design and QA knowledge extracted into reusable agent skills/personas: a Refactoring UI-derived private design skill if the local PDF can be found and used, React static-analysis/design-review personas, and Effect AI docs distilled into implementation guidance.

Arthur is considering Git checkpoint discipline now and Jujutsu later for stacked agent work. He also wants less scattered timestamp-only Markdown metadata; evaluate a centralized SQLite/ledger approach before moving docs wholesale.

Arthur clarified that raw HTML string assertions and broad markup snapshots are the wrong default for owned React UI. They are appropriate for real HTML scraping/parsing boundaries, not for testing components we control. UI tests should target state before render, semantic behavior, and visual QA artifacts; snapshots belong to stable normalized contracts or narrow intentional component variants.

### 2026-06-14

Arthur clarified that CuaDriver-backed automation is still considered broken if it steals focus through browser hotkeys. For browsers, launch a background dev-mode instance with CuaDriver and use CDP/DevTools for tab creation, navigation, activation, close, DOM, and network work. Prefer CuaDriver `launch_app`, `get_window_state`, AX `element_index` actions, `page`, MCP, or CDP for browser protocol work. Treat browser shortcuts such as `cmd+l`, `cmd+t`, `ctrl+tab`, and `cmd+shift+w` as explicit intrusive fallbacks, not default background automation, even when posted to a target pid/window.

