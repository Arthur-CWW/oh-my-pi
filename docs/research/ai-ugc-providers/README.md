# AI UGC Provider Research

Research outputs and provider comparisons for cheap AI UGC generation APIs.

Keep vendor-specific price tables, GPT-Pro outputs, founder/company notes, and transient reliability findings here — not in `docs/state/video-creative-direction.md`.

Provider research is expensive enough to preserve: commit useful summaries, prompts, source URLs, and price/reliability snapshots so changes can be tracked over time.

Durable provider direction belongs in state only when it changes default behavior across future sessions, e.g. "prefer cheap PAYG APIs over another subscription" or "Kie is the cheap experiment default; fal is fallback/benchmark."

## Files

- `kie-fal-higgsfield-chatgpt.md` — ChatGPT Pro research output comparing Kie, fal, and Higgsfield/Pixazo.
