{
  "name": "@wirebabel/pi-dynamic-workflows",
  "version": "0.1.0",
  "private": true,
  "description": "Vendored pi-dynamic-workflows extension with adversarial review prompt templates for Pi.",
  "type": "module",
  "keywords": [
    "pi-package",
    "pi",
    "workflow",
    "agents",
    "adversarial-review"
  ],
  "license": "MIT",
  "scripts": {
    "typecheck": "tsc -p tsconfig.typecheck.json --noEmit",
    "test": "bun test ./test ./src"
  },
  "dependencies": {
    "acorn": "^8.16.0"
  },
  "peerDependencies": {
    "typebox": "*",
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  },
  "devDependencies": {
    "bun-types": "^1.3.9",
    "typescript": "^5.9.3",
    "@earendil-works/pi-ai": "^0.75.5",
    "@earendil-works/pi-coding-agent": "^0.75.5",
    "@earendil-works/pi-tui": "^0.75.5"
  },
  "pi": {
    "extensions": [
      "./extensions/workflow.ts"
    ],
    "prompts": [
      "./prompts"
    ]
  }
}
