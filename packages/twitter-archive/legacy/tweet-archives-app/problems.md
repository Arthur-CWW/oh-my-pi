> Can you clean up the claude.md with whatever mistakes you made?
These are some of the issues i see from the current implementation, don't start but let's make a few simple tasks specs in markdown:
Also can you clean up the things so that the scripts are in a separate directory, and can we resolve the links as quote tweet (don't start yet
  just right down that task ) . I need to do the YouTube iframe thing and the PNGs are not resolving correctly. Also I think Twitter uses a link shortener, so the Twitter URL in the original tweet, you have to
   get it from the links column. Don't start on any of the implementation tasks. We just want to clean up first and just write down the implementation tasks that we want. And also, I want J and K for
  navigation and H and L for navigating left and right. J and K to navigate up and down the tweets. I also have a... I want to be able to debug the tweets when they look weird, so if I press i, the tweet that
  I'm currently hovered over, it should display the JSON for that tweet which you used to render. And display all the properties. Maybe in a side draw (The Sheet component from shadcn)