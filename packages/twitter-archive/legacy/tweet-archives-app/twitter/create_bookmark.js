// Redacted legacy scratch file.
//
// The original standalone tweet-archives repo contained a copied browser
// request for X/Twitter bookmark-folder creation, including Authorization,
// CSRF, cookie, and transaction headers. Those credentials are intentionally
// not preserved in this monorepo import.
//
// If this workflow is ever rebuilt, use an approved API/session helper that
// reads credentials from a local secret store or environment variables and do
// not commit captured browser headers.

throw new Error("Legacy bookmark request capture was redacted; do not use committed browser credentials.")
