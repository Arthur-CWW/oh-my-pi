{
  "name": "@wirebabel/fleet-board",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local-first fleet Kanban board",
  "scripts": {
    "dev": "bunx portless fleet-board bun run src/server.ts",
    "build:ui": "bun build public/board.ts --outfile public/board.js --target browser --format esm && bun build public/watch.ts --outfile public/watch.js --target browser --format esm",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "bun-types": "^1.3.9",
    "typescript": "^5.9.3"
  }
}
