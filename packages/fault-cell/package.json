{
	"name": "@wirebabel/fault-cell",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"description": "App-agnostic disposable NixOS fault cell: typed scenario operations, real VM faults, hashed run manifests",
	"bin": {
		"fault-cell": "./src/cli.ts"
	},
	"scripts": {
		"check": "bun run typecheck && bun run test",
		"typecheck": "tsc --noEmit",
		"test": "bun test test/",
		"provision": "bash scripts/provision-runtime.sh",
		"cli": "bun src/cli.ts"
	},
	"dependencies": {
		"effect": "4.0.0-beta.98"
	},
	"devDependencies": {
		"@types/node": "25.9.4",
		"bun-types": "^1.3.9",
		"typescript": "^5.9.3"
	}
}
