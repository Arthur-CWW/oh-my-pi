{
  "name": "@wirebabel/control-plane",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "SQLite ledger foundation for the agent control plane",
  "exports": {
    ".": "./src/index.ts"
  },
  "bin": {
    "control-plane": "./src/cli.ts"
  },
  "omp": {
    "extensions": ["./src/omp-publisher.ts"]
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "check": "bun run typecheck && bun run test",
    "cli": "bun run src/cli.ts",
    "proof": "bun scripts/m1-proof.ts && bun src/cli.ts ingest --from test/.tmp/m1-proof/outbox/session-m1-proof.jsonl --db test/.tmp/m1-proof/ledger.sqlite --json && bun src/cli.ts status --json --db test/.tmp/m1-proof/ledger.sqlite && bun src/cli.ts model-calls --json --db test/.tmp/m1-proof/ledger.sqlite"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.0",
    "effect": "^4.0.0-beta.71"
  },
  "devDependencies": {
    "bun-types": "^1.3.9",
    "typescript": "^5.9.3"
  }
}
