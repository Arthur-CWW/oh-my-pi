# Control plane P1 report

Worker `CtrlPlaneFoundation` (gpt-implementer) wrote the full package but was hard-cancelled at 60 requests before running the gate; coordinator fixed 10 typecheck errors (env weak-type, `get()` nullability, drizzle bun-sqlite `.run()` → `.returning().all()` for insert detection) and gated.

## Commands run (coordinator)

- `cd packages/control-plane && bun install && bun run check` → typecheck clean, 2 tests pass (27 assertions)

## Files

- package.json, tsconfig.json
- src/schema.ts — all 9 row families, hot-path indexes, UNIQUE(sessionId,seq) on events
- src/migrate.ts — user_version-gated migration 0001, WAL + synchronous=NORMAL pragmas
- src/errors.ts — StorageError, ArtifactError (Schema.TaggedError)
- src/ledger.ts — LedgerStore Context.Service + Layer via openLedger(dbPath); idempotent INSERT OR IGNORE writes returning {inserted}; ingestBatch in one transaction; sha256-derived artifact ids; no Date.now/Math.random/randomUUID in src
- src/index.ts
- test/ledger.test.ts — every row family, idempotent double-publish, unknown-event-kind verbatim round-trip (incl. NUL), artifact linkage, migration idempotence, :memory: open

## Contract deviations

- `ingestBatch` untested here — covered by P4a tailer packet (double-ingest DST invariant).
