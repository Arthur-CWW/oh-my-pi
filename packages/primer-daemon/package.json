{
  "name": "@wirebabel/primer-daemon",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local-first primer daemon for reading evidence retrieval",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "check": "bun run typecheck && bun run test",
    "cli": "bun src/cli.ts",
    "dashboard": "bun src/dashboard.ts",
    "dev": "bunx portless@latest primer bun src/dashboard.ts"
  },
  "dependencies": {
    "effect": "^4.0.0-beta.71"
  },
  "devDependencies": {
    "bun-types": "^1.3.9",
    "typescript": "^5.9.3"
  }
}
